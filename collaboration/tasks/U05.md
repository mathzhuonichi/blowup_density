# U05: Resolve pinned upstream toolchain compatibility

Kind: compatibility. State: in-progress. Owner: erenup. Priority: P0.

## Goal

Audit selected import closures under Lean/mathlib 4.34.0-rc2 before a future source port, or keep independent checkouts until compatibility is demonstrated. Preserve both original pins; no forced lockfile upgrade and no proof edits in this planning task.

## Dependencies

U01, U04

These are mathematical dependencies. Specification work may inspect available source before the prerequisite is fully certified.

## Starting evidence

- [formalization/lean-toolchain](../../formalization/lean-toolchain)
- [vendor/HeliCorgi/lean-toolchain](../../vendor/HeliCorgi/lean-toolchain)
- [vendor/HeliCorgi/lakefile.lean](../../vendor/HeliCorgi/lakefile.lean)

## Deliverable and acceptance

Check a minimal upstream import closure under the chosen pinned toolchain; report compatibility or exact failures. Do not mass-upgrade both dependency trees.

Registered component contracts: none yet.

A component test does not close its parent theorem. For an implementation PR, first provide a reviewed concrete specification and register its typed binding and axiom test. No hypothetical proposition fields or admitted theorem may stand in for a missing PDE statement.

Run `make check`; for Lean implementation work also run `make test` and `make test-mutations`. Include the task ID and exact commands in the PR.

## Attempts

Lane 010, part 2 (port). Full record: [research/U05/PORT.md](../../research/U05/PORT.md).
Everything below is something that failed, or a documented deviation from the probe's plan.

1. **`example := @MNS2.r3HelmholtzPressure` did not compile.**
   `lake build NSFormalization.Section4.HeliCorgiPort` failed after 3 m 31 s with
   `failed to compile definition, consider marking it as 'noncomputable' because it
   depends on 'MNS2.r3HelmholtzPressure', which is 'noncomputable'`. The 84 vendored
   modules were unaffected. Fixed by writing `noncomputable example := …` for that one
   reference; the other five references are `Prop`-valued and need no marker.

2. **The smoke module alone does not build all 84 modules.** It imports the three
   unbroken targets, whose closure is 77 modules; the other 7
   (`R3ConjugationReflection`, `R3FourierConjugationBridge`,
   `R3StokesConjugationEquivariance`, `R3LerayConjugationEquivariance`,
   `R3ConvectionConjugationEquivariance`, `EndpointSafeTwoSpaceRestart`,
   `EndpointSafeTwoSpaceConcatenation`) are reached only through the continuation branch.
   All 84 are covered once `FormalPatched.R3MildContinuation` is built too; to check the
   library on its own use `lake build NSFormalization/Formal` (34.8 s, 84 oleans).

3. **The probe's recommended fix was rejected and replaced.** `REPORT.md` §(c) proposes
   substituting `⟨x, h⟩ → NNReal.mk x h` at ~42 sites in 10 files. Those sites include the
   *statements* of `MNS2.IsMildSolutionOn` (`EndpointSafeTwoSpaceDuhamel.lean:613`) and
   `MNS2.r3EndpointSafeProjectedMild_equation_at_time`
   (`R3EndpointSafeProjectedDuhamel.lean:171`), which live in the 84 good modules compiled
   in place from `vendor/`. Applying it would have required vendor edits — forbidden by
   `CONTRIBUTING.md` and by `experiments/build_changed_lean.py`, which raises on any path
   under `vendor/HeliCorgi/` — and it is what produced the two follow-on failures the
   probe recorded at `EndpointSafeTwoSpacePicard.lean:832` and
   `R3ProjectedMomentumDuhamelInfrastructure.lean:779`. **No `NNReal.mk` substitution is
   used in this port.** Instead the one failing `rw` is replaced by a term-mode
   application of the same lemma at the explicit time (`have hconj := …`), which needs no
   pattern match and therefore never meets the `implicit`-transparency check. Nine added
   lines in one file; the other three copies differ from vendor only in their imports.

4. **The three never-elaborated modules turned out clean.** `R3QuantitativeLifespan`,
   `EndpointSafeTwoSpaceUniqueness` and `R3MildContinuation` had never been type-checked
   at 4.34.0-rc2; the probe estimated 0–4 h of unknown breakage. They compile with zero
   source changes and zero warnings.

5. **`build_changed_lean.py --base-ref erenup/integration --dry-run` reports `none`.**
   Not a defect in the port: the script diffs `<base>..HEAD`, and this lane is forbidden
   to commit, so the five new/changed files sit in the working tree where the script
   cannot see them. Exercised instead through a stand-in that calls the script's own
   `targets()` over the committed diff plus the working-tree and untracked changes and
   issues the same `lake build` from `verification/`; it lists exactly
   `FormalPatched.{EndpointSafeTwoSpaceUniqueness, R3MildContinuation,
   R3QuantitativeLifespan, R3RealLocalMildSolution}` and
   `NSFormalization.Section4.HeliCorgiPort`, and builds them (exit 0, 2 m 07 s cold).
   The unmodified script will produce the same list once the branch is committed.

6. **`make snapshot` now fails; `make check` does not.** `formalization/lakefile.toml` is
   listed in `logs/FORMALIZATION_SOURCE_MANIFEST.json`, so editing it sets
   `source_hashes_match: false` and `check_formalization_plan.py --snapshot --check`
   asserts on `['formalization/lakefile.toml']`. The manifest was outside this task's
   write scope and was not regenerated; the merger should decide whether to re-snapshot.

7. **Not attempted, and still open.** The 52 vendored warnings are untouched, so nothing
   under a `warningAsError = true` library (`verification`'s `Tests`,
   `vendor/NavierStokesAndEuler`'s `Euler`) may import `Formal.*` or `FormalPatched.*` as
   things stand. No contract, binding or acceptance test was registered against any
   ported declaration; making one is separate work.
