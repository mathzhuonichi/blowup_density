# Lane 392-T24-Uc1-Ua1 — T24 wave 1 (codex units): Uc1 `zero_from_rest` (prop:conservative) and Ua1 the geometry/kinematics fields of `AffineVariationAPI` (prop:affine, whole space over raw packet fields)

You are a Lean 4 (v4.34.0-rc2 + Mathlib) proof worker on the repository checked out at your working directory
`/data_8T/ping/blowup_density/.claude/worktrees/392-T24-Uc1-Ua1` (git branch `erenup/392-T24-Uc1-Ua1`, based on `origin/erenup/integration-section3`).
Read `CLAUDE.md` ("合同 import 规则": `formalization/` cannot import `Contracts.*` — state whole-space units over raw packet fields as `Section3/T14/PacketEnergy.lean` does and
convert in the probe), **`research/T24/T24_SPLIT.md` §0 and units Uc1, Ua1 (targets verbatim with `Spec.lean` line numbers, inputs with file:line, routes)**, `research/T24/Spec.lean`
(`ConservativeForcingAPI` `:1391-1414`, `PeriodicPotentialT :1367`, `conservativeForceT :1372`; `AffineVariationAPI` `:1000-1117`, `affineVelocity`, the cylinder/support definitions),
`research/T24/RECONCILIATION.md` §3, `formalization/NSFormalization/Paper1/ConservativeForce.lean` (`zero_of_negative_gradient_on_Ico :91` — grep the exact name), the canonical T10/T11
modules (`Section3/T11/LocalTheory.lean` `ClassicalSolutionT` and its fields; the `rfl` bridges of T11 U1 `Section3/T11/FlowConversion.lean`), `Contracts/V1/Packet.lean` (`PacketAPI`
clauses for the probe), and the top 40 lines of `logs/LESSONS.md`.

## Ground rules (non-negotiable)
- Work ONLY inside this worktree; never `git push`, merge or rebase; committing on your branch is allowed.
- Lean: `. scripts/lean-env.sh`; `lake` only from `verification/` with `LEAN_NUM_THREADS=6`.
- No `sorry`/`admit`/`axiom`/`native_decide`; `set_option maxHeartbeats N in` only per declaration, `N ≤ 400000`, commented. No edits to existing modules; new files only.
- **No placeholders, no aliases, no named inputs, no goal repackaging.** An honest partial with the exact residual statement and error text beats a stub.
- Every declaration must print exactly `[propext, Classical.choice, Quot.sound]`.

## Goal
1. `formalization/NSFormalization/Section3/T24/Conservative.lean` (namespace `NSFormalization.Section3.T24`): the T24c canonical vocabulary (`PeriodicPotentialT`, `conservativeForceT`
   restated verbatim over canonical T10/T11) and `theorem zero_from_rest` verbatim (`Spec.lean:1408-1414`): `∀ ν, 0 < ν → ∀ T, 0 < T → ∀ φ, PeriodicPotentialT φ → ∀ S : ClassicalSolutionT ν 0 (conservativeForceT φ) T, ∀ t ∈ Ico 0 T, ∀ x, S.velocity (t,x) = 0`
   — route: `ConservativeForce.lean:91 zero_of_negative_gradient_on_Ico` with `u := S.velocity`, `p := S.pressure`, transferring `S.velocity_smooth`/`pressure_smooth`
   (`ContDiffOn` on `Ico 0 T`), `S.velocity_periodic`/`pressure_periodic`, `φ`'s `PeriodicPotentialT` clauses, `S.divergence`, `S.momentum` (`conservativeForceT φ = −pressureGradient φ`), `S.initial` (`a = 0`).
2. `formalization/NSFormalization/Section3/T24/AffineBasics.lean`: the T24a canonical vocabulary over raw packet fields (`u p f : VelocityField`, carrier, the `PacketAPI` clauses used as
   explicit hypotheses — `affineVelocity`, the cylinder, `admissible`, `affineCkSeminorm` if Ua1 needs it) and the five Ua1 fields verbatim as theorems: `radius_pos`, `window` (the statement's
   parameter hypotheses), `zero_initial` (from `u (0,·) = 0` + `b (0,·) = 0` since `tsupport b ⊆ Ioo τ₀ τ₁ ×ˢ ball`, `τ₀ > 0`), `late_agreement` (`b = 0` for `t ≥ τ₁`), `distinct`
   (`add_right_injective` on `affineVelocity`).
3. Probe `research/T24/probes/uc1_ua1_closes.lean` importing `Contracts.*` and closing the Spec's fields (T24c fields directly; T24a fields via `Bindings.packet ν hν`'s raw fields) by `exact`.

## Deliverables
1. The two modules; 2. the probe; 3. `research/T24/ATTEMPTS_UC1_UA1.md`, `research/T24/axioms_uc1_ua1.lean`, status in `research/T24/T24_SPLIT.md`, report `research/T24/REPORT_392.md`.

## Gates
`cd verification && LEAN_NUM_THREADS=6 lake build NSFormalization.Section3.T24.Conservative NSFormalization.Section3.T24.AffineBasics` (0 errors), `lake env lean` on modules / probe /
axioms file; `make check`.

## Report
Commit on your branch; end with four parts. Also write it to `research/T24/REPORT_392.md`.
