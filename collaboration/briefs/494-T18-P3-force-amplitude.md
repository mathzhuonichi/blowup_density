# Lane 494-T18-P3-force-amplitude — close Partial P3: Remark 3.13, divergence of the force amplitudes (graph node `R313`)

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/494-T18-P3-force-amplitude` (branch `erenup/494-T18-P3-force-amplitude`, = `origin/erenup/core`). Read `collaboration/briefs/COMMON_PHASE5.md` first, then `PLAN.md` §1 row P3, `proof_graph.json` node `R313` (Partial: "there is no separately exported divergence-of-force-amplitudes conclusion"; `completion_from: ["S33","C35_T"]`), the article `paper/revised/sections/03-torus.tex` Remark `rem:peaks`: `‖g_ε − g‖_{L∞_{t,x}} ≥ ε⁻³‖F‖_∞ − Cε⁻² → ∞`, "the first term is the amplitude of F_ε and the second bounds H_ε; F is nonzero by eq:packetenergy since U blows up", and the code: T18 `Section3/T18/Assembly.lean` (`InsertionData`, `PeriodicInsertionAPI`; the field `force_difference_eq`/`forceDifference` giving `force ε − g = H_ε + F_ε` — grep `force_difference_eq` in `Section3/T18/SobolevRate.lean:186`), T15 `Section3/T15/Scaling.lean` (`periodizedScaledForce f x₀ T ε`: lattice sum of `ε⁻³ f((t−T)/ε², (x−x₀)/ε)` — read the exact definition and exponent), `Section3/T15/SingleCopy.lean` (`tsum_eq_single_copy_of_mem_cube`, `force_singleCopy`: inside the cube only one lattice term is nonzero), T17 `Section3/T17/Correction.lean` (fields `forceProfile_bound`-type uniform bound on `rescaledForceProfile` at derivative order 0 and the chart formula `correctionForce ν v D ε (correctionChartPoint …) = (ε^2)⁻¹ • rescaledForceProfile …`, plus the support of `correctionForce` — so `|H_ε| ≤ C ε⁻²` everywhere), T14 `Section3/T14/PacketEnergy.lean` (`energy_le_work_of_packet`, `work_eq_square_of_packet`) and the registered packet clauses (`verification/Contracts/V1/Packet.lean`: `force_smooth`, `force_support : CompactPositiveTimeSupport`, `SpeedUnboundedAtOne`), T19 `Section3/T19/Threading.lean` (`insertion`, exports).

## Goal
1. `Section3/T18/ForceAmplitude.lean`:
   (a) `packetForce_ne_zero`: the registered packet force is not identically zero — if `f = 0` then by `energy_le_work_of_packet` the work vanishes, the kinetic energy is zero, the (continuous) velocity slices vanish, contradicting `SpeedUnboundedAtOne` (state it for the raw packet clauses the T14 theorems take, then instantiate at `Bindings.packetImportFamily`… in the probe/binding only; `formalization/` cannot import `Contracts`/`Bindings`).
   (b) `packetForce_sup_pos`: `0 < ⨆ z, ‖f z‖ₑ` (or the real sup over the compact support, attained by continuity — pick the ENNReal `⨆` form so no finiteness side condition is needed, and prove it is finite too).
   (c) `periodizedScaledForce_amplitude`: `⨆ z, ‖periodizedScaledForce f x₀ T ε z‖ₑ = ofReal (ε⁻³) * ⨆ z, ‖f z‖ₑ` for `ε ∈ Ioc 0 ε₀` (single copy in the cube + the change of variables `z ↦ (T + ε² τ, x₀ + ε y)`; `≥` suffices for the remark if `=` is awkward — say which you proved).
   (d) `correctionForce_amplitude_le`: `⨆ z, ‖correctionForce ν v D ε z‖ₑ ≤ ofReal (C * ε⁻²)` from the record's order-0 profile bound and chart formula (zero off the chart image).
   (e) The remark for any `data : InsertionData` and `A : PeriodicInsertionAPI data`: `∀ ε ∈ Ioc 0 A.ε₀, ⨆ z, ‖A.force ε z − data.g z‖ₑ ≥ ofReal (ε⁻³ * ‖F‖_∞ − C * ε⁻²)` and `Tendsto (fun ε ↦ ⨆ z, ‖A.force ε z − data.g z‖ₑ) (𝓝[>] 0) atTop`, with `C` the record's constant; then instantiated at T19's `insertion` (fixed ball) as `forceAmplitude_diverges`.
2. Contract `T03.force_amplitude` (new: `Contracts/V1/ForceAmplitude.lean` stating the divergence for the registered inserted family — over the registered `PeriodicInsertionAPI` of `Contracts/V1/PeriodicInsertion.lean` and, as a corollary, for the raw-data family of `T03.periodic_insertion` — `Bindings/ForceAmplitude.lean`, `Tests/ForceAmplitude.lean`), registry entry (scope: "Remark 3.13: the sup norm of g_ε − g is at least ε⁻³‖F‖_∞ − Cε⁻² and diverges as ε → 0 for every inserted family of the registered record; F ≠ 0 is proved from the packet energy identity and the blow-up clause").
3. Blueprint per COMMON §3: node `R313` → Closed, `depends_on: ["S33","C35_T"]`; `RESULT_MAP.md` Remark 3.13 → Closed; guide row `rem:peaks` → Closed; counts; regenerate; gates; `make paper`.
Report `research/T18/REPORT_494.md`.
