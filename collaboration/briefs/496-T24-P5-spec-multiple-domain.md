# Lane 496-T24-P5-spec-multiple-domain — Partial P5 step 1: the bounded-domain record for Proposition 3.16 (spec only, no proofs)

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/496-T24-P5-spec-multiple-domain` (branch `erenup/496-T24-P5-spec-multiple-domain`, = `origin/erenup/core`). Read `collaboration/briefs/COMMON_PHASE5.md` (context; this lane writes a specification, not proofs), `PLAN.md` §1 row P5, `proof_graph.json` node `M316_B` (Partial: "The bounded-domain no-slip variant has not been assembled"; `completion_from: ["M316","B314"]`), the article `paper/revised/sections/03-torus.tex` Proposition `prop:multiple` and its proof ("Fix finitely many disjoint interior balls B_1,…,B_N in the torus or in a bounded three-dimensional domain … smooth forced solution with zero initial velocity, smooth on [0,T), finite energy and dissipation, separate limsup blow-up in each ball … In a bounded domain its boundary condition is homogeneous no-slip"; the proof superposes the scaled building blocks `U_j` with start time `T − ε_j²`, no background), the reconciled torus record `research/T24/Spec.lean:1140-1340` (`finiteVelocitySum/PressureSum/ForceSum`, `MultipleRegionsAPI` 30 fields, `multipleRegionsStatement`) and its canonical form `Section3/T24/Multiple.lean` + `MultipleComponents.lean` (`RegionsData`, per-region placement/scaling/`ε`, `component`, supports), `MultipleAssembled.lean`, `MultipleRegions.lean`, `MultipleAssembly.lean`; the owner's domain vocabulary `Section3/T23/DomainSolution.lean` (`ClassicalSolutionOmega`, `IsBoundedBoxOrSmoothDomain`, `initialClassOmega`, `forceClassOmega`, `domainMaximalLifespan`), `Section3/T23/Placement.lean` (`DomainPlacementData`, `domainPlacementData` at a prescribed interior ball), `Section3/T23/Triple.lean`/`Solution.lean` (how a `ClassicalSolutionOmega` was built from `v + w + scaledPacket`), `research/T23/Spec.lean`, `research/T24/RECONCILIATION.md`.

## Goal (spec only; `research/` files, plus one canonical record module without proofs of inhabitation)
1. `research/T24/SpecOmega.lean`: `MultipleRegionsOmegaAPI` — the bounded-domain twin of the torus record: same 30-field skeleton with `Ω` (`IsBoundedBoxOrSmoothDomain Ω`) in place of the fundamental cube (regions: `closure (ball c_j ρ_j) ⊆ Ω`, pairwise disjoint), `DomainPlacementData` in place of `PlacementData`, `ClassicalSolutionOmega ν Ω 0 f T` in place of `ClassicalSolutionT`, components = the **un-periodised** scaled building blocks (no lattice sum: I03 `scaledVelocity`/`scaledPressure`/`scaledForce` at start time `T − ε_j²` — cite `Section4/I03` names), the no-slip clause, `SpeedUnboundedAtOn T (ball c_j ρ_j)` per ball, energy/dissipation bounds with the `Ω`-restricted norms (define them from `domainSobolevENorm`/the L² restriction the way `T22`/`T23` do; say exactly which registered norm you use), and `multipleRegionsOmegaStatement`. Every field docstring cites the article lines. Mark every place where the domain twin genuinely differs from the torus record (a `TORUS→Ω:` comment) and list them in `research/T24/COMPARISON_OMEGA.md` with the reason. If a torus field has no domain meaning (e.g. periodicity), drop it and say so.
2. `Section3/T24/MultipleOmega.lean`: the canonical restatement of that record (structure + statement `def`s only; `formalization/` cannot import `Contracts.*`; raw fields as in `Multiple.lean`), compiling.
3. A `research/T24/probes/multiple_omega_spec_closes.lean` that at least elaborates the record at a concrete `Ω` (the unit box) and one region, checking the field types against the torus record where they coincide (`example` per shared field).
4. `research/T24/T24_SPLIT.md`: a new section "P5 units" splitting the proof into lanes (components on `Ω` from I03 — smoothness on the closed slab, div, momentum, support inside `B_j`; the sum as `ClassicalSolutionOmega` with no-slip and pressure gauge; per-ball blow-up; energy/dissipation additivity; assembly + `T04.multiple_regions_v2` registration), each with the exact target field and supplier.
Report `research/T24/REPORT_496.md` (four parts). Gates: `lake build NSFormalization.Section3.T24.MultipleOmega`, `lake env lean` on the spec and probe (0 output), `make check`.
