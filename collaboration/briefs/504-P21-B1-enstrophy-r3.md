# Lane 504-P21-B1-enstrophy-r3 — P6 Route B unit B1: the general enstrophy differential inequality on ℝ³ (no critical smallness)

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/504-P21-B1-enstrophy-r3` (branch `erenup/504-P21-B1-enstrophy-r3`, = `origin/erenup/core`). Lane 503 works in parallel on the norm bridges (B0): thread any bridge you need as an explicit hypothesis (exact statement), do not create its modules. Read `collaboration/briefs/COMMON_PHASE5.md` (ground rules; commit after each closed lemma; new modules must be listed in `formalization/blueprint/entrypoints.json` `proof_modules`; after adding modules run `python3 experiments/audit_article_axioms.py --build --output-dir tmp/article-audit --workers 2` so `make check` does not stop at "Source changed: rerun the article axiom audit"), then **`research/P21/ASSESSMENT.md`** (lane 499: §1 exact targets in registered vocabulary, §2 Tao vs the tree, §3 Route B units B0–B5 with risks, §4) and `research/P21/REPORT_499.md`. Route B in one line: for the smooth-data, fixed-force targets, start from the proved high-order local solution and maximal development; prove on each shorter classical interval, with `Y = ‖u‖²_{H¹}`, `Z = ‖u‖²_{H²}`, the differential inequality `Y' + cν Z ≤ Cν (1+Y)³ + Cν ‖f‖²_{L²}` (3D interpolation `|convection pairing| ≤ C‖u‖_{H¹}^{3/2}‖u‖_{H²}^{3/2}` + Young), then an ODE barrier gives one time `d > 0` and a uniform `Y` bound depending only on the H¹ datum bound and the force; if the maximal time were `≤ d`, the squared-H² integral would be finite at that endpoint and the unconditional H⁷/H³ continuation contradicts maximality. Risks listed in ASSESSMENT §3 apply verbatim (δ before datum and restart time; inhomogeneous H² includes low modes; endpoint integrability by monotone limits; carriers/bridges).

## Goal (unit B1; L)
New module `Section4/A04/EnstrophyInequality.lean`: for a classical whole-space solution `w : ClassicalSolutionR ν a f T` (registered record, `Contracts/V1/Data.lean` vocabulary via the canonical restatement in `Section4/A02/Restrict.lean` §0) with `f` smooth and `a` in the class, on every compact subinterval of `(0,T)`:
1. the raw differentiated energy identity for `Y(t) = ‖u(t)‖²_{H¹}` (`Section4/C01/EnstrophyIdentityRaw.lean` supplies the differentiated identity — read it and state exactly what it gives; extend to the inhomogeneous H¹ = L² + Ḣ¹ by adding the ordinary energy identity from `Section4/C01`);
2. the convection pairing bound `|∫ ((u·∇)u)·Δu| ≤ C ‖u‖_{H¹}^{3/2} ‖u‖_{H²}^{3/2}` (3D interpolation: `‖u‖_{L⁶} ≤ C‖∇u‖_{L²}` from `A05.gradient_l6`, `‖∇u‖_{L³} ≤ ‖∇u‖_{L²}^{1/2}‖∇u‖_{L⁶}^{1/2}`, Hölder `L⁶·L³·L²`) — with ALL lower-order terms kept (no homogeneous shortcut);
3. Young's absorption: `Y' + cν Z ≤ Cν (1 + Y)³ + Cν ‖f(t)‖²_{L²}` with `Z = ‖u‖²_{H²}`, explicit constants `c, C` depending only on `ν` (state them), valid on every closed subinterval where the solution is classical.
Peel each step as its own named lemma with a probe. If step 2 or 3 does not close in time, deliver the closed lemmas and the exact residual inequality with error text. Deliverables: module (entrypoints), `research/P21/probes/b1_closes.lean`, `research/P21/axioms_b1.lean`, `research/P21/ATTEMPTS_B1.md`, B1 status in `research/P21/P6_SPLIT.md` (create if lane 503 has not). Gates as in COMMON. Report `research/P21/REPORT_504.md`.
