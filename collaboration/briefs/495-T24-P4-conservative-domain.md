# Lane 495-T24-P4-conservative-domain — close Partial P4: Proposition 3.17 on a bounded no-slip domain (graph node `C317_B`)

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/495-T24-P4-conservative-domain` (branch `erenup/495-T24-P4-conservative-domain`, = `origin/erenup/core`). Read `collaboration/briefs/COMMON_PHASE5.md` first, then `PLAN.md` §1 row P4, `proof_graph.json` node `C317_B` (Partial: "The bounded-domain conservative-force variant is not exported"; `completion_from: ["C317","BU"]`), the article `paper/revised/sections/03-torus.tex` Proposition `prop:conservative` ("On a bounded domain, let f = −∇φ and impose homogeneous no-slip velocity. … any smooth solution with zero initial velocity is identically zero on its classical lifespan"; proof: `∫ f·u = −∫ ∇φ·u = 0` by incompressibility and the zero normal trace, then the energy identity from zero), and the code: the torus version `Section3/T24/Conservative.lean` (`PeriodicPotentialT`, `conservativeForceT φ := fun z ↦ -pressureGradient φ z.1 z.2`, `zero_from_rest`) and `Section3/T24/ConservativeAssembly.lean` (`ConservativeForcingAPI` — two fields `potential_pairing`, `zero_from_rest` —, `conservativeForcing`, `restSolution`); the owner's bounded-domain layer `Section3/T23/DomainSolution.lean` (`ClassicalSolutionOmega ν Ω a f T`: fields `velocity_smooth : SmoothOnClosedSlab`, `initial`, `divergence`, `momentum`, `no_slip`, `pressure_gauge`; `IsBoundedBoxOrSmoothDomain`, `domainNormalizePressure`, `IBP`), `Section3/T23/BoundaryIntegration.lean` (`ibp_boundedDomain : IsBoundedBoxOrSmoothDomain Ω → IBP Ω`), `Section3/T23/NoSlipEnergy.lean` (`IBP.integral_pressure_energy_zero` — read it: it is the pairing `∫_Ω ∇p · u = 0` for divergence-free no-slip `u`), `Section3/T23/DifferenceEnergy.lean` (`difference_energy_identity`, `velocity_eq_of_ibp`), `Section3/T23/NoSlipUniqueness.lean` (`noSlip_uniqueness`, unconditional), `verification/Contracts/V1/ConservativeForcing.lean` + `Bindings/ConservativeForcing.lean` (registered torus statement and how the domain vocabulary is registered in `Contracts/V1/BoundaryInsertion.lean` / `Contracts/V2/BoundaryInsertion.lean`).

## Goal
1. `Section3/T24/ConservativeOmega.lean`: `conservativeForceOmega φ := fun z ↦ -pressureGradient φ z.1 z.2` (same token as the torus version; the article imposes no periodicity here — take `ContDiff ℝ ∞ φ` as the potential class and say so), `restSolutionOmega ν Ω φ T hT : ClassicalSolutionOmega ν Ω 0 (conservativeForceOmega φ) T` (zero velocity, pressure `−φ` normalised by `domainNormalizePressure` — check that the momentum field then holds because `∇(−φ + c(t)) = −∇φ`), and `structure ConservativeForcingOmegaAPI : Prop` mirroring the torus record: `potential_pairing : ∀ ν T (hT) Ω (hΩ : IsBoundedBoxOrSmoothDomain Ω) φ (hφ), ∀ S : ClassicalSolutionOmega ν Ω 0 (conservativeForceOmega φ) T, ∀ t ∈ Ico 0 T, ∫ x in Ω, inner ℝ (conservativeForceOmega φ (t,x)) (S.velocity (t,x)) = 0` (prove it **directly** by `IBP.integral_pressure_energy_zero` with `ibp_boundedDomain hΩ`, as in the article) and `zero_from_rest : … ∀ t ∈ Ico 0 T, ∀ x ∈ Ω, S.velocity (t,x) = 0` (either the article's energy route — energy identity from rest with the pairing zero, then `eqOn_of_integral_norm_sub_sq_eq_zero` — or `noSlip_uniqueness` against `restSolutionOmega`; state which, and if both close, keep the article's route as the proof and mention the other as a probe). `theorem conservativeForcingOmega : ConservativeForcingOmegaAPI`.
2. Contract `T04.conservative_forcing_v2` (`Contracts/V2/ConservativeForcing.lean`: the V1 torus record extended (or paired) with the domain record over the registered domain vocabulary; `Bindings/ConservativeForcingV2.lean`, `Tests/ConservativeForcingV2.lean`), registry entry (scope: "Proposition 3.17, both branches: periodic potential on the torus; smooth potential on a bounded box-or-regular-level domain with homogeneous no-slip; pairing vanishes and the solution from rest is zero on its lifespan").
3. Blueprint per COMMON §3: node `C317_B` → Closed, `depends_on: ["C317","BU"]`; `RESULT_MAP.md` Proposition 3.17 → Closed; guide row `prop:conservative` → Closed; counts; regenerate; gates; `make paper`.
Report `research/T24/REPORT_495.md`.
