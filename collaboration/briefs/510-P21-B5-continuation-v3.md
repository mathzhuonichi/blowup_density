# Lane 510-P21-B5-continuation-v3 — P6 Route B unit B5 on ℝ³: the H¹-uniform endpoint theorem and the `Contracts/V3/Continuation` registration (no blueprint-status change; the torus half and the `L21_H1` closure are lane 511)

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/510-P21-B5-continuation-v3` (branch `erenup/510-P21-B5-continuation-v3` = lane 507's branch (B4-ℝ³ closed: `Section4/A04/H1Restart.lean`, `H1BridgesSmooth.lean`; includes lanes 503 B0 and 506 B3, both under review) merged with `origin/erenup/core` (504 B1 on core)). Read `collaboration/briefs/COMMON_PHASE5.md` (ground rules; commit after every closed step; `python3 experiments/audit_article_axioms.py --build --output-dir tmp/article-audit --workers 2` before `make check`; `check_contracts.py` has no `--base-ref`, use `--summary`), `research/P21/REPORT_507.md`, `research/P21/P6_SPLIT.md` (§ B5 API / normalization corrections written by lane 507), `research/P21/ASSESSMENT.md` § Registered-vocabulary H¹ restart targets, `research/P21/Targets.lean` (`h1RestartR`, `h1UniformEndpointR`), and the registration pattern of a closing lane already on core (`verification/Contracts/V2/ConservativeForcing.lean` + `Bindings/ConservativeForcingV2.lean` + `Tests/ConservativeForcingV2.lean`, lane 495; registry entries in `verification/contracts.json`, e.g. `A04.continuation_v2`). Do NOT edit the modules of lanes 503/504/506/507; add what you need in new files.

## 1. Endpoint theorem (`formalization/`)
New module `Section4/A04/H1RestartBeyond.lean` (in `entrypoints.json` `proof_modules`): `restartBeyondH1` = `h1UniformEndpointR` of Targets.lean, literally:
```
∀ ν, 0 < ν → ∀ f, MemForceR f → ∀ S, 0 < S → ∀ K : ℝ≥0∞, K ≠ ⊤ → ∃ δ > 0,
  ∀ a u p, a ∈ initialClassR → SolvesBelow ν a f S u p →
    (∀ t ∈ Ico 0 S, sobolevENorm 1 (fun x ↦ u (t, x)) ≤ K) → ENNReal.ofReal (S + δ) < maximalLifespanR ν a f
```
Route: lane 507's `h1RestartAt` (per-time `ofReal (t₀ + δ) ≤ maximalLifespanR ν a f` for `t₀ ∈ Ico 0 S` with the H¹ bound) + `A04/Continuation.lean:45 restartBeyond_of_restartAt` gives `ofReal (S + δ) ≤ …`; halve δ for the strict inequality (`ofReal (S + δ/2) < ofReal (S + δ) ≤ …`). Mirror `RestartFixedForce.lean:276 restartBeyond_fixed` for the shape. Also export the non-strict `restartBeyondH1_le` and, as a named predicate, `RestartFixedForceH1 ν f S : Prop` (the H¹ analogue of `RestartFixedForce`, i.e. the body of `h1RestartR`), if it makes the contract cleaner.

## 2. Contract V3 (`verification/`; V1/V2 frozen)
- `verification/Contracts/V3/Continuation.lean`, importing `Contracts.V2.Continuation` (and `Contracts.V2.LocalTheory` for `ManuscriptLocalRegularity`, `initialClassR`, `sobolevENorm`, `MemForceR`, `timeShift`, `SolvesBelow`, `maximalLifespanR` — reuse the V2/V1 registered definitions, do not restate anything that already exists in `Contracts.*`): `structure ContinuationV3API : Prop` with two fields, `restartH1` (statement = `h1RestartR` in registered vocabulary, docstring: "appendix-a-local-theory.tex:146-151 at the H¹ ball, one δ before restart time and datum, fixed force in the manuscript class") and `restartBeyondH1` (= `h1UniformEndpointR`, strict lifespan conclusion, docstring citing the same lines), plus `def continuationV3Statement : Prop := ContinuationV2API-level statement ∧ ContinuationV3API` if the existing V2 exposes a statement Prop (check `Contracts/V2/Continuation.lean:70-150` and `Bindings/ContinuationV2.lean` for how V2 is exported) — otherwise register the V3 API alone and note that V2 stays registered separately. Every definition you must restate gets an `rfl` bridge theorem in the binding; if any registered contract definition differs from the local one (e.g. `Contracts.V2.Continuation.timeShift` vs `A04.timeShift`), use the existing bridges from `Bindings/ContinuationV2.lean` / `Bindings/LocalTheoryV2.lean` (the `ClassicalSolutionR` structure needs the fieldwise conversions already there).
- `verification/Bindings/ContinuationV3.lean`: `theorem continuationV3_holds : ContinuationV3API := ⟨…h1RestartR…, …restartBeyondH1…⟩` through the existing conversions; `verification/Tests/ContinuationV3.lean`: `checkedContinuationV3` + `run_cmd TestSupport.checkAxioms ``checkedContinuationV3` + `example`s projecting both fields (Tests has `warningAsError`; import only Contracts/Bindings/TestSupport).
- Registry `verification/contracts.json`: append `{"id": "A04.continuation_v3", "parent_task": "A04", "version": 3, "specification": "verification/Contracts/V3/Continuation.lean", "binding_module": "Bindings.ContinuationV3", "test_module": "Tests.ContinuationV3", "declaration": "BlowupDensity.Tests.checkedContinuationV3", "scope": "Proposition 2.1 / appendix A on R3: restart uniform on an H1 datum ball with one duration before restart time and datum, and the H1-uniform strict endpoint continuation, for a fixed force in the manuscript class; V2's H7 routes stay registered.", "enabled": true}` (35 contracts). Keep `A04.continuation_v2` unchanged.
- Blueprint: do NOT change `L21_H1`'s status (lane 511 closes it with the torus half). Only add the new binding/module to the evidence-adjacent metadata if the checkers demand (e.g. `entrypoints.json`); regenerate `AXIOM_AUDIT.json` and `DEPENDENCY_GRAPH.md` if `make check` requires.
- Apply lane 504's deferred linter notes ONLY IF that file is not under someone else's review — it is on core already: `Section4/A04/EnstrophyInequality.lean` lines 143/249/256/465 `<;>` → `(…; …)` per `research/P21/REVIEW_504-P21-B1-enstrophy-r3.md`; rebuild and confirm zero lane-local warnings on direct `lake env lean`.

## 3. Gates and report
`make check` (contracts summary shows 35; all 11 policy tests), `make test`, `make test-mutations`, `make paper`, `git diff --check`, `scripts/gates.sh` on the new modules, `python3 experiments/check_reader_documents.py`. Probes: `research/P21/probes/b5r_registered.lean` (`#print axioms BlowupDensity.Tests.checkedContinuationV3`; non-vacuity: instantiate `restartBeyondH1` on the zero solution). Report `research/P21/REPORT_510.md` (four parts) and update `research/P21/P6_SPLIT.md` (B5-ℝ³ done; what lane 511 still needs for the torus and the `L21_H1` closure). Commit after each closed step with `[510-P21-B5] …`; no push/merge/rebase.
