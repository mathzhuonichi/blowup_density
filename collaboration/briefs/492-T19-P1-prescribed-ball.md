# Lane 492-T19-P1-prescribed-ball — close Partial P1: Theorem 3.6 for an arbitrary prescribed coordinate ball (graph node `G36_FULL`)

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/492-T19-P1-prescribed-ball` (branch `erenup/492-T19-P1-prescribed-ball`, = `origin/erenup/core`). Read `collaboration/briefs/COMMON_PHASE5.md` first, then `PLAN.md` §1 row P1, `formalization/blueprint/proof_graph.json` node `G36_FULL` (Partial, `completion_from: ["G36"]`) and `CLOSURE_AUDIT.md` lines 15–30, the article `paper/revised/sections/03-torus.tex` Theorem `thm:insertion` ("Fix any nonempty coordinate ball. For all sufficiently small ε > 0 there are g_ε ∈ 𝓕 and a solution u_ε with (i)–(iv)"), and the code: T15 `Section3/T15/Assembly.lean` (`placementData` — fixed centre `(1/2,1/2,1/2)`, radius `3/8`; `placementThreshold`, `placementCarrier`, `placement_chart_in_cube`), T24 `Section3/T24/MultipleComponents.lean` (`RegionsData.placement j`: the same constructor at a **prescribed** centre/radius with `region_interior : closure (ball c ρ) ⊆ interior fundamentalCube` — this is the shape you need), T18 `Section3/T18/Assembly.lean` (`InsertionData`, `RawPremises`, `assemble`, `periodicInsertionStatement`), T19 `Section3/T19/Threading.lean` (`extendByZero`, `insertionData` — fixed ball —, `insertion`, the export lemmas, `exists_force_close`), the owner's raw-data model `verification/Bindings/BoundaryInsertionV2.lean: boundaryInsertion_from_data` (bounded-domain analogue: the prescribed ball has radius `2*r`, only the article's hypotheses are arguments), `verification/Contracts/V1/PeriodicInsertion.lean` (`periodicInsertionStatement`, the record), `Contracts/V2/BoundaryInsertion.lean` + `Bindings/BoundaryInsertionV2.lean` + `Tests/BoundaryInsertionV2.lean` (V2 pattern), `research/T18/{Spec.lean,RECONCILIATION.md}`, `research/T19/{T19_SPLIT.md,REPORT_461.md}`.

## Goal
1. `Section3/T15/PlacementAt.lean`: `placementDataAt (center : Space) (radius : ℝ) (hρ : 0 < radius) (hcube : closure (Metric.ball center radius) ⊆ interior fundamentalCube) hK hf T hT : PlacementData u p f K` with `chartCenter := center`, `chartRadius := radius`, `x₀ := center`, threshold adapted to `radius` (factor the construction of T24's `RegionsData.placement`; do not edit T24 or `placementData`), with `rfl` projection lemmas for `T`, `chartCenter`, `chartRadius`, `x₀`.
2. `Section3/T19/ThreadingAt.lean`: `insertionDataAt center radius hρ hcube … reference`, `insertionAt`, and the same exports as `Threading.lean` (`force_mem`, `lifespan`, `solution`, `blowup_limsup`, `energyRate`, mixed/Sobolev bounds, `forceDifference_sobolev_tendsto`, `exists_force_close_at`) — restated over the prescribed ball; plus the article's clause (iii): for `t < T` the difference `u_ε − v` is divergence free and supported in a ball of diameter `O(ε)` inside the chosen ball (from the record's `velocityDifference_support`/`diffSupport_in_chart` fields — read their exact form; the periodic set version must be reduced to the single ball inside the prescribed chart).
3. The article-scope theorem `periodicInsertion_from_data` (canonical, in `ThreadingAt.lean` or `Section3/T19/FromData.lean`): for every `ν > 0`, prescribed ball (`center radius hρ hcube`), `a ∈ initialClassT`, `g ∈ forceClassT`, `T > 0`, `δ > 0`, `reference : ClassicalSolutionT ν a g (T + δ)`: `∃ ε₀ > 0, ∃ (force : ℝ → SpaceTimeField) (velocity : ℝ → SpaceTimeField), ∀ ε ∈ Ioc 0 ε₀, [ (i) force ε ∈ forceClassT ∧ maximalLifespanT ν a (force ε) = ofReal T ∧ limsup blow-up; (ii) history: velocity ε = reference.velocity on t ≤ T − 2ε²; (iii) support/divergence of the difference in the chosen ball; (iv) the three simultaneous bounds eq:Eclose / eq:Fclose / eq:Hsclose ]` — copy each conjunct's wording from the registered record fields (with `extendByZero reference` identified with `reference.velocity` on `Ico 0 (T+δ)` via `extendByZero_velocity_eqOn`, so the statement mentions only `reference.velocity`). Non-vacuity probe: instantiate at the zero force/datum with the unit cube's centre ball and one off-centre ball.
4. Contract `T03.periodic_insertion_v2` (`Contracts/V2/PeriodicInsertion.lean` defining `periodicInsertionStatementV2` = the raw-data statement over registered vocabulary, `Bindings/PeriodicInsertionV2.lean`, `Tests/PeriodicInsertionV2.lean`), registry entry (scope: "Theorem 3.6 from raw data for every prescribed coordinate ball whose closure lies in the interior of the fundamental cube; the building block is the registered packet; only the article's hypotheses are arguments").
5. Blueprint per COMMON §3: node `G36_FULL` → Closed (scope: the prescribed-ball clause), evidence `Section3/T19/ThreadingAt.lean` + `Bindings/PeriodicInsertionV2.lean`, `depends_on: ["G36"]`; `RESULT_MAP.md` Theorem 3.6 → Closed; guide row `thm:insertion` → Closed; counts 22/5; regenerate; gates; `make paper`.
Report `research/T19/REPORT_492.md`.
