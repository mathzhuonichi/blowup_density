# Lane 394 continuation (fix after review REJECT) — close or honestly scope the canonical `force_profile_identity` conformance; repair stale docs

You are a Lean 4 (v4.34.0-rc2 + Mathlib) proof worker continuing lane 394 in the worktree
`/data_8T/ping/blowup_density/.claude/worktrees/394-T17-UCAN-canonical-correction` (branch `erenup/394-T17-UCAN-canonical-correction`).
Read first: `research/T17/REVIEW_394-T17-UCAN-canonical-correction.md` §3 (the two required fixes and the reproduction probe
`research/T17/probes/rev394_field_conformance.lean:14` with its exact type mismatch), your `research/T17/REPORT_394.md`, `research/T17/ATTEMPTS_UCAN.md`
("G1 and the honest U4 residual"), `Section3/T17/Correction.lean:155` (the canonical `force_profile_identity` field: periodized `correctionForce ν v D ε`),
`Section3/T17/ForceProfile.lean:248` (`physicalForce_eq_rescaledForceProfile`, single-copy chart form), `Section3/T17/Transport.lean:281` (`force_eq`, lattice-lift equality),
and the top 40 lines of `logs/LESSONS.md`.

## Ground rules (non-negotiable)
- Work ONLY inside this worktree; never `git push`, merge or rebase; committing on your branch is allowed. `. scripts/lean-env.sh`; `lake` only from `verification/` with `LEAN_NUM_THREADS=6`.
- No `sorry`/`admit`/`axiom`/`native_decide`; no placeholder fields; do not weaken the canonical field. The canonical structure's field list stays unchanged.
- Every declaration prints exactly `[propext, Classical.choice, Quot.sound]`.

## Fix 1 (blocking) — canonical `force_profile_identity` conformance
Goal: a probe declaration (in `research/T17/probes/correction_canonical.lean`, or a new `research/T17/probes/force_profile_canonical.lean`) whose **type is literally the
canonical field's type** at the concrete data, proved from the landed pieces: `Transport.force_eq` (periodized correction force = lattice lift of the single-copy chart force,
for concrete `correctionData`) composed with `ForceProfile.physicalForce_eq_rescaledForceProfile` (single-copy chart identity) and a local single-copy germ (on the fixed
profile cylinder only one lattice copy is non-zero — the chart's spatial support is a ball of radius `< 1/2` inside the unit cell; prove that germ from the support facts in
`Transport.lean`/`CorrectionProfile.lean`, `latticeLift` support lemmas in `Section3/T16/LatticeLift.lean`). Premises allowed: exactly the canonical record's own hypotheses
(the ones already in scope at the field) plus the U-CAN documented `hv : ContDiff ℝ ∞ v` if the landed U4 theorem needs it (spec issue G1, `research/T17/SPEC_ISSUES.md`) — state it
explicitly. Budget: up to ~90 minutes of proof work. Outcome A (closed): commit the probe, update `REPORT_394.md` §3 to say the field is closed at the probe level and cite the declaration.
Outcome B (not closed): do **not** stub. Write the exact residual as a `theorem`-shaped statement in `ATTEMPTS_UCAN.md` (the precise lemma still missing, with the error text of the
last attempt), add a probe `example (hres : <residual>) : <canonical field type> := …` showing the residual is the only gap, and record in `REPORT_394.md` that the lead re-scopes this
closure as a separate proof unit "T17 U4b (periodized force-profile identity)" — the lead has pre-approved this amendment **only if** Outcome A genuinely fails within budget.

## Fix 2 (documentation)
`ForceProfile.lean:46` claims lane 373 is not on this base — false (the module imports `Transport` at `:1`); `:47` and `:247` cite a non-existent `research/T17/ATTEMPTS_U4.md`.
Point them to `research/T17/ATTEMPTS_UCAN.md` ("G1 and the honest U4 residual") and state that `Transport.force_eq` is present. Preserve all mathematical statements.

## Gates (paste outputs)
`lake build NSFormalization.Section3.T17.Correction NSFormalization.Section3.T17.ForceProfile` (0 errors), `lake env lean` on both modules and on every probe you touched (0 output),
`lake env lean research/T17/probes/rev394_field_conformance.lean` (or your replacement — say which), the axioms audit file, `make check`.

## Report
Commit on your branch (`[394-T17] Fix after review: …`); append a "## Continuation (fix)" section to `research/T17/REPORT_394.md` with the four parts (what closed / files / exact gap /
commands), and end your message with the same four parts.
