# Lane 487 (continuation) — T23 U9: close the U2b → U3 potential seam and finish the assembly + registration

Same worktree/branch (`/data_8T/ping/blowup_density/.claude/worktrees/487-T23-U9-assembly-registration`, `erenup/487-T23-U9-assembly-registration`), same ground rules as `collaboration/briefs/487-T23-U9-assembly-registration.md`. Read your own `research/T23/REPORT_487.md` §3 and `ATTEMPTS_U9.md` (the exact residual: U3's `LocalCorrectionCore reference.velocity … D` needs `potential_formula` = the original reference's radial integral at **all** times/points, while U2b's matched `D` carries the whole-space extension's potential, which agrees with the reference only on the interior cylinder).

**Lead ruling (2026-09-19 17:40Z, recorded as G2 in `research/T23/SPEC_ISSUES.md`):** this is a seam between two parallel lanes' *hypotheses*, not a mathematical obstruction; it is closed by weakening the hypothesis to what U3 actually uses. You are authorised to edit lane 482's modules (`Section3/T23/Triple.lean`, `Solution.lean`, `PressureNormalization.lean`) and lane 477's `LocalCorrection.lean` **for exactly this purpose**: (i) find every use of `potential_formula` (and any other all-time/all-space identity) in U3's proofs; (ii) replace the `LocalCorrectionCore` hypothesis by a **windowed** core (`potential_formula` / agreement only on the open interior cylinder `Ioo 0 (T+δ) ×ˢ ball x₀ r`, or on the smaller ball `r/2` that U2b's construction provides — pick the weakest that U3's proofs need), re-proving U3's fields under it (the cut-off correction, the packet and the pressure only read the potential where the correction is supported, i.e. inside the window — if some U3 field genuinely needs the identity outside the window, say exactly which and why); (iii) alternatively, if simpler, build the assembled `D` from the **reference's own** radial potential and prove U2b's matching (`A.correction = C`) against it on the window by the window-agreement lemmas of `SpatialExtension.lean`/`LocalCorrectionBridge.lean`. Keep every previously proved theorem statement of 476/477/478/481/483–486 valid (only the U3 hypothesis shape changes; record each edit in `ATTEMPTS_U9.md`).
Then finish the original U9 goal: `def boundaryInsertionAPI …` (48 fields, box domains), `boundaryInsertionStatement'_box_holds`, `boundaryInsertionStatement'_of_ibp`, the non-vacuity probe, and the contract trio + registry `T04.boundary_insertion` exactly as the original brief specifies (registered count 54 → 55). If, after the seam is closed, a *different* single field still does not close, register nothing, state that field's exact residual, and deliver everything else. Write `research/T23/REPORT_487b.md` (four parts). Same gates.
