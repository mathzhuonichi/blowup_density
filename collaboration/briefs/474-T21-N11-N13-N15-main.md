# Lane 474-T21-N11-N13-N15-main — T21 bundle M: units N11, N13, N14, N15 (the `MainTheoremAPI` side of `thm:main`)

You are a Lean 4 (v4.34.0-rc2 + Mathlib) proof worker on the repository checked out at your working directory
`/data_8T/ping/blowup_density/.claude/worktrees/474-T21-N11-N13-N15-main` (git branch `erenup/474-T21-N11-N13-N15-main`, = `origin/erenup/integration-section3` with T19 registered (`T03.density`, #435: canonical `Section3/T19/Assembly.lean` `periodicDensityAPI`,
`Density.lean` records) and T20 registered). Lane 472 works **in parallel** on N0–N10/N12 (modules `Section3/T21/{CriticalBridge,OrderLowering,ForceMonotonicity,Zero,Ball,Disjointness,NonDensity}.lean`): do not create those files; where you need N10 (`nonDensity`) or N12
(`zeroInitialClass`), take them as **threaded hypotheses** of your theorems exactly as `T21_SPLIT.md` §1 N14/N15 allow ("may prove now against threaded density and NonDensityAPI"), i.e. state N14/N15 over `(D : PeriodicDensityAPI)` / `(N : NonDensityAPI c)` arguments or
over the explicit field hypotheses; the registration lane will discharge them from 472's inhabitant. For N12 you may use `Section3/T20/CriticalEnergy.lean:402 zero_mem_initialClassT` directly.
Read `CLAUDE.md`, **`research/T21/T21_SPLIT.md`** (§0; §1 units N11, N13, N14, N15 verbatim targets/routes; §2), `research/T21/RECONCILIATION.md` §3–§4, `research/T21/Spec.lean` (`MainTheoremAPI` 5 fields, `mainOfDensityAndNonDensity`, `mainOfInputs`),
`Section3/T19/Density.lean` + `Assembly.lean` (`periodicDensityAPI.fixedInitialDensity`), `Section3/T19/Bookkeeping.lean:55 thresholdValue` (N11), `verification/Contracts/V1/TorusLocalTheory.lean` (`RelativelyDenseT`, `breakdownSetT`; `breakdownSetTZero` is T21's, `Spec.lean:203`),
the R³ template `Paper1/PeriodicMain.lean:44-53` / `Section4/R41/NonDensityL1.lean` (order argument), and the top 40 lines of `logs/LESSONS.md`.

## Ground rules (non-negotiable)
- Work ONLY inside this worktree; never `git push`, merge or rebase; committing on your branch is allowed. `. scripts/lean-env.sh`; `lake` only from `verification/` with `LEAN_NUM_THREADS=6`. Build the closure first (`lake build NSFormalization.Section3.T19.Assembly`).
- No `sorry`/`admit`/`axiom`/`native_decide`; `set_option maxHeartbeats N in` only per declaration, `N ≤ 400000`, commented. No edits to existing modules; new files only: `formalization/NSFormalization/Section3/T21/Main.lean` (no `verification/` files in this lane).
- **No named inputs, no placeholders, no goal repackaging.** An honest partial with the exact residual statement and error text beats a stub; a stub is rejected outright.
- Every declaration prints exactly `[propext, Classical.choice, Quot.sound]`. `grep -rn` before claiming "not in the tree"; `sed -n` before citing a line. Record every failed approach with its exact error text in the ATTEMPTS file.

## Goal
`Section3/T21/Main.lean` (namespace `NSFormalization.Section3.T21`): N11 `thresholdValue` (reuse), N12 transport, N13 `fixedInitialDensity` (from `periodicDensityAPI.fixedInitialDensity`, a-first binders exactly as the field), N14 `zeroInitialDensityIff` (backward: N13 at the
zero datum via N12 + `breakdownSetTZero` unfolding; forward: `le_of_not_lt` + threaded `nonDensity`), N15 `zeroInitialNonDensity` (threaded `nonDensity` reordered), and `def mainTheoremAPI (N : NonDensityAPI c) … : MainTheoremAPI` (or over the exact hypotheses the Spec's
`mainOfDensityAndNonDensity : ∀ c, PeriodicDensityAPI → NonDensityAPI c → MainTheoremAPI` needs — prove a theorem `mainOfDensityAndNonDensity_holds : mainOfDensityAndNonDensity` using the canonical `periodicDensityAPI` for the density argument where the Spec quantifies it,
keeping the `NonDensityAPI c` argument threaded). Deliverables: the module, `research/T21/probes/main_closes.lean` (each field by `exact`), `research/T21/axioms_n11_n15.lean`, `research/T21/ATTEMPTS_N11_N15.md`, status lines in `T21_SPLIT.md`.

## Gates
`cd verification && LEAN_NUM_THREADS=6 lake build NSFormalization.Section3.T21.Main` (0 errors), `lake env lean` on the module (0 output), the probe, the axioms file, `make check`.

## Report
Commit on your branch; end with four parts (theorems with exact statements / files / gaps with error text / commands and results). Also write it to `research/T21/REPORT_474.md`.
