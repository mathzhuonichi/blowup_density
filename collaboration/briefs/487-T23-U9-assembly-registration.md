# Lane 487-T23-U9-assembly-registration — T23 U9: assemble the 48-field `BoundaryInsertionAPI` from U1–U8, close the repaired statement (box branch), register `T04.boundary_insertion`

You are a Lean 4 (v4.34.0-rc2 + Mathlib) proof/registration worker on the repository checked out at your working directory
`/data_8T/ping/blowup_density/.claude/worktrees/487-T23-U9-assembly-registration` (git branch `erenup/487-T23-U9-assembly-registration`, = `origin/erenup/integration-section3` (476–482, 484 merged) + the branches of 483 (U4), 485 (U6), 486 (U8), which are in review).
Read `CLAUDE.md` (contract import rules; bindings by `rfl`/fieldwise conversions; `Tests` = `warningAsError`; Prop-valued `def`s need `set_option linter.defProp false`), **`research/T23/T23_SPLIT.md`** (U9 verbatim: the four remaining fields `domain`, `delta_pos`, `reference_force_mem`, `initial_mem`; the 48-field inventory), **`research/T23/SPEC_ISSUES.md`** (G0: register the **repaired existential** statement `boundaryInsertionStatement'`, never the literal universal one; G1: the smooth-domain branch is conditional on `IBP Ω` — V1 registers the **box** branch unconditionally and the smooth branch as the explicit conditional theorem; both are lead rulings, the V1 wording is owner-pending and must be stated in `scope`), every unit report `research/T23/REPORT_{476,477,478b,480,481,482,483,484,485,486}.md` (each lists exactly which hypotheses it threaded — your job is to discharge them all from the one matching family: 481's `exists_matching_supplier` at the same `D`, 482's triple/solution, 483's differences/no-slip, 484's comparison, 485's rates, 486's blow-up/lifespan), the canonical modules under `Section3/T23/` (`Boundary.lean` is the record), the registration pattern of `research/T24/REPORT_471.md` + `verification/{Contracts/V1,Bindings,Tests}/MultipleRegions.lean` and `research/T21/REPORT_475.md`, `verification/contracts.json`, `collaboration/work_items.json` (T23 item), and the top 40 lines of `logs/LESSONS.md`.

## Ground rules (non-negotiable)
- Work ONLY inside this worktree; never `git push`, merge or rebase; committing on your branch is allowed. `. scripts/lean-env.sh`; `lake` only from `verification/` with `LEAN_NUM_THREADS=6`. Build the closure first (`lake build` of every `Section3/T23/*` module).
- No `sorry`/`admit`/`axiom`/`native_decide`; no placeholder fields; existing `Contracts/V1/*` and `Tests/*` untouched; you may edit T23 canonical modules **only** to remove duplicate declarations among the parallel lanes (record each in `ATTEMPTS_U9.md`). Never import `Paper1/BoundaryCorollary.lean`.
- **No named inputs.** Every threaded hypothesis of U1–U8 must be discharged from the chosen family or from registered/canonical suppliers; if one cannot be, state the exact residual (which field, which hypothesis, why) and deliver the rest — do not register a contract whose binding leaves a hypothesis open.
- Every declaration prints exactly `[propext, Classical.choice, Quot.sound]`. Commit incrementally.

## Goal
1. `Section3/T23/Assembly.lean`: for the raw packet + clauses, a box domain `Ω` (`IsBoxDomain`), a prescribed interior ball, a reference `ClassicalSolutionOmega` on `T+δ` and the horizon `T`: `def boundaryInsertionAPI … : BoundaryInsertionAPI …` assembling all 48 fields (U1 placement, U2/U2b cutoff + supplier, U3 triple/solution, U4 differences, U5 comparison, U6 rates, U7 box uniqueness via U8's lifespan, U8 blow-up, the four U9 fields), and `theorem boundaryInsertionStatement'_box_holds` (the repaired statement restricted to box domains) plus `theorem boundaryInsertionStatement'_of_ibp` (smooth branch conditional on `IBP Ω`). Non-vacuity probe: the unit box, the registered nonzero packet, the zero reference (or the simplest admissible reference), `T = 1`.
2. Contract trio `verification/Contracts/V1/BoundaryInsertion.lean` / `Bindings/BoundaryInsertion.lean` / `Tests/BoundaryInsertion.lean` — the record and the **repaired** statements token-for-token from `Boundary.lean` over registered vocabulary (registered `T04.bounded_domain_norm` for the T22 layer; fieldwise conversions for `ClassicalSolutionOmega`; the 481 contract-facing constructor moved from the research probe into the binding); registry entry `T04.boundary_insertion` (`version: 1`, `parent_task: "T04"`, `scope`: box domains unconditional, smooth-level domains conditional on the explicit `IBP Ω`; G0 existential repair; owner-pending wording), `work_items.json` T23 `contracts`, `python3 experiments/tasks.py render`.
Deliverables: modules, probe, `research/T23/axioms_u9.lean`, `research/T23/ATTEMPTS_U9.md`, U9 status line, `research/T23/REPORT_487.md` (four parts).

## Gates
`lake build NSFormalization.Section3.T23.Assembly`, `make check`, `make test` (new `checked…` line: standard axioms only), `make test-mutations`, `python3 experiments/check_contracts.py --base-ref origin/erenup/integration-section3` (registered count +1, `base_compatibility_checked: true`), the probe, the axioms file.
