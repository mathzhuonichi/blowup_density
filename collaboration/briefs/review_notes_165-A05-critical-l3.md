1. The module carries a local `def dotHomogeneousENorm` (copied from `research/R43/Spec.lean`). Lane 164 has registered the canonical definition in `verification/Contracts/V1/HomogeneousNorm.lean` and `formalization/…/Section4/D01/HomogeneousNorm.lean` (on branch `erenup/164-D01-g1-homogeneous-norm`, read it via `git show origin/erenup/164-D01-g1-homogeneous-norm:formalization/NSFormalization/Section4/D01/HomogeneousNorm.lean` after `git fetch origin`). Compare token by token: will `A05.dotHomogeneousENorm = D01.dotHomogeneousENorm` be `rfl` (same infimum, same `IsHomogeneousSliceDatum` predicate, same enorm)? If not, list the exact differences — this decides whether the A05 V2 registration is a `rfl` bridge or a lemma.
2. Hypothesis fidelity: `velocityCriticalL3 (z) (hz : MemHInfty z)`. Compare with `research/A05/Spec.lean:366` (what hypotheses does the spec field take — `initialClassR`? a solution slice? `MemHInfty`?) and with what R43/R44 will feed it (velocity slices of a `ClassicalSolutionR`, which are `MemHInfty` by `w.sobolev`). Is `MemHInfty` weaker or stronger than the spec's hypothesis? Any mismatch must be listed.
3. Vacuity: when `z` has no order-`1/2` homogeneous datum the right-hand side is `⊤` and the inequality is trivial. Confirm that for `z` with a datum the statement is non-vacuous (the nonzero bump example in `axioms_critical_l3.lean`) and check whether the paper's hypothesis `‖a‖_{Ḣ^{1/2}} < cν` already forces a finite datum, so no `≠ ⊤` side condition is missing downstream.
4. Constant: `criticalL3Const = 3 * scalarCriticalConst (1/2)` — trace `RieszKernelNormalization.coefficient`, `potentialConstant`, `512 ^ (1/targetExponent)` to their definitions and confirm each is a positive real (no `ENNReal.toReal` of a possibly-`⊤` quantity).
