# Lane 508-P21-B4-restart-torus — P6 Route B unit B4 on 𝕋³: the uniform H¹ restart theorem `h1RestartT` from B0 + B2 + B3 and the existing periodic maximal/continuation theory

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/508-P21-B4-restart-torus` (branch `erenup/508-P21-B4-restart-torus` = `origin/erenup/core` + lane 503 (B0 bridges), lane 506 (B3 barrier), lane 505 incl. `cont_505` (B2 closed: `enstrophy_differential_on_IccT`, conditional only on the three torus bridges), while lane 504 (B1) is already on core). Read `collaboration/briefs/COMMON_PHASE5.md` (ground rules: commit after every closed lemma; new modules into `formalization/blueprint/entrypoints.json` `proof_modules`; rerun `python3 experiments/audit_article_axioms.py --build --output-dir tmp/article-audit --workers 2` before `make check`; `check_contracts.py` has no `--base-ref`, use `--summary`), `research/P21/ASSESSMENT.md` (targets, Route B, risks), `research/P21/P6_SPLIT.md` (B0/B2/B3 handoffs; the exact three torus norm-bridge hypotheses B2 threads), `research/P21/REPORT_505.md` (+ its `## cont` section), `REPORT_503.md`, `REPORT_506.md`, `research/P21/Targets.lean` (`h1RestartT` is the Prop to prove), and — as the ℝ³ template — lane 507's `Section4/A04/H1Restart.lean` and `research/P21/REPORT_507.md` if already on your branch/core (launch note: lane 507 is NOT merged yet; its in-progress module is readable at `/data_8T/ping/blowup_density/.claude/worktrees/507-P21-B4-restart-r3/formalization/NSFormalization/Section4/A04/H1Restart.lean` — read it for the route, do not copy it into your worktree). Do NOT edit the modules of lanes 503/505/506 (`Section3/T11/H1Bridges.lean`, `Section3/T11/EnstrophyInequality.lean`, `Section4/A04/EnstrophyBarrier.lean`); anything you need beyond them goes into your own module.

## Target (unit B4, 𝕋³)
New module `Section3/T11/H1Restart.lean` whose main theorem is literally `h1RestartT` of `research/P21/Targets.lean`, i.e. the `restart` field of `PeriodicContinuationH3API` (`verification/Contracts/V1/TorusLocalTheory.lean:473-482`) with `periodicSobolevENorm 3` replaced by `periodicSobolevENorm 1`:
```
∀ ν, 0 < ν → ∀ f, f ∈ forceClassT → ∀ S, 0 ≤ S → ∀ K : ℝ≥0∞, K ≠ ⊤ → ∃ δ > 0,
  ∀ t₀ ∈ Icc 0 S, ∀ a' ∈ initialClassT, periodicSobolevENorm 1 a' ≤ K →
    ∃ w : ClassicalSolutionT ν a' (timeShiftT t₀ f) δ, PeriodicLocalRegularity ν a' (timeShiftT t₀ f) δ w
```
Use the T10/T11 local names (the contract names are `rfl`-bridged in `verification/Bindings/TorusLocalTheory.lean`).

## Structural facts you should know before choosing the route
- The whole existing H¹ downstream on the torus is already parametrized by the named input `PeriodicQuantitativeLocalInput'` (`Section3/T11/LocalExistence.lean:24`): `Restart.lean:72 restart (H)`, `RestartBeyond.lean:408 restartBeyond (H)`, `ExtendsBeyond.lean:101 extendsBeyond_of_input (H)`. The registered H³ versions (`ExistenceInputH3.lean:350 restartH3`, `:406 restartBeyondH3`, `:456 extendsBeyondH3`) instantiate the same proofs with `periodicQuantitativeLocalInputH3` (Picard horizon with an H³ datum ball). BUT `PeriodicQuantitativeLocalInput'` controls the force only through `forceSobolevENormT 1 m g ≤ M m` (L¹ in time), which does not bound `sup_t ‖g(t)‖_{L²}`; the B3 barrier as stated needs a constant force cap F. Two admissible routes:
  (R1, preferred, fixed force) prove `h1RestartT` directly for `f ∈ forceClassT`: the shifted force `timeShiftT t₀ f` is smooth and periodic (503: `timeShiftT_smooth_periodic`) and its L² slices on `[0,1]` are bounded by `forceL2CapT f S` (503: `timeShiftT_force_slice_le_forceL2CapT`, `forceL2CapT_ne_top` for `MemForceT`; check the `forceClassT` ↔ `MemForceT` relation in T10). Then, if useful for B5, state a generalized input predicate `PeriodicQuantitativeLocalInputH1Sup` (H¹ ball + sup-in-time L² cap on `[0,1]` + the L¹ H^m bounds) and check whether `Restart.lean`/`RestartBeyond.lean` proofs go through verbatim with it (record the answer in P6_SPLIT.md for B5).
  (R2, stronger, only if it is not harder) prove `PeriodicQuantitativeLocalInput'` itself with a barrier variant that accepts L¹-in-time forcing (e.g. on `W := sqrt(1+Y)`: `W' ≤ C W⁵ + ‖g(t)‖_{H¹}`), which would close `restart`, `restartBeyond`, `extendsBeyond_of_input` at H¹ for free. A new barrier lemma then goes into your module, not into 506's file.
- Torus slices are smooth periodic fields, so 503's periodic bridges (`periodicSobolevENorm_one_toReal_sq_eq`, `periodicSobolevENorm_two_toReal_sq_eq`, `periodicSobolevENorm_two_eq_one_add_gradient_hessian`; energies `periodicL2Energy/periodicGradientEnergy/periodicHessianEnergy`) should apply directly (no compact-support issue as on ℝ³); what remains is converting them to B2's carriers (`lTwoSqT/gradientSqT/laplacianSqT`, κ = 1) including `periodicHessianEnergy z = laplacianSqT z` (Parseval on the lattice: Σᵢⱼ|kᵢkⱼ|² = |k|⁴) or the inequality direction B2's `hTwo` needs; and `hGradient`.
- Time regularity: `ClassicalSolutionT.sobolev` gives a continuous `IsPeriodicDatum m` path on `Ico 0 T` for every m (`periodicSobolevENorm m (slice t) = ‖G t‖`, datum unique); B2's identity gives the derivative on `Ioo 0 T`; hence `Y` continuous on `[0,T)`, differentiable inside, `Y 0 = (periodicSobolevENorm 1 a').toReal^2`, `Z` continuous ⇒ integrable on compact subintervals, `periodicSobolevENorm 2 (slice t) ≠ ⊤`.
- Maximal solution and continuation on the torus: `Section3/T11/Maximal.lean` (`maximalLifespanT`, existence of the maximal periodic solution — see how `verification/Bindings/TorusLocalTheory.lean:374 exists_maximal` is discharged), the unconditional H³ continuation `ExistenceInputH3.lean:456 extendsBeyondH3` (its `hHigh` input is discharged in the binding at `TorusLocalTheory.lean:395-411`; reuse the same T11 theorems), and `ExtendsBeyondT` (`Contracts/V1/TorusLocalTheory.lean:262`). Contradiction shape: if `maximalLifespanT ν a' f' ≤ ofReal δ'` then B2+B3 give `squaredHTwoIntegralT S' u ≠ ⊤` at `S' = lifespan.toReal`, `extendsBeyondH3` gives a strictly larger classical solution, contradicting the supremum defining the lifespan.
- Regularity bundle: `PeriodicLocalRegularity` (`T11/LocalTheory.lean`, fields `sobolev_smooth`, `pressure_poisson`, `projected`). Find how the binding's `regularity` field (`Bindings/TorusLocalTheory.lean:359`) and `restartH3` obtain it, and whether a theorem gives it for an arbitrary classical periodic solution (uniqueness `T11/Uniqueness.lean` + local patches otherwise). Record exactly what you used.

## Proof route (each step its own committed lemma, with probes)
1. Reconciliation B0 ↔ B2: discharge B2's three torus bridge hypotheses for slices; export `enstrophy_differential_on_IccT'` with no bridge hypotheses. (If B2's final theorem is still missing on your branch, i.e. `cont_505` did not close it, first read `ATTEMPTS_B2.md` and close the remaining residual in your module — say so in the report.)
2. Time-regularity adapters; 3. force cap; 4. barrier (506 `enstrophy_uniform_barrier_and_dissipation`, `c := ν`, `C := Cν`, `K := K.toReal^2`, `F := (forceL2CapT f S).toReal`, `δ' := min d 1`); 5. maximal-lifespan contradiction via `extendsBeyondH3` + 506 `enstrophy_endpoint_integral`; 6. conclusion `h1RestartT` with `δ := δ'/2` (or δ') and the regularity bundle; 7. B5 handoff in `research/P21/P6_SPLIT.md`: the exact statement B5 needs for `h1UniformEndpointT` (= `restartBeyond` field at order 1) and whether `RestartBeyond.lean:408`'s proof consumes only the `restart` conclusion (then B5 is an instantiation).

## Deliverables and gates
Module (in `entrypoints.json`), `research/P21/probes/b4t_closes.lean` (non-vacuity on an explicit case + one must-fail mutation), `research/P21/axioms_b4t.lean` (exactly `[propext, Classical.choice, Quot.sound]`), `research/P21/ATTEMPTS_B4T.md`, `P6_SPLIT.md` (B4-𝕋³ status + B5 handoff), `research/P21/REPORT_508.md` (four parts). No contract, blueprint-status or `paper/` change (B5 registers). Gates per COMMON. Commit after each closed lemma with `[508-P21-B4T] …`; no push/merge/rebase. Honest partial with exact residual goals beats a fake closure.
