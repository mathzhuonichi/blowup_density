# Lane 503-P21-B0-h1-norm-bridges — P6 Route B unit B0: H¹/H² Fourier-norm ↔ derivative-energy bridges and the shifted-force cap (both domains)

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/503-P21-B0-h1-norm-bridges` (branch `erenup/503-P21-B0-h1-norm-bridges`, = `origin/erenup/core`). Read `collaboration/briefs/COMMON_PHASE5.md` (ground rules; commit after each closed lemma; new modules must be listed in `formalization/blueprint/entrypoints.json` `proof_modules`; after adding modules run `python3 experiments/audit_article_axioms.py --build --output-dir tmp/article-audit --workers 2` so `make check` does not stop at "Source changed: rerun the article axiom audit"), then **`research/P21/ASSESSMENT.md`** (lane 499: §1 exact targets in registered vocabulary, §2 Tao vs the tree, §3 Route B units B0–B5 with risks, §4) and `research/P21/REPORT_499.md`. Route B in one line: for the smooth-data, fixed-force targets, start from the proved high-order local solution and maximal development; prove on each shorter classical interval, with `Y = ‖u‖²_{H¹}`, `Z = ‖u‖²_{H²}`, the differential inequality `Y' + cν Z ≤ Cν (1+Y)³ + Cν ‖f‖²_{L²}` (3D interpolation `|convection pairing| ≤ C‖u‖_{H¹}^{3/2}‖u‖_{H²}^{3/2}` + Young), then an ODE barrier gives one time `d > 0` and a uniform `Y` bound depending only on the H¹ datum bound and the force; if the maximal time were `≤ d`, the squared-H² integral would be finite at that endpoint and the unconditional H⁷/H³ continuation contradicts maximality. Risks listed in ASSESSMENT §3 apply verbatim (δ before datum and restart time; inhomogeneous H² includes low modes; endpoint integrability by monotone limits; carriers/bridges).

## Goal (unit B0 of ASSESSMENT §3 Route B; M)
New modules `Section4/A04/H1Bridges.lean` (ℝ³) and `Section3/T11/H1Bridges.lean` (T³), namespace as their neighbours:
1. Identify the registered inhomogeneous Sobolev norms at orders 1 and 2 with derivative energies for smooth compactly-supported / smooth periodic fields: `sobolevENorm 1 z` (resp. `periodicSobolevENorm 1 z`) squared = `‖z‖²_{L²} + ‖∇z‖²_{L²}` and order 2 = order 1 + `‖∇²z‖²_{L²}` (or the exact equivalences with explicit constants that the Fourier definitions give — state what you prove; equalities with the registered weights `1 + |ξ|²` are expected). Reuse `Section4/D01` datum lemmas, `Section4/A05`, `Section3/T10/ParsevalZero`-type identities, `Section3/T20/H1Energy.lean` (read its hypotheses: its absorbed estimate assumes critical smallness — do NOT reuse that part).
2. The shifted-force cap: for `f ∈ MemForceR` (resp. `forceClassT`) and `S ≥ 0`, a bound `sup_{t₀ ∈ Icc 0 S} sup_{t ∈ Icc 0 1} ‖f(t₀ + t)‖_{L²} ≤ F(S)` (continuous force on the compact window `[0, S+1]`) and the corresponding statement for the time-shifted force `timeShift t₀ f` (`Section4/A04/RestartFixedForce.lean: referenceForce_timeShift_norm_le` is the model) — on the torus check whether `timeShiftT t₀ f` stays in `forceClassT` (ASSESSMENT warns it may not) and state exactly what B3/B4 will need.
3. The exact Route B target statements of ASSESSMENT §1 (`h1RestartR`, `h1RestartT` and the uniform endpoint targets) as `def … : Prop` in `research/P21/Targets.lean` (research file importing Contracts — type-check only; no proof).
Deliverables: modules (listed in entrypoints), probes `research/P21/probes/b0_closes.lean`, `research/P21/axioms_b0.lean`, `research/P21/ATTEMPTS_B0.md`, status in `research/P21/P6_SPLIT.md` (create it from ASSESSMENT §3 Route B table). Gates: builds, `lake env lean` zero output, probes, axioms, `make check`. Report `research/P21/REPORT_503.md` (four parts).
