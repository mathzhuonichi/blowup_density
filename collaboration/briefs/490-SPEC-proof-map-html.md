# Lane 490-SPEC-proof-map-html — an explanatory, good-looking HTML "proof map" of Sections 3 and 4 for a non-expert reader

You are a documentation/visualisation worker on the repository checked out at your working directory
`/data_8T/ping/blowup_density/.claude/worktrees/490-SPEC-proof-map-html` (git branch `erenup/490-SPEC-proof-map-html`, = `origin/erenup/integration-section3`: Section 4 (37 contracts) + Section 3 (18 contracts) = 55 registered contracts, everything compiled). **No Lean work, no Lean build** — read files only.

## Audience and purpose
The reader is the project owner's colleague with no Lean background ("小白"). One self-contained HTML page (Chinese prose, identifiers in English) that answers, clearly and honestly:
1. **What is being proved** — the paper's goal in one paragraph per section: Section 4 = the whole-space (R³) theorems 4.1–4.7 (density / non-density of blow-up forces: the set of forces that produce a finite-time singularity from zero data is dense in `L¹_tH^s_x` exactly when `s < 1/2`, plus the critical-regularity converse and the explicit insertion construction); Section 3 = the same programme on the torus T³ (`thm:main`, `cor:nondensity`, `prop:density`, `prop:critical`, `thm:insertion`, `lem:correction`, `prop:scaling`, `cor:boundary`, `prop:affine/multiple/conservative`). Quote the informal statements from `paper/sections/04-whole-space.tex` and `paper/sections/03-torus.tex` (read them; cite the line ranges), paraphrased for a layperson with an intuitive picture (a "packet" that blows up at time T is rescaled, corrected and inserted into a smooth background; the correction is small in the force norm below the critical order 1/2).
2. **The dependency graph** — interactive SVG (inline, no external libraries; hand-laid-out DAG, left-to-right, grouped in swim-lanes): Section 4 chains (insertion spine `I01 → I02 → I03 → R42 → R41`; analysis spine `A01 → A02 → A04 → C01 → R43/R44 → R41`; norm/data layer `D01, B01, B02, A03, A05`; closing contracts `R45, R46, R47`), Section 3 nodes `T10 … T24` with the critical path `T10 → T11 → T18 → T19 → T21` and the converse path `T10/T11 → T20 → T21`, the corollary layer (`T22 → T23`, `T15 → T24`), and the **cross-section edges** (which R³ contracts each torus node consumes: `T14` imports the `I01` packet, `T23` consumes `I02/I03`, `T15/T17/T18` mirror `I03/I02/R42`, etc. — derive these from the actual `import`s and from `research/T*/RECONCILIATION.md` / `T23_SPLIT.md` §0, not from memory). Clicking a node opens a side panel with: plain-language meaning, paper label, registered contract id(s) + version, Lean folder (`formalization/NSFormalization/Section3/T19/`, `verification/Contracts/V1/Density.lean`, …), PR number(s), status colour, size (modules / declarations), and caveats.
3. **Is every dependency formalised in Lean?** A per-node status table (all 55 contracts, from `verification/contracts.json`: id, version, parent task, module, one-line scope) with colour coding: ✅ registered contract; ✅ proved-and-consumed canonical module; ⚠ conditional/owner-pending wording. Facts to state: every registered declaration depends only on `[propext, Classical.choice, Quot.sound]`; no `sorry`/`admit`/extra axioms; statement fidelity was checked by two independent blind drafts + reconciliation per node (`research/T*/COMPARISON.md`, `RECONCILIATION.md`), every proof lane was reviewed by an independent codex reviewer with negative-mutation probes (`research/T*/REVIEW_*.md`, `probes/rev*_mutation.lean`).
4. **Is it elegant / trustworthy?** Explain the architecture in one diagram: paper → reconciled Spec (`research/T*/Spec.lean`) → canonical proofs (`formalization/…`) → frozen contract statements (`verification/Contracts/V1`) → bindings (`rfl` bridges / fieldwise conversions) → axiom tests (`verification/Tests`, `warningAsError`) → registry (`verification/contracts.json`, `check_contracts.py`). Explain why this separation matters (kernel checks the proof, the contract layer checks that the *statement* is the paper's; CI refuses silent edits to frozen contracts).
5. **Does it all compile?** Section 4: 206 modules genuinely re-elaborated, exit 0, 37 contracts (from the two build reports quoted in `research/MAINT/section4_pr259_body_snapshot_20260919.md` — the PR #259 body — and PR comments; cite what you can verify in-repo). Section 3: `logs/SECTION3_BUILD_20260919i.md` — 194 modules, 148.65 s, 0 errors, 55 contracts, 67 standard-axiom check lines, 335 probes passing / 49 expected reviewer negatives, 2,309 axiom lists all standard. Show the gate commands.
6. **Honest caveats** (a visible "what is still owner-pending" box, from `logs/SECTION3_OWNER_DECISIONS_20260919.md`): T17 G4/G5 wording (registered amended block; slab bridge), T23 G0 (existential repair registered, the literal universal-cutoff statement is *false* and not registered), G1 (smooth-level domains only conditional on the explicit `IBP Ω`; boxes unconditional), Prop/Type conventions, CI has no quota so local gates replace it, PR #259 → main awaits owner review, PR #270 → integration ready.

## Sources (read, do not invent numbers; every number on the page must trace to one of these)
`verification/contracts.json`; `formalization/blueprint/DEPENDENCY_GRAPH.md` and `tasks.json`; `collaboration/SECTION3_PLAN.md` (T10–T24 DAG, §8); `archive/section4/PLAN_SECTION4.md` (Section 4 chains, waves); `research/MAINT/section3_pr_body_snapshot_20260919.md` (the T10–T24 progress table with PR numbers) and `research/MAINT/section4_pr259_body_snapshot_20260919.md`; `logs/SECTION3_BUILD_20260919i.md`, `logs/SECTION3_OWNER_DECISIONS_20260919.md`, `logs/AGENT_RUNS.csv` (lane → PR), `PLAN.md`; `paper/sections/{03-torus,04-whole-space}.tex`; `research/T*/RECONCILIATION.md` §4 (dependency ledgers) and `research/T23/T23_SPLIT.md` §0/§2; `ls formalization/NSFormalization/Section3 Section4` and `find … -name '*.lean' | wc -l` per node for sizes. `CLAUDE.md` for the architecture rules.

## Deliverable
- `docs/proof_map/index.html` — one file, inline CSS + vanilla JS + inline SVG, no external requests, works offline in any browser and at phone width (responsive; the graph pans/zooms or stacks). Light theme, clean typography, a colour legend, a top summary strip ("55 contracts · 400 modules · 0 sorry · 3 standard axioms · Section 4 build ✓ · Section 3 build ✓"), then the six sections above in order. Chinese UI text; identifiers/paths monospace. Keep it under ~400 KB.
- `docs/proof_map/README.md` — how the data was extracted (commands), the commit checked (`git rev-parse HEAD`), and a table mapping every number on the page to its source file/line.
- Do not touch anything else. Commit on your branch (docs only). Write a four-part report to `research/MAINT/REPORT_490.md` (what the page contains / files / anything you could not verify and left out or marked as unverified / commands run).

## Ground rules
Work ONLY inside this worktree; never `git push`, merge or rebase. No Lean builds. If a claim cannot be verified from the sources, mark it visibly as "未核实" on the page rather than asserting it. Commit a first skeleton of the page within 15 minutes and refine incrementally.
