1. Route β: confirm `HasAprioriBoundInv` differs from `HasAprioriBound` (`Section4/A01/Horizon.lean:106`) exactly by adding the angle-invariance hypothesis `∀ θ t, sobolevTranslation 1 (q+1) (0,θ) (u t) = u t` on the quantified `u` — no other weakening (same `R`, same windows, same Duhamel clause).
2. Consumer re-closure: `localTheory_on_prescribed_horizon_of_boundInv` must have **the same conclusion** as `Horizon.localTheory_on_prescribed_horizon` (`:137-157`, all seven output clauses incl. `‖u‖ ≤ R`, the descent, divergence-free, Duhamel, invariance) with only the hypothesis weakened; diff the two statements token by token and report any dropped clause. Same for `forced_global_mild_of_boundInv` vs `Continuation.forced_global_of_bound_unconditional`.
3. Where does the invariance of the consumer's own solution come from in the proof (`forced_ordinary_descent`, `Continuation.lean:147`, or the vendor's invariant continuation)? Confirm it is proved, not assumed.
4. Non-vacuity: the positive-time example — does it exercise `localTheory_on_prescribed_horizon_of_boundInv` with a genuine `HasAprioriBoundInv` witness, or only the trivial implication?
5. Route α (invariance propagates to every Duhamel solution) was not attempted; note whether the vendor has translation-equivariance + uniqueness for `quadraticDuhamel` that would make α a short follow-up (grep `sobolevTranslation`, `quadraticDuhamel`, `unique` in the vendor and `FormalPatched/`).
