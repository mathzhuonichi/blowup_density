# Lane 500-T24-P5b-domain-blowup-energy — Partial P5 units P5.3 + P5.4: per-ball singularity and energy/dissipation additivity on Ω

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/500-T24-P5b-domain-blowup-energy` (branch `erenup/500-T24-P5b-domain-blowup-energy`, = `origin/erenup/core` with lane 496 merged). Lane 497 works **in parallel** on P5.1/P5.2 (`MultipleOmegaComponents.lean`, `MultipleOmegaAssembled.lean`): do not create those modules; state your theorems over the explicit facts you need (the per-region placement/`ε`, the component pins `component_pin`, the supports `component_support`/`component_force_support`, `regions_disjoint`, the sum formula `assembled_velocity = Σ (component j).velocity`) **as hypotheses**, exactly as the T24 torus lane 469 did (`MultipleRegions.lean` defines its own local `assembledVelocity` with the same body; the assembly lane reconciles by `rfl`). Read `collaboration/briefs/COMMON_PHASE5.md` first (ground rules; commit after each closed unit; thread cross-lane facts as explicit hypotheses — the assembly lane 501 discharges them; new modules must be listed in `formalization/blueprint/entrypoints.json` `proof_modules` or `make check` fails with "Unreferenced Lean modules"). Then `PLAN.md` §1 row P5, `research/T24/T24_SPLIT.md` section "P5 units" (lane 496; exact target fields and suppliers per unit), `research/T24/COMPARISON_OMEGA.md`, `research/T24/SpecOmega.lean`, the canonical record `Section3/T24/MultipleOmega.lean` (`MultipleRegionsOmegaAPI`, `energyEssSupOmega`, `energyGradientOmega`, `multipleRegionsOmegaStatement`), the torus proofs to mirror `Section3/T24/{MultipleComponents,MultipleAssembled,MultipleRegions,MultipleAssembly}.lean`, the owner's domain layer `Section3/T23/{DomainSolution,Placement,Triple,Solution,NoSlipEnergy,BoundaryIntegration}.lean`, the un-periodised scaled packet `Section4/I03/*` (`scaledVelocity`, `scaledPressure`, `scaledForce`, `scaledEquation`, `scaledDivergenceFree`, `scaledBlowup`, energy identities) and `Source/PacketScaling`, and the article `paper/revised/sections/03-torus.tex` `prop:multiple` (revised lines 511–535).

## Goal
New module `Section3/T24/MultipleOmegaRegions.lean`:
- **P5.3**: `region_agreement` (on `B_j` every other component vanishes ⇒ the sum equals `component j`), `region_blowup : SpeedUnboundedAtOn T (ball c_j ρ_j) assembledVelocity` — localise the raw scaled blow-up witness (`Source.PacketScaling speed_unbounded_at_target`, `zeroPastField_speed`, the raw packet `SpeedUnboundedAtOne`; the T15 route in `Section3/T15/Blowup.lean` and its use in `MultipleRegions.lean` show the transfer) into the ball, without periodisation.
- **P5.4**: `energy_bound : (energyEssSupOmega Ω T u)^2 ≤ ofReal (M^2 Σ ε_j)` and `dissipation_bound : (energyGradientOmega Ω T u)^2 = ofReal (D^2 Σ ε_j)` — restricted integrals equal whole-space integrals for each compactly supported component and its full Euclidean gradient (supports inside `Ω`), disjoint-support additivity of slice energies and gradients (mirror `MultipleRegions.lean`), then the I03 identities (`eLpNorm_scaled_slice`, `energyEssSup_scaled_le`, `scaled_total_dissipation`, `energyGradient_scale…` — read the exact names in `Section4/I03/Energy.lean` and the T15 `Energy.lean` wrappers).
Deliverables: module (listed in `entrypoints.json`), `research/T24/probes/p5b_closes.lean` (four fields by `exact` at the threaded hypotheses), `research/T24/axioms_p5b.lean`, `research/T24/ATTEMPTS_P5B.md`, P5.3/P5.4 status lines in `T24_SPLIT.md`. Gates: build, `lake env lean` zero output, probe, axioms, `make check`. Report `research/T24/REPORT_500.md` (four parts).
