# Lane 478 (continuation) — T23 U7: close `noSlip_uniqueness` for the **box** branch; record the smooth-domain branch as an exact residual

Same worktree/branch (`/data_8T/ping/blowup_density/.claude/worktrees/478-T23-U7-noslip-uniqueness`, `erenup/478-T23-U7-noslip-uniqueness`), same ground rules as `collaboration/briefs/478-T23-U7-noslip-uniqueness.md`. Read your own `research/T23/REPORT_478.md`, `ATTEMPTS_U7.md` (the exact residual list), `Section3/T23/{BoxIntegration,NoSlipUniqueness}.lean`
(the canonical domain record, `IsBoxDomain`, `IsRegularLevelDomain`, `SmoothOnClosedSlab`, and the seven proved lemmas incl. the `Icc a b : Set (Fin 3 → ℝ)` integration by parts, `difference_energy_integrable`, `eqOn_of_integral_norm_sub_sq_eq_zero`, `spatialDerivative_bound`).

**Lead priority (2026-09-19 16:12Z): close the box case completely first; the smooth-level-domain case is an honest residual unless Mathlib has the divergence theorem you need** (a regular-level-domain IBP theorem was not found by your search — that is acceptable; state the exact missing theorem as the residual).

## Goal
1. `theorem noSlip_uniqueness_box` — the U7 target restricted to `IsBoxDomain Ω` (same binders otherwise): (i) transport your `Icc` integration-by-parts to the Spec's box (`Ω` = the open Euclidean box; the boundary is where no-slip kills the boundary term — the difference `w` vanishes on `∂Ω` by the no-slip field of both solutions; the closed slab smoothness gives the regularity on `closure Ω`),
   (ii) the difference-energy identity `E(t) = ∫_Ω ‖w(t)‖²`: `E` is differentiable on `(0, S)` for `S < min T₁ T₂` with `E' = −2ν∫‖∇w‖² − 2∫ ((w·∇)u₂)·w` (pressure term vanishes by `div w = 0` + no-slip via (i); the `(u₁·∇)w · w` term vanishes by `div u₁ = 0` + no-slip via (i)), (iii) `|∫ ((w·∇)u₂)·w| ≤ C·E` with `C` from `spatialDerivative_bound`, so `E' ≤ 2C·E`,
   (iv) Grönwall (`E(0) = 0` since both start from `a'`; Mathlib `gronwallBound`/`norm_le_gronwallBound_of_norm_deriv_right_le` or the `ODE_solution_unique`-style lemmas) ⇒ `E ≡ 0` on `[0,S]`, then `eqOn_of_integral_norm_sub_sq_eq_zero` ⇒ `u₁ = u₂` on `Ω`, and `S ↑ min T₁ T₂`.
2. `theorem noSlip_uniqueness` in the exact U7 form = case split on `IsBoundedBoxOrSmoothDomain`: box branch from (1); for the smooth branch, if no divergence theorem is available, do **not** fake it: deliver instead `noSlip_uniqueness_of_ibp` (the full theorem under an explicit hypothesis `IBP Ω` stating exactly the boundary integration-by-parts identity you need for regular-level domains — one named `def`, with a docstring saying it is the sole missing analytic input, proved for boxes as `ibp_box`), and record in
   `research/T23/SPEC_ISSUES.md` (§G1, new) the exact statement of the missing theorem and the Mathlib search evidence. The lead will decide whether V1 registers the box case only.
Deliverables: updated modules, `research/T23/probes/noslip_box_closes.lean`, updated `axioms_u7.lean`, `ATTEMPTS_U7.md`, U7 status line, `research/T23/REPORT_478b.md` (four parts). Same gates as before. Commit after each closed step.
