# Lane 502 (continuation) — refresh again on the latest `erenup/core`

Same worktree/branch (`/data_8T/ping/blowup_density/.claude/worktrees/502-MAINT-core-refresh`), same brief `collaboration/briefs/502-MAINT-core-refresh.md`. Since your first run the lead merged more closing lanes into `erenup/core` and has merged `origin/erenup/core` into this branch (generated files taken from core, i.e. stale). Redo the full procedure on the merged tree: regenerate `DEPENDENCY_GRAPH.md`, rerun `audit_article_axioms.py --build` and update `AXIOM_AUDIT.json`, make every count/list consistent with `proof_graph.json` (README, `CLOSURE_AUDIT.md`, guide `\coverage` rows, `RESULT_MAP.md`, `experiments/check_reader_documents.py` `partial = {…}` set), then `make check`, `make test`, `make test-mutations`, `make paper`. Append a "second run" section to `research/MAINT/REPORT_502.md` with the new counts and gate outputs. Commit.
