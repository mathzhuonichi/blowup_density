# Lane 511-P21-B5-torus-closure — P6 Route B unit B5 on 𝕋³ + closure of the last Partial node `L21_H1` (Proposition 2.1): the periodic H¹-uniform endpoint theorem, the `Contracts/V2/TorusLocalTheory` registration, and the blueprint / guide / reader-document closure

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/511-P21-B5-torus-closure` (branch `erenup/511-P21-B5-torus-closure` = lane 508 (B4-𝕋³ closed: `Section3/T11/H1Restart.lean`, `h1RestartT`) + lane 510 (B5-ℝ³ closed: `Section4/A04/H1RestartBeyond.lean`, `Contracts/V3/Continuation.lean`, `Bindings/ContinuationV3.lean`, `Tests/ContinuationV3.lean`, registry `A04.continuation_v3`, 35 contracts) + `origin/erenup/core`; it also contains lanes 503/505/506/507, all reviewed or under review — do NOT edit any of their modules; add new files only). Read `collaboration/briefs/COMMON_PHASE5.md` (ground rules and the 4-step closing procedure; commit after every closed step; `python3 experiments/audit_article_axioms.py --build --output-dir tmp/article-audit --workers 2` before `make check`; `check_contracts.py` has no `--base-ref`, use `--summary`), `research/P21/P6_SPLIT.md` (§ "B4-T³ handoff — lane 508": exact endpoint target and the consumption check; § lane-511 handoff written by lane 510), `research/P21/REPORT_508.md`, `REPORT_510.md`, `research/P21/Targets.lean` (`h1RestartT`, `h1UniformEndpointT`), `research/P21/ASSESSMENT.md` (targets + coverage ledger), and lane 510's registration files as the pattern for the torus half.

## 1. Periodic endpoint theorem (`formalization/`)
New module `Section3/T11/H1RestartBeyond.lean` (add to `entrypoints.json` `proof_modules`): `restartBeyondH1T` = `h1UniformEndpointT` of Targets.lean = the `restartBeyond` field of `PeriodicContinuationH3API` (`verification/Contracts/V1/TorusLocalTheory.lean:502-518`) with `periodicSobolevENorm 3` replaced by `periodicSobolevENorm 1` (larger classical solution on `S + δ`, exact velocity and normalized-pressure overlap on `Ico 0 S`). Lane 508 checked that `Section3/T11/RestartBeyond.lean:408 restartBeyond (H)` consumes its named input ONLY through `restart H`; so either (a) restate that proof with `h1RestartT` in place of `restart H` (preferred: copy the proof skeleton into the new module, no edit of `RestartBeyond.lean`), or (b) if `restartBeyond`'s proof factors through an intermediate lemma taking the `restart`-shaped conclusion as a hypothesis, apply that lemma to `h1RestartT`. Keep δ as delivered by `h1RestartT` at `S` (or smaller); record which.

## 2. Contract V2 for the torus (`verification/`; V1 frozen)
- `verification/Contracts/V2/TorusLocalTheory.lean` importing `Contracts.V1.TorusLocalTheory` (reuse every V1 registered definition — `ClassicalSolutionT`, `PeriodicLocalRegularity`, `initialClassT`, `forceClassT`, `periodicSobolevENorm`, `timeShiftT`, `SolvesBelowT`, `maximalLifespanT` — restate nothing that exists): `structure PeriodicContinuationH1API : Prop` with fields `restart` (token-for-token the V1 `restart` field with `1`; docstring "appendix-a-local-theory.tex:146-151 at the H¹ ball, verbatim") and `restartBeyond` (token-for-token the V1 `restartBeyond` field with `1`; docstring "appendix-a-local-theory.tex:146-153, verbatim, uniform H¹ trajectory bound"); `def torusLocalTheoryV2Statement : Prop := Nonempty TorusLocalTheoryAPI ∧ PeriodicContinuationH1API` — or whatever shape `Bindings/TorusLocalTheory.lean` / `Tests/TorusLocalTheory.lean` use for V1 (mirror it; check `verification/contracts.json` entry `T01.torus_local_theory` and its `declaration`).
- `verification/Bindings/TorusLocalTheoryV2.lean`: discharge `restart` with lane 508's `h1RestartT` through the existing `toContract` / `periodicLocalRegularity_toContract` conversions used by `Bindings/TorusLocalTheory.lean` (and by 508's probe), `restartBeyond` with §1's theorem; `rfl` bridges for anything restated. `verification/Tests/TorusLocalTheoryV2.lean`: `checkedTorusLocalTheoryV2` + `run_cmd TestSupport.checkAxioms ``checkedTorusLocalTheoryV2` + `example`s projecting both new fields (Tests has `warningAsError`).
- Registry: append `{"id": "T01.torus_local_theory_v2", "parent_task": "T11", "version": 2, "specification": "verification/Contracts/V2/TorusLocalTheory.lean", "binding_module": "Bindings.TorusLocalTheoryV2", "test_module": "Tests.TorusLocalTheoryV2", "declaration": "BlowupDensity.Tests.checkedTorusLocalTheoryV2", "scope": "Proposition 2.1 / appendix A on T3: restart uniform on an H1 datum ball with one duration before restart time and datum, and the H1-uniform restartBeyond with exact velocity and normalized-pressure overlap, for a fixed force in forceClassT; V1's H3 routes stay registered.", "enabled": true}` (36 contracts; check `parent_task` against the existing V1 entry and use the same).

## 3. Closure of `L21_H1` (the 4-step procedure; both domains are now registered)
- `formalization/blueprint/proof_graph.json`: node `L21_H1` → `"status": "Closed"`, `"completion_from": []`, `"depends_on": ["L21T", "L21R"]` (Closed may not depend on Partial; check that both are Closed), `scope` rewritten in the owner's style, e.g. "The H¹-uniform restart clauses of Proposition 2.1 are proved on both domains for the used fixed force classes (`MemForceR`, `forceClassT`) via the enstrophy differential inequality, a uniform ODE barrier and the registered H⁷/H³ continuation routes; they are not inputs to the proved continuation routes." `evidence`: `verification/Bindings/ContinuationV3.lean`, `verification/Bindings/TorusLocalTheoryV2.lean`, `formalization/NSFormalization/Section4/A04/H1Restart.lean`, `formalization/NSFormalization/Section3/T11/H1Restart.lean` (+ the two `H1RestartBeyond.lean`). Keep the node in its panel. Run `python3 experiments/check_formalization_plan.py` (regenerates `DEPENDENCY_GRAPH.md`) and `python3 experiments/test_contract_policy.py`.
- `formalization/blueprint/RESULT_MAP.md`: row "Proposition 2.1 | `prop:local` | Closed | …" adding the four new sources with exact line anchors (keep the existing five). `CLOSURE_AUDIT.md`: regenerate/update per the owner's tooling (see how lanes 495/501 did it: `git log -p --follow formalization/blueprint/CLOSURE_AUDIT.md | head`).
- `paper/formalization_guide.tex`: `\mapped{Proposition}{prop:local}` block → `\coverage{Closed}` and add `\source{B/ContinuationV3}{L}{continuationV3_holds}{theorem}`-style lines for the two new bindings and the two `h1Restart*` theorems (line numbers exact; follow the existing `\source{…}` macro conventions in the file); in the paragraph "Local theory and continuation (Proposition prop:local)" append two or three sentences: the H¹-uniform restart is obtained by the enstrophy inequality `Y' + νZ ≤ Cν(1+Y)³ + Cν‖f(t)‖²_{L²}` on the maximal smooth solution, a barrier time depending only on ν, the H¹ ball and the force cap on `[0,S+1]`, and the existing integral continuation criterion — no change to the article's mathematics. `README.md`: counts → "27 Closed and 0 Partial" (verify the exact wording/numbers used by the owner). `experiments/check_reader_documents.py`: `partial = set()`. Regenerate `AXIOM_AUDIT.json` (audit `--build`) and both PDFs (`make paper`); the guide PDF must show Proposition 2.1 Closed.
- Update `research/P21/P6_SPLIT.md` (all units closed, table complete) and add a short "Outcome (2026-09-21)" paragraph at the top of `research/P21/ASSESSMENT.md` (do not rewrite history). Apply any deferred reviewer style notes recorded in `research/P21/REVIEW_50{3,5,6,8}*.md` lead rulings (only files not under active review: check with the lead's notes in the review files; when in doubt leave them).

## 4. Gates and report
`make check` (36 contracts; 27 article mappings; 0 Partial; all 11 policy tests), `make test`, `make test-mutations`, `make paper`, `python3 experiments/check_reader_documents.py`, `git diff --check`, `scripts/gates.sh` on the new modules. Probes: `research/P21/probes/b5t_registered.lean` (`#print axioms` of `checkedTorusLocalTheoryV2` and `checkedContinuationV3`; non-vacuity: instantiate the torus `restartBeyond` at order 1 on the zero solution). Report `research/P21/REPORT_511.md` (four parts, plus the exact list of blueprint/guide/README lines changed). Commit after each closed step with `[511-P21-B5T] …`; no push/merge/rebase.
