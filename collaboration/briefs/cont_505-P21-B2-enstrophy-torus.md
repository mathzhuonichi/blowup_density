# cont_505 — finish P6 Route B unit B2 (𝕋³ enstrophy differential inequality); same worktree, same branch

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/505-P21-B2-enstrophy-torus` (branch `erenup/505-P21-B2-enstrophy-torus`). This is a continuation of lane 505: your own previous run delivered ten kernel-checked declarations in `Section3/T11/EnstrophyInequality.lean` and an honest partial. Read first: `research/P21/REPORT_505.md`, `research/P21/ATTEMPTS_B2.md` (Residual A, Residual B, and the intended final constants), `research/P21/P6_SPLIT.md`, `collaboration/briefs/COMMON_PHASE5.md` (ground rules: commit after every closed lemma; `python3 experiments/audit_article_axioms.py --build --output-dir tmp/article-audit --workers 2` before `make check`; `check_contracts.py` has no `--base-ref`, use `--summary`), and the ℝ³ mirror `Section4/A04/EnstrophyInequality.lean` (lane 504, on this branch), especially `convection_bound_of_norm_bridges`, `weightedEnergyIdentity`, `abs_pairing_carrier_le`, `enstrophy_differential_of_norm_bridges`, `enstrophy_differential`, `enstrophy_differential_on_Icc` — mirror their shapes with a `T` suffix.

Known tooling fact: `make check` currently fails ONLY in `test_recoloring_a_missing_clause_cannot_hide_whole_statement_partial` (owner test bound to `C35_FULL`, closed by lane 493). That is fixed on `erenup/core` by lane 509 (#465); do not merge core into this branch and do not touch the test — just note it in the report. Everything else in `make check` must pass.

## Goal: close the two residuals and deliver the final theorems
1. **Residual A (velocity L⁶, mean retained).** Prove `velocity_six_le_gradient_twoT : ∀ z, SmoothPeriodicT z → periodicLpENorm 6 z ≤ C_v * (periodicLpENorm 2 z + periodicLpENorm 2 (gradientTensor z))` (or the equivalent shape your `convection_interpolationT` consumes), with an explicit `C_v`. Two routes; take whichever closes first, record the other in ATTEMPTS:
   - (a) the route you already reduced to: the cutoff-product estimate `eLpNorm (gradientTensor (cutoffMul z)) 2 volume ≤ 343 * (3 * (ofReal cutoffGradBound * periodicLpENorm 2 z + periodicLpENorm 2 (gradientTensor z)))` via `T12.dirDeriv_cutoffMul_eq` (product rule), the triangle inequality for `eLpNorm`, and the 7³-cell covering of the cutoff support (each cell contributes one normalized cell norm; 343 = 7³).
   - (b) mean split on the normalized torus: `z = (z − meanT z) + meanT z`; the constant part has `periodicLpENorm 6 (const) = |meanT z| ≤ periodicLpENorm 2 z` (measure 1: Jensen/Cauchy–Schwarz), and the mean-zero part uses the T12 mean-zero embedding — check whether T12's `lem:critical-embeddings` torus half is stated for an arbitrary mean-zero smooth periodic field (then apply it to `meanZeroPartT z`) or only for gradients (then generalize its Fourier proof verbatim: it only uses that the zero mode vanishes).
2. **Residual B (physical RHS of the differentiated H¹ norm).** Prove the identity recorded in ATTEMPTS_B2 §Residual B: split the H¹ Fourier pairing into the L² and gradient parts (weight `1+|2πk|²`), Parseval each part into a physical pairing, use the periodic convection cancellation `periodicPairing ((z·∇)z) z = 0` for divergence-free smooth periodic `z` (T11 `EnergyIdentity` has the torus integration by parts), and the pressure cancellation `periodicPairing (∇p) z = 0` (already handled in T11's identity); then force Cauchy–Schwarz `|periodicPairing z b| ≤ ‖z‖₂‖b‖₂`. If the direct periodic integration-by-parts alternative recorded in ATTEMPTS is shorter, use it.
3. **Assembly.** `convection_boundT` (`|N| ≤ C * Y^(3/4) * L^(3/4)` with explicit `C`), `enstrophy_differentialT` (pointwise on `Ioo 0 T`) and `enstrophy_differential_on_IccT`, mirroring 504's `enstrophy_differential_on_Icc` exactly, for `w : ClassicalSolutionT ν a f T`, `hf : f ∈ forceClassT` (or `MemForceT` — use whatever `T10/T11` classical solutions carry; say which), with `Y t = (periodicSobolevENorm 1 (slice w.velocity t)).toReal ^ 2`, `Z t = (periodicSobolevENorm 2 (slice w.velocity t)).toReal ^ 2`, conclusion `deriv Y t + ν * Z t ≤ Cν * (1 + Y t) ^ 3 + Cν * lTwoSqT (slice f t)` on `Icc r s ⊂ (0,T)`, with `c = 1`, κ = 1 and the constants of ATTEMPTS §Intended final constant. Thread EXACTLY the three torus norm-bridge hypotheses recorded in `P6_SPLIT.md` (`hOne` on all interior times; `hTwo`, `hGradient` pointwise on the compact interval) as explicit hypotheses, as 504 does. Lane 503 proved the torus bridges in `Section3/T11/H1Bridges.lean` on branch `erenup/503-P21-B0-h1-norm-bridges` (`periodicSobolevENorm_one_toReal_sq_eq`, `periodicSobolevENorm_two_toReal_sq_eq`, energies `periodicL2Energy/periodicGradientEnergy/periodicHessianEnergy`) — do NOT import or restate them (B4 discharges the hypotheses); but state your hypotheses in terms of your own `lTwoSqT/gradientSqT/laplacianSqT` so that B4's reconciliation is a one-line conversion where possible, and record in P6_SPLIT.md the exact conversions B4 will need (e.g. `periodicHessianEnergy z = laplacianSqT z` is a Parseval identity for smooth periodic fields; state it as the obligation if you do not prove it).
4. Probes: extend `research/P21/probes/b2_closes.lean` (non-vacuity of the final theorem on an explicit classical solution, e.g. a trivial/steady one, plus one mutation that must fail: drop the dissipation term and check the inequality is no longer provable by the same proof). Update `research/P21/axioms_b2.lean` (all exported theorems standard-axioms-only), `ATTEMPTS_B2.md` (mark residuals closed, keep the failed paths), `P6_SPLIT.md` (B2 status: closed conditional only on the three torus bridges), and append a `## cont` section to `research/P21/REPORT_505.md` (four parts: what was proved / what is in Lean / gaps / commands + results).
5. Gates as in COMMON (audit rerun, `make check` modulo the known policy test, `make test`, `make test-mutations`, `make paper`, `git diff --check`). Commit after each closed lemma with `[505-P21-B2] …` messages; no push/merge/rebase. If a step does not close after a serious attempt, deliver the exact residual goal text as before — honest partial beats a fake closure.
