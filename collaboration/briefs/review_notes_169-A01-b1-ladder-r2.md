1. `exists_differentiable_datumPath` / `datumPath_hasDerivAt`: confirm the derivative is genuinely the order-`m` datum of the projected residual `νΔu + P(F − (u·∇)u)` computed from the Duhamel equation (route α), with `HasDerivAt` in the Hilbert space `RealVectorSobolev m` at every `t ∈ Ioo 0 S`, and that the path differentiated is the *same* continuous selection as lane 161's (or a path with the same defining property). Check the order bookkeeping `m + 2 ≤ q + 1` / `m ≤ q − 1` and why the top orders `m ∈ {q, q+1}` are excluded (order lowering injectivity `lowerDatumL_injective`).
2. The single `set_option maxHeartbeats 400000 in` at `DatumPathDeriv.lean:138` (`cylinderResidual_invariant`): confirm it is per-declaration, commented with a reason, and not masking a slow `decide`/`simp` that a `rw` would fix; try the proof at the default 200000 once and report.
3. `residualDatum_is_timeDerivative` is "conditional": state its exact hypothesis and confirm it is honest (it is the R3 bridge, not a placeholder).
4. Non-vacuity: does the axioms file instantiate the theorems on the zero pair `u := 0, U := 0` with a *derivable* residual (`R = 0`)?
