(Second review, after the rework commit `c78a39c`; the first review's REJECT was about the direction of row (v) and is resolved by design — judge the reworked module.)
1. `forcePath_of_memForceR` packages `(C01.forcePath hf, jet continuity, MemL1Hm f)`; check it is exactly the `(F, hF)` shape that `HasAprioriBound` (`Section4/A01/Horizon.lean:106`) and `localTheory_on_prescribed_horizon` (`:137`) consume, so a constructor can take the paper's `f` and feed the local theory without any invented force. Is the `∃ F, F = C01.forcePath hf ∧ …` form useful or should it be the unpacked triple? (note only)
2. `forceOfPath_forcePath_eq_on_horizon`: pointwise on `Icc 0 S`; confirm no a.e. weakening is hidden and that `forceOfPath` outside the horizon is documented as arbitrary/zero and not claimed in `F_R`.
3. `initialClassR_of_smoothL2` now takes lane 162's cylinder hypotheses (`hu`, `hU`, `hdiv`) and `a.field =ᵐ ⇑U`: confirm these are exactly what `localTheory_on_prescribed_horizon`'s output provides at `t = 0` (`U 0 = a.toLp`, divergence-free clause), so the constructor's `a' := a.field` lands in `initialClassR` with no extra hypothesis; and that `MemHInfty a.field` is obtained honestly (all-order data from `SmoothL2Field`).
4. Non-vacuity: instantiate `forcePath_of_memForceR` on `f := 0` (`memForceR_zero`) and, if cheap, a nonzero `f`.
