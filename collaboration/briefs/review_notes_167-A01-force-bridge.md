1. `ForcePathSmoothness F` requires `ContDiffOn ℝ ∞ (forceOfPath F) futureDomain` where `forceOfPath` is the **zero extension** beyond `S`. For a generic path `F` this is FALSE at `t = S` (a jump), so the hypothesis may be unsatisfiable exactly where the constructor needs it. Test it: try to prove `ForcePathSmoothness F` for `F := forcePath f` (lane 146, `Section4/C01/JetPaths.lean`) restricted from a global `f` with `MemForceR f` that is not flat at `S`; report whether it is provable, provably false, or open. If it is unsatisfiable in general, say so plainly in the verdict.
2. Direction check: in the actual A01 usage (`LocalTheoryAPI.solution`, `HasAprioriBound`), the path `F` comes from a global paper force `f ∈ F_R` via `C01.forcePath f`. Is the right row-(v) statement then `forceOfPath (forcePath f) = f` on `[0,S]` (so `f' := f`, no invented force), making `memForceR_forceOfPath` unnecessary for the constructor? State which theorem the constructor should consume and whether this lane provides it.
3. `initialClassR_of_smoothL2` takes pointwise divergence `= 0`; is that what the cylinder pair provides (a.e./datum-level `divergenceFreeSpace`), or is a bridge missing (see lane 162 `ConstructorDivergence.lean` in worktree `.claude/worktrees/162-A01-c6-divergence`, read-only)?
