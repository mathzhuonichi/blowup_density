# Lane 497-T24-P5a-domain-components-sum — Partial P5 units P5.1 + P5.2: components on Ω and the finite-sum `ClassicalSolutionOmega`

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/497-T24-P5a-domain-components-sum` (branch `erenup/497-T24-P5a-domain-components-sum`, = `origin/erenup/core` with lane 496 merged). Read `collaboration/briefs/COMMON_PHASE5.md` first (ground rules; commit after each closed unit; thread cross-lane facts as explicit hypotheses — the assembly lane 501 discharges them; new modules must be listed in `formalization/blueprint/entrypoints.json` `proof_modules` or `make check` fails with "Unreferenced Lean modules"). Then `PLAN.md` §1 row P5, `research/T24/T24_SPLIT.md` section "P5 units" (lane 496; exact target fields and suppliers per unit), `research/T24/COMPARISON_OMEGA.md`, `research/T24/SpecOmega.lean`, the canonical record `Section3/T24/MultipleOmega.lean` (`MultipleRegionsOmegaAPI`, `energyEssSupOmega`, `energyGradientOmega`, `multipleRegionsOmegaStatement`), the torus proofs to mirror `Section3/T24/{MultipleComponents,MultipleAssembled,MultipleRegions,MultipleAssembly}.lean`, the owner's domain layer `Section3/T23/{DomainSolution,Placement,Triple,Solution,NoSlipEnergy,BoundaryIntegration}.lean`, the un-periodised scaled packet `Section4/I03/*` (`scaledVelocity`, `scaledPressure`, `scaledForce`, `scaledEquation`, `scaledDivergenceFree`, `scaledBlowup`, energy identities) and `Source/PacketScaling`, and the article `paper/revised/sections/03-torus.tex` `prop:multiple` (revised lines 511–535).

## Goal
New modules `Section3/T24/MultipleOmegaComponents.lean` (P5.1) and `Section3/T24/MultipleOmegaAssembled.lean` (P5.2), namespace `NSFormalization.Section3.T24`, over a data carrier `RegionsOmegaData` mirroring `RegionsData` (raw packet + clauses, `Ω` with `IsBoundedBoxOrSmoothDomain Ω`, `T`, `N`, regions with `closure (ball c_j ρ_j) ⊆ Ω`, pairwise disjoint):
- **P5.1**: per-region `domainPlacementData` at the prescribed ball, `ε j` with `eps_admissible`/`eps_time`, the components `component j : ClassicalSolutionOmega ν Ω 0 (scaledForce …) T` built from the un-periodised scaled packet at start time `T − ε_j²` (fields: closed-slab smoothness via the T23 open-neighbourhood convention, `initial`, `divergence`, `momentum` from I03 `scaledEquation`/`scaledDivergenceFree` restricted to `Ω`, `no_slip` because the support lies in `B_j ⊂ Ω`, `pressure_gauge` via `domainNormalizePressure`), `component_pin`, `component_support`, `component_force_support` — each with the exact field statement of `MultipleOmega.lean`.
- **P5.2**: `assembledVelocity/Pressure/Force` (finite sums), the three `*_formula` (rfl), `solution : ClassicalSolutionOmega ν Ω 0 assembledForce T` (cross transports vanish by disjoint supports — mirror `MultipleAssembled.lean: crossTransport_eq_zero`; smoothness via `SmoothOnClosedSlab.add`; pressure gauge of a finite sum of gauged pressures), `solution_pin`, `force_mem` (`forceClassOmega` closed under finite sums — `MemForceOmega.add`), `rest`, `no_slip`.
Deliverables: modules (listed in `entrypoints.json`), `research/T24/probes/p5a_closes.lean` (each target field by `exact`), `research/T24/axioms_p5a.lean`, `research/T24/ATTEMPTS_P5A.md`, P5.1/P5.2 status lines in `T24_SPLIT.md`. Gates: builds, `lake env lean` zero output, probe, axioms, `make check`. Report `research/T24/REPORT_497.md` (four parts).
