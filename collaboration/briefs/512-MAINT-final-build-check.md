# Lane 512-MAINT-final-build-check — full compile + consistency check of the final Phase 5 `erenup/core` (all six Partials closed) before the delivery PR to `main`

You are a Lean 4 (v4.34.0-rc2 + Mathlib) build-verification worker in the worktree `/data_8T/ping/blowup_density/.claude/worktrees/512-MAINT-final-build-check` (branch `erenup/512-MAINT-final-build-check` = `origin/erenup/core` @ the merge of #473, after lanes 492–511 were merged: 36 registered contracts, proof graph 41/41 Closed, 27/27 article entries Closed). NOTE: after the last merges two comment-level edits landed on top of the tree lane 511 audited (`Section4/A04/H1Bridges.lean` and `Section3/T11/H1Bridges.lean` header citations; `Section3/T11/H1Restart.lean:244` `<;>` → `;`), so `AXIOM_AUDIT.json` may be flagged stale by `make check` ("Source changed: rerun the article axiom audit") — that is expected: rerun the audit, confirm 72 declarations / 27 entries / zero forbidden axioms, commit the regenerated file separately, and say so in the report.. Read `README.md`, `experiments/README.md`, `verification/README.md`, `formalization/blueprint/README.md`, `collaboration/briefs/COMMON_PHASE5.md`, and the previous report `logs/MAIN_BUILD_20260920.md` (lane 491; reproduce its structure).

## Ground rules
- Work ONLY inside this worktree; never `git push`, merge or rebase; committing on your branch is allowed. `. scripts/lean-env.sh`; `lake` only from `verification/` with `LEAN_NUM_THREADS=6`. **Do not fix Lean**: if a module fails, record the exact error text; do not edit `.lean` files or `lakefile.toml`. The only tracked edits allowed: the report, and regenerated artifacts if step 4 shows them stale (`AXIOM_AUDIT.json`, `DEPENDENCY_GRAPH.md`, `CLOSURE_AUDIT.md`, PDFs) — commit those separately and say so.
- Facts only: exit codes, counts, wall time, exact error/warning lines.

## Deliverable — `logs/FINAL_BUILD_20260921.md`
1. Environment table (`git rev-parse HEAD`, `origin/main`, toolchain, Mathlib rev from `verification/lake-manifest.json`, threads).
2. Module inventory per top directory (`find formalization/NSFormalization -name '*.lean' | wc -l` and per `Section3/T*`, `Section4/*`, …), HeliCorgi roots in `formalization/lakefile.toml`, and the list of modules added in Phase 5 (`git diff --name-only origin/main..HEAD -- formalization verification | grep '\.lean$'`).
3. **Full rebuild in one `lake build`**: delete `formalization/.lake/build/lib/lean/NSFormalization/` and `…/ir/NSFormalization/` (only those), then `cd verification && time LEAN_NUM_THREADS=6 lake build <every NSFormalization module>` → `tmp/final_build.log`; record exit code, wall time, `grep -c 'error'`, Built/Replayed counts, every warning line from `NSFormalization/` (list them; the Route B modules should be warning-free after lanes 510/511's cleanups), `.olean` count. Then `lake build` of every `Bindings.*` and `Tests.*` module (list them from `verification/`), same records.
4. Owner gates on the final tree, each with exit code and the key summary lines: `make check`, `make test`, `make test-mutations`, `make paper`, `python3 experiments/check_reader_documents.py`, `python3 experiments/check_formalization_plan.py` (must not change `DEPENDENCY_GRAPH.md`; if it does, that is a stale artifact — commit the regeneration separately), `python3 experiments/audit_article_axioms.py --build --output-dir tmp/article-audit --workers 2` (must leave `AXIOM_AUDIT.json` unchanged; same rule), `git diff --check`, `python3 experiments/check_contracts.py --summary` (36 contracts).
5. Contract closure table: for all 36 registry entries, the test module, the `checked…` declaration and its axiom set (from the `make test` output), plus a note for the seven entries added in Phase 5 (`T02.correction_v2`, `T03.periodic_insertion_v2`, `T03.force_amplitude`, `T04.conservative_forcing_v2`, `T04.multiple_regions_v2`, `A04.continuation_v3`, `T01.torus_local_theory_v2`).
6. Delivery-diff sanity: `git diff --stat origin/main..HEAD -- formalization verification paper experiments README.md output Makefile .github` (this is what the delivery PR will carry) and `git diff --name-only origin/main..HEAD | grep -vE '^(formalization|verification|paper|experiments|README.md|output|Makefile|.github)'` (the process layer that will NOT be delivered) — just list them.
7. Verdict paragraph: is the tree ready for a single PR to `main`? List anything the owner should know (e.g. contract V3/V2 additions, guide text additions, README counts).
Commit the report on your branch (`[512-MAINT] …`); no push.
