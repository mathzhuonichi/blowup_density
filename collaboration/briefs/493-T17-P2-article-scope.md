# Lane 493-T17-P2-article-scope — close Partial P2: Lemma 3.5 in the article's own formulation (graph node `C35_FULL`)

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/493-T17-P2-article-scope` (branch `erenup/493-T17-P2-article-scope`, = `origin/erenup/core`). Read `collaboration/briefs/COMMON_PHASE5.md` first, then `PLAN.md` §1 row P2, `proof_graph.json` node `C35_FULL` (Partial: "The unrestricted article formulation has not been exported with all differences in hypotheses discharged"; `completion_from: ["C35_T"]`), the guide row for `lem:correction` ("The formal statement includes explicit smoothness, chart and positive-viscosity hypotheses. These are supplied in the downstream gluing theorem"), the article `paper/revised/sections/03-torus.tex` Lemma `lem:correction` and Lemma `lem:potential` (the objects `w_ε`, `H_ε` and every displayed bound: eq:derivativebounds, eq:wE, eq:Hmixed, eq:Hs…), and the code: `Section3/T17/Correction.lean` (`CorrectionAPI`, 45 fields; note `reference_periodic : IsPeriodicOn univ v` is a field), `Section3/T17/Assembly.lean` (`correctionStatementAmended`, `correctionAPI_of_smooth`, `correctionStatementAmended_holds`), `Section3/T17/SlabBridge2.lean` (`correctionStatementSlab'` — hypotheses `IsPeriodicOn univ v` + `ContDiffOn ℝ ∞ v (Ioo 0 (place.T+δ) ×ˢ univ)` — and `correctionStatementSlab'_holds`, the window-agreement lemmas), `Section3/T19/Threading.lean` (`extendByZero`, `extendByZero_velocity_eqOn`, `extendByZero_periodic`, `extendByZero_smooth`, and how `insertionData` calls the slab bridge), `research/T17/SPEC_ISSUES.md` (G4, G5), `verification/Contracts/V1/Correction3.lean` + `Bindings/Correction3.lean` (registered `correctionStatementAmended`, fieldwise conversions), the V2 pattern files named in COMMON.

## Goal
1. `Section3/T17/ArticleScope.lean`: `def correctionStatementArticle : Prop` whose hypotheses are exactly the article's: `ν > 0`; a periodic classical solution `reference : ClassicalSolutionT ν a g (T + δ)` with `δ > 0`, `a ∈ initialClassT`, `g ∈ forceClassT`; the placement `place : PlacementData u p f K` of the building block (the coordinate ball) with the ball radius `r` of Lemma 3.4 (`0 < r`, `r < 1/2`, `ball place.x₀ r ⊆ ball place.chartCenter place.chartRadius`) and the raw packet support clause `∀ t ∈ Ioo 0 1, tsupport (u (t,·)) ⊆ K` (a property of the registered building block; if it can be derived from `place`/the packet record instead of assumed, do so); conclusion: `∃ D : CutoffData, LocalPotentialAPI (extendByZero reference).velocity u place.Kstar place.x₀ r place.T δ D ∧ Nonempty (CorrectionAPI ν place (extendByZero reference).velocity r δ D)` **together with** the identification `∀ t ∈ Ico 0 (T+δ), ∀ x, (extendByZero reference).velocity (t,x) = reference.velocity (t,x)` — so that every field that reads `v` on the cut-off window (all of them; check `correction_support`/`eps_time`) is a statement about the article's `v`. If the record's `reference_periodic` field is the only obstacle to stating the conclusion directly for `reference.velocity`, say so in the docstring and keep the zero extension (G5); do not weaken any bound. Prove it from `correctionStatementSlab'_holds` + `extendByZero_*`.
2. Also export the article's displayed bounds as named theorems over `reference.velocity` where they are pointwise on the window (e.g. the derivative bounds of `w_ε`, `H_ε`, the support volume/length, eq:wE, eq:Hmixed, eq:Hs), each by projecting the record field and rewriting with the identification — so a reader finds Lemma 3.5 (a)–(d) by name.
3. Contract `T02.correction_v2` (`Contracts/V2/Correction3.lean` defining `correctionStatementV2` = the article-scope statement over registered vocabulary (`ClassicalSolutionT`, registered `PlacementData`/`CutoffData`/`CorrectionAPI` from `Contracts/V1/Correction3.lean`), `Bindings/Correction3V2.lean` with the fieldwise conversions as in `Bindings/Correction3.lean`, `Tests/Correction3V2.lean`), registry entry (scope: "Lemma 3.5 with the article's hypotheses only: a periodic classical reference on [0,T+δ], the building-block placement and the Lemma 3.4 ball; the reference is zero-extended outside [0,T+δ) and identified with the article's v there").
4. Blueprint per COMMON §3: node `C35_FULL` → Closed, `depends_on: ["C35_T"]`; `RESULT_MAP.md` Lemma 3.5 → Closed; guide row `lem:correction` → Closed; counts; regenerate; gates; `make paper`.
Report `research/T17/REPORT_493.md`.
