# Lane 507-P21-B4-restart-r3 — P6 Route B unit B4 on ℝ³: the uniform H¹ restart theorem `h1RestartR` from B0 + B1 + B3 and the existing maximal/continuation theory

Worktree `/data_8T/ping/blowup_density/.claude/worktrees/507-P21-B4-restart-r3` (branch `erenup/507-P21-B4-restart-r3` = `origin/erenup/core` + lanes 503 (B0), 504 (B1), 506 (B3), all three currently under review). Read `collaboration/briefs/COMMON_PHASE5.md` (ground rules: commit after every closed lemma; new modules go into `formalization/blueprint/entrypoints.json` `proof_modules`; rerun `python3 experiments/audit_article_axioms.py --build --output-dir tmp/article-audit --workers 2` before `make check`; `check_contracts.py` has no `--base-ref`, use `--summary`), then `research/P21/ASSESSMENT.md` (§ Registered-vocabulary H¹ restart targets, § Route B, § Principal risks), `research/P21/P6_SPLIT.md` (B1 and B3 handoffs), `research/P21/REPORT_503.md`, `REPORT_504.md`, `REPORT_506.md`, and `research/P21/Targets.lean` (`h1RestartR` is the exact Prop to prove). Do NOT edit the modules of lanes 503/504/506 (`Section4/A04/H1Bridges.lean`, `EnstrophyInequality.lean`, `EnstrophyBarrier.lean`, `Section3/T11/H1Bridges.lean`): they are under review; anything you need beyond them goes into your own module. The lead merges their reviews into core.

## Target (unit B4, ℝ³ only; torus is lane 508 later)
New module `Section4/A04/H1Restart.lean` whose main theorem is literally `h1RestartR` of `research/P21/Targets.lean` (restate the Prop in the module, keep the shape):
```
∀ ν, 0 < ν → ∀ f, MemForceR f → ∀ S, 0 ≤ S → ∀ K : ℝ≥0∞, K ≠ ⊤ → ∃ δ > 0,
  ∀ t₀ ∈ Icc 0 S, ∀ a' ∈ initialClassR, sobolevENorm 1 a' ≤ K →
    ∃ w : ClassicalSolutionR ν a' (timeShift t₀ f) δ, ManuscriptLocalRegularity ν a' (timeShift t₀ f) δ w
```
(One δ before restart time and datum. Use the A02/A04 local names; the V2 contract names are `rfl`-bridged.)

## Proof route (each step = its own committed lemma; peel with probes)
1. **Reconciliation B0 ↔ B1.** Discharge the three hypotheses of `enstrophy_differential_on_Icc` (lane 504) for velocity slices of a classical solution, exporting `enstrophy_differential_on_Icc'` with no bridge hypotheses (inputs only `w : ClassicalSolutionR ν a f T`, `MemForceR f`, `0 < ν`, `0 < r`, `s < T`). Known obstacles, in decreasing order of expected difficulty:
   - Lane 503's identities `sobolevENorm_one_toReal_sq_eq` / `sobolevENorm_two_toReal_sq_eq` assume `HasCompactSupport z`; solution slices are only `MemHInfty`-type fields (`ClassicalSolutionR.sobolev`: a continuous `IsSobolevDatum m` path for every m; `ManuscriptLocalRegularity.sobolev_smooth` even a smooth one). You need the same identities for such fields. Options: (a) prove the Fourier-energy identity directly from `IsSobolevDatum` (D01 `SmoothDatum.lean:237`: the datum is characterized by pairing against Schwartz test functions; the datum of `∂ⱼz` is the multiplier `2πiξⱼ` applied to the datum of `z`, then the weighted norm splits by Parseval) — inspect how 503 proved `sobolevENorm_toReal_sq_eq_angular` and `angularSobolevSq_one_eq_physical` and whether the compact support is only used to build `compactSchwartzComponents`; (b) approximate a `MemHInfty` field by compactly supported ones in the H² norm and pass both sides to the limit; (c) look for existing D01/A01/A05 material (`A05/SmoothJets.lean` `SmoothL2`, A01 smooth datum representation, `research/D01/RECONCILIATION.md`) that already relates `sobolevENorm` to physical derivative energies without compact support. Put the generalized bridges in your module (or a new `Section4/A04/H1BridgesSmooth.lean`), not in 503's file.
   - Convert 503's energies to 504's carriers: `l2EnergyR z = l2Sq z`, `gradientEnergyR z = κ * gradientSq z` (κ = (2π)⁻², the `2π` derivative convention), and for `hTwo` you need `hessianEnergyR z ≤ κ² * laplacianSq z` — equality holds (Parseval: Σᵢⱼ|ξᵢξⱼ|² = |ξ|⁴, i.e. ‖∇²z‖₂ = ‖Δz‖₂ for these fields); prove whichever direction/identity 504's statement needs. `hGradient` (`eLpNorm (gradTensor z) 2 ≤ ofReal (sqrt (gradientSq z))`) should be near-definitional; check A05's `gradTensor` against 504's `gradientSq`.
   - If a conversion turns out to need a different κ convention, 504 exported `enstrophy_differential_of_norm_bridges` for any `0 < κ ≤ 1`.
2. **Time regularity adapters for B3.** For a classical solution on `[0,T)` (force `MemForceR`): `Y t := (sobolevENorm 1 (slice t)).toReal ^ 2` is continuous on `Ico 0 T` (from `ClassicalSolutionR.sobolev` at m = 1 and the norm/datum bridge `sobolevENorm s z = ‖A‖` for `IsSobolevDatum s z A` — find it in D01, the datum is unique), differentiable on `Ioo 0 T` (504's `inhomogeneousEnergyIdentity` / `weightedEnergyIdentity` give the derivative), `Y 0 = (sobolevENorm 1 a').toReal ^ 2 ≤ K.toReal ^ 2`; `Z t := (sobolevENorm 2 (slice t)).toReal ^ 2` is continuous on `Ico 0 T`, hence measurable and `IntegrableOn Z (Icc r s)` for `0 ≤ r ≤ s < T`, and `sobolevENorm 2 (slice t) ≠ ⊤`.
3. **Force cap.** From 503: `F := (forceL2CapR f S).toReal`, `forceL2CapR_ne_top`, and `timeShift_force_slice_le_forceL2CapR` give `l2Sq (slice (timeShift t₀ f) t) ≤ F` for `t₀ ∈ Icc 0 S`, `t ∈ Icc 0 1` (convert 503's `eLpNorm` form to 504's `l2Sq`). Note `restart_force` (A04 `RestartWiring.lean:37`): `MemForceR (timeShift t₀ f)` for `t₀ ≥ 0`.
4. **Barrier.** Apply 506's `enstrophy_uniform_barrier_and_dissipation` with `c := ν`, `C := Cν` (504's constant), `K := K.toReal ^ 2`, `F` as above; it returns `d > 0`, `M` depending only on (ν, K, F). Set `δ' := min d 1` (stay inside the force window `[0,1]`) and `δ := δ' / 2` (or `δ'` itself if the strict lifespan inequality lets you restrict; keep δ explicit).
5. **Maximal-lifespan contradiction.** Fix `t₀ ∈ Icc 0 S`, `a' ∈ initialClassR` with `sobolevENorm 1 a' ≤ K`, `f' := timeShift t₀ f`. Let `T* := maximalLifespanR ν a' f'`. Show `ENNReal.ofReal δ' < T*`: if `T* ≤ ofReal δ'` (so `T*` finite; `0 < T*` by the A01 local carrier: `A01.localCarrier ν a' f' hν ha' (restart_force f hf t₀ ht₀.1)` and `horizon_le_lifespan`), take the maximal solution `(u,p)` (`A02.exists_maximal_of_localSolution` instantiated with `A01.localHorizon'`/`localCarrier` — an instantiated version probably already exists in `A02/MaximalWiring.lean` or `A04/RestartWiring.lean`, e.g. via `IsMaximalSolution`), `S' := T*.toReal ∈ (0, δ']`, `SolvesBelow ν a' f' S' u p` from `IsMaximalSolution`. For every `s < S'` the barrier on `[0,s]` (Y continuous on `Icc 0 s`, differentiable on `Ioo 0 s`, inequality from step 1 on `Icc r s` for every `0 < r`, hence on `Ioo 0 s`) bounds `∫₀ˢ Z ≤ B` uniformly; 506's `enstrophy_endpoint_integral` then gives `IntegrableOn Z (Icc 0 S')` and `∫₀^{S'} Z ≤ B`; convert to `squaredHTwoIntegral S' u ≠ ⊤` (`A04/Continuation.lean:22`: `∫⁻ t in Ioo 0 S', sobolevENorm 2 (slice t) ^ 2`; use `sobolevENorm ≠ ⊤` on slices and `lintegral = ofReal ∫` for integrable nonnegative functions). Then `extendsBeyond_of_memForceR' ν a' f' hν ha' hf' S' hS' u p hu hfin : ofReal S' < T*`, contradicting `T* = ofReal S'`. Do not target `A01.localHorizon'` (ASSESSMENT warns it cannot be lower-bounded in H¹); the existential/maximal route above is the intended one.
6. **Conclusion.** From `ofReal δ < T*` obtain `w : ClassicalSolutionR ν a' f' δ` (`exists_horizon_gt_of_lt_lifespan` + `restrict`, as in `exists_maximal_of_localSolution`). Regularity bundle: find the theorem giving `ManuscriptLocalRegularity` for an arbitrary classical solution with `initialClassR` datum and `MemForceR` force — candidates: `A04/RestartFixedForce.lean:146 classical_hasSmoothSobolevPath` (the `sobolev_smooth` field), the pressure clause via A04's pressure adapters, or the registered `localTheoryV2` binding (`verification/Bindings/LocalTheoryV2.lean:93`, see which fields the V2 API already proves for all classical solutions). If only the selected carrier's version exists, derive it by uniqueness (`A02/Uniqueness.lean:120 velocity_unique`) on overlapping windows plus restarts (regularity is local in time). Record exactly what you used.
7. **Handoff for B5 (do if time permits, otherwise state precisely):** the `restartAt` shape consumed by `A04/Continuation.lean:45 restartBeyond_of_restartAt` and `RestartFixedForce.lean:276 restartBeyond_fixed` (which is the H⁷ analogue of the endpoint target `h1UniformEndpointR`): `∀ t₀ ∈ Ico 0 S, sobolevENorm 1 (u(t₀)) ≤ K → ofReal (t₀ + δ) ≤ maximalLifespanR ν a f`, via `w.restart_datum` and the proved `ShiftedLocalExtension` (`shiftedLocalExtension` in A04). Write the B5 handoff in `research/P21/P6_SPLIT.md`.

## Deliverables and gates
- Module (in `entrypoints.json`), `research/P21/probes/b4_closes.lean` (non-vacuity: instantiate `h1RestartR` pieces on an explicit case, e.g. zero force and a concrete datum; plus one must-fail mutation: drop the H¹ bound hypothesis or the dissipation and show the same proof no longer typechecks), `research/P21/axioms_b4.lean` (every exported theorem exactly `[propext, Classical.choice, Quot.sound]`), `research/P21/ATTEMPTS_B4.md` (all routes tried, exact residual goals for anything not closed), `research/P21/P6_SPLIT.md` (B4 status + B5 handoff), `research/P21/REPORT_507.md` (four parts: theorem proved / what is in Lean / gaps / commands + results).
- No contract, blueprint-status or `paper/` change in this lane (B5 registers). Gates per COMMON: audit rerun, `make check`, `make test`, `make test-mutations`, `make paper`, `git diff --check`. Commit after each closed lemma with `[507-P21-B4] …`; no push/merge/rebase. If the compact-support reconciliation (step 1) does not close after a serious attempt, deliver everything else with that single bridge threaded as an explicit hypothesis of the exact statement you need, and say so in the report — an honest conditional theorem with one named analytic hypothesis beats a fake closure.
