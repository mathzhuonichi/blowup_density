1. **证了哪个定理。** 已证齐次 Fourier 缩放、角频率转换、真实齐次向量 datum 的空间/时间缩放，以及实际 correction force 的统一齐次分量界。注册范数上的 `packetHomogeneousIdentity` 给出精确等式 `‖F_ε‖ = ε^β ‖F‖`；`homogeneousScaling` 装配现有 `HomogeneousScalingAPI` 的两个字段，指数分别为 `β = 2/q − 3/2 − s`、`β+1`，在 `q=2,s=-1` 为 `1/2`、`3/2`。注册范数结果条件于下述唯一分析输入。

2. **Lean 里现在有什么。** 新增 `formalization/NSFormalization/Section4/I03/HomogeneousScaling.lean` 与 `verification/Bindings/ScalingHomogeneous.lean`，共 36 个具名声明，命名空间均为 `NSFormalization.Section4.I03`。后者必须放 Bindings，因为 Contracts 是 NSFormalization 的下游。packet 的 API 常数就是原始力的有限注册齐次时间范数的 `.toReal`；correction 常数为 `(2π)^s * K.toReal`，`K` 是已有统一紧支撑 profile 时间界的三个分量之和。两个字段使用完全相同的 `Bindings.scaling C th` family 与缩小后的阈值；没有注册新合同或修改既有 Lean 模块。审计包含零力注册范数、实际 PacketAPI 的齐次 slice path，以及独立 `packet_homogeneous_example.lean` 中 `Bindings.packet 1` 的有限 profile 常数。原始两个 spec 字段逐 token 抄录于 `ATTEMPTS_HOMOGENEOUS.md`。

3. **缺口是什么。** 唯一具名假设 `CompactHomogeneousRealization` 精确要求：对 `-3/2<s<0` 的每个光滑紧支撑力，D01 的具体 `compactHomogeneousPath` 在 `Data.forceTimeMeasure` 下 a.e. 强可测。slice pairing、角频率范数比较、空间缩放、标量范数时间可测性均已证明；不能把最后一项冒充向量路径可测性。零力满足此假设的实例已证明，非零 packet 的路径可测性仍未证明。故两个齐次字段仍未无条件注册，Prop. 4.6 / Thm 4.7 的相关应用仍条件于此输入；没有使用负阶 Bessel→齐次的错误收缩论证。

4. **跑了什么命令、结果。** 每次 Lean 命令先 `. scripts/lean-env.sh`，从 `verification/` 运行并设置 `LEAN_NUM_THREADS=6`。`lake build NSFormalization.Section4.I03.HomogeneousScaling`、`lake build Bindings.ScalingHomogeneous` 成功；两份新模块的 `lake env lean` 输出均为 0 字节。`lake env lean ../research/I03/axioms_homogeneous_scaling.lean` 成功，36 个声明的公理列表均恰好是 `[propext, Classical.choice, Quot.sound]`，非空洞 examples 与独立具体 packet probe 通过（既有 Packet/Scaling bindings 有重名，不能在同一环境导入）。`make check`、`make test`、`make test-mutations` 通过；没有禁止证明占位符或 heartbeat 覆盖。构建/测试会回放既有依赖的 linter 信息，新模块无警告。额外 `check_contracts.py --base-ref origin/erenup/integration` 失败：共享 remote ref 已前进至本 lane 分叉之后，检查器报告本分支从未含有的 `Contracts/V1/MainThresholds.lean` 被删除；未 merge/rebase。改用实际分叉提交 `028b15efd6527f614ae036231f8381098199e875` 的兼容性检查通过。`git diff --check` 通过。完整命令输出保存在本 worktree 的 `tmp/250-*.log`（不提交日志）。
