# T17 spec / assembly issues (lead log)

- **2026-09-18 10:30Z (lane 370, G1)**: Paper1's `profile_smooth` / `profile_uniform_global_derivative_bound` need **global** `ContDiff ℝ ∞ v`; the reconciled `CorrectionAPI` (`research/T17/Spec.lean`) carries `reference_periodic` only, and T16's `LocalPotentialAPI` gives `v` smooth on the chart cylinder only. Options for U12 (assembly): (a) add `reference_smooth : ContDiff ℝ ∞ v` to `CorrectionAPI` (the reference in `thm:insertion` is a classical solution, smooth on `Ico 0 (T+δ) ×ˢ univ`, so a time-cutoff extension is globally smooth) — a spec amendment to be recorded in RECONCILIATION §3; or (b) re-prove the profile fields through a chart truncation of `v` (`Section3/T16/BallPotential.lean`'s bump argument) so local smoothness suffices. Lead preference: (b) if cheap at assembly, else (a) with the docstring note. Also G3: `PlacementData` cannot be imported into `formalization/` (needs `Contracts.V1.PacketAPI`); canonical units use bare `(x₀, T)`, assembly instantiates `place.x₀`, `place.T`.

## G4 addendum — accepted counterexample and completed block (lane 453 continuation)

The user accepted the geometry counterexample and finalized the amended raw-field
statement with **both** the raw packet support clause
`∀ t ∈ Ioo (0 : ℝ) 1, tsupport (fun x : Space ↦ u (t,x)) ⊆ K`
and `Metric.ball place.x₀ r ⊆ Metric.ball place.chartCenter place.chartRadius`.
The other premises are exactly `0 < ν`, `0 < r`, `r < 1/2`, `0 < δ`,
`IsPeriodicOn univ v`, `ContDiff ℝ ∞ v`, and vanishing spatial divergence on
`Ioo 0 (place.T + δ) ×ˢ Metric.ball place.x₀ r`.

The former intermediate G4 is disproved in the standalone research probe
`research/T17/probes/assembly_geometry_obstruction_module.lean`, including its
inline axiom audit. It is no longer a canonical module. The completed block is
proved in `Section3/T17/Assembly.lean:correctionStatementAmended_holds` and
registered as `T02.correction`. No further structural fact is assumed:
`place.Kstar_compact`, `place.time_pos`, closure monotonicity followed by
`place.chartBall_in_cube`, T16's `theta_radius_pos`, and
`ε₀ ≤ place.ε₀ ≤ 1` discharge all remaining premises. The 45 fields are unchanged.
- **2026-09-19 12:45Z (lead, G4 — `correctionStatement` hypotheses)**: `research/T17/Spec.lean:980` and the canonical `Section3/T17/Correction.lean:294` quantify `correctionStatement` over an arbitrary `v : SpaceTimeField`, `ν`, `r`, `δ` with **no hypotheses**, while `CorrectionAPI` carries the fields `reference_periodic : IsPeriodicOn univ v` and `viscosity_pos : 0 < ν` (and `potential : LocalPotentialAPI …`, whose registered statement `T02.local_potential` (`Contracts/V1/LocalPotential.lean:297-305`) needs `0 < r`, `r < 1/2`, `0 < T`, `0 < δ`, `IsCompact K`, periodicity, chart-cylinder smoothness and divergence-freeness of `v`, and the packet support clause). As written the statement is therefore false for non-periodic `v` or `ν ≤ 0`. **Ruling:** the assembly lane (U12, lane 453) states and registers `correctionStatement` with exactly the T16 hypothesis block **plus** `0 < ν` **plus** the global `ContDiff ℝ ∞ v` (G1 resolved by option (a) *at the statement level*: `reference_smooth` becomes a hypothesis of the existential statement, the `CorrectionAPI` field list stays as reconciled). In `thm:insertion` the reference is a classical solution, smooth on `Ico 0 (T+δ) ×ˢ univ`, so T18 U12 discharges it after a time-cutoff extension. Recorded for the owner as a V1 spec amendment (RECONCILIATION §3); the registry `scope` must spell out the hypothesis block.
- **2026-09-19 12:53Z (lead, G4 addendum after lane 453's counterexample)**: the first amended block was still insufficient — lane 453 (astra) proved in Lean that with chart radius `1/8`, `r = 1/4` and a nonzero constant reference all the listed hypotheses hold while the field `ball_in_chart : ball place.x₀ r ⊆ ball place.chartCenter place.chartRadius` fails (`research/T17/REPORT_453.md`, probe `assembly_geometry_obstruction.lean`). **Final ruling — `correctionStatementAmended` hypotheses:** `0 < ν`, `0 < r`, `r < 1/2`, `0 < δ`, `IsPeriodicOn univ v`, `ContDiff ℝ ∞ v` (G1 (a)), `∀ t ∈ Ioo 0 (place.T + δ), ∀ x ∈ ball place.x₀ r, spatialDivergence v t x = 0`, the raw packet support clause `∀ t ∈ Ioo 0 1, tsupport (fun x ↦ u (t, x)) ⊆ K`, and `ball place.x₀ r ⊆ ball place.chartCenter place.chartRadius`. Everything else structural (`Kstar_compact`, `0 < place.T`, `hcube`, `theta_radius_pos`, `eps_le_one`) is derivable from the placement/T16 records. The unamended `correctionStatement` stays in the contract as documentation only.

## G5 — the registered T16/T17 hypothesis blocks are not satisfiable by a classical reference (lead, 2026-09-19 13:40Z)

- **Finding.** `correctionStatementAmended` (registered `T02.correction`, `Contracts/V1/Correction3.lean:433`) and the canonical T16 entry `localPotentialAPI` (`Section3/T16/LocalPotential.lean`, used at `Section3/T17/Assembly.lean` in `correctionStatementAmended_holds`) require **global** `ContDiff ℝ ∞ v` and **global** `IsPeriodicOn univ v`. T18's `InsertionData` (lane 455) types the correction as `CorrectionAPI ν place reference.velocity r δ D` with `reference : ClassicalSolutionT ν a g (place.T + δ)`, and `ClassicalSolutionT` only provides `velocity_smooth : ContDiffOn ℝ ∞ velocity (Ico 0 T ×ˢ univ)` and `velocity_periodic : IsPeriodicOn (Ico 0 T) velocity` (`Contracts/V1/TorusLocalTheory.lean:189`). A classical solution's velocity is arbitrary for `t < 0` and no smooth extension across `t = 0` is available, so T19 (U7/U8/U9/U13, the T18 instantiation from `RegularThroughT`) cannot discharge the two global hypotheses. Modifying the reference does not help: `CorrectionAPI.potential` (T16, `potential_formula : ∀ t x, …` and `potential_curl` over the whole cylinder `Ioo 0 (T+δ) × ball x₀ r`) pins `D.potential` to the *given* `v` at all times, so a correction built for a time-cutoff `χ·v` does not transfer to `v` with the same `D`.
- **Ruling.** No contract change. Add a canonical **slab bridge** (T17 U13, lane 460): `correctionStatementSlab` = the amended block with `IsPeriodicOn (Ico 0 (place.T+δ)) v` and `ContDiffOn ℝ ∞ v (Ico 0 (place.T+δ) ×ˢ univ)` in place of the two global hypotheses, proved from the existing canonical proofs (the window-local fields via a cutoff `χ·v` and window agreement; the T16 potential via a generalized T16 theorem with the same local hypotheses, as a new theorem, existing signatures untouched). T19's threading unit consumes the slab form. If the T16 potential genuinely needs global periodicity, lane 460 reports the exact field and we escalate to a T16/T17 V2 (owner).
- **Consequence for T19.** `T19_SPLIT.md` wave 2 gets a new unit **U0 — `insertionFromReference`** (threading): from `hν, ha, hg, hδ, reference` build `InsertionData` (packet = registered `T01.packet_import` family, placement + scaling = T15 U15 (459) at the given `T`, `r`/`D`/correction = slab bridge) and `RawPremises`, hence `Nonempty (PeriodicInsertionAPI data)` (455). U7/U8/U9/U13 consume U0.
- **G5 addendum (lead, 2026-09-19 13:55Z, after lane 460's counterexample)**: the first slab statement (hypotheses `IsPeriodicOn (Ico 0 (T+δ)) v` + `ContDiffOn` on the closed slab) is **false**: `CorrectionAPI.reference_periodic : IsPeriodicOn univ v` is a *field*, and `v := 0` for `t ≥ 0`, `v(t,x) := x` for `t < 0` satisfies every hypothesis but not the field (`Section3/T17/SlabBridge.lean: not_correctionStatementSlab`, lane 460). No other field needs global data; global `ContDiff ℝ ∞ v` is consumed only by the proof. Revised ruling: the bridge keeps `IsPeriodicOn univ v` as a hypothesis and drops global smoothness to `ContDiffOn ℝ ∞ v (Ioo 0 (place.T+δ) ×ˢ univ)` (`correctionStatementSlab'`, lane 460 continuation); T19 U0 feeds the **zero extension** `v_ext := if t ∈ Ico 0 (T+δ) then reference.velocity else 0` of the classical reference (globally periodic, smooth on the open slab) and replaces the T18 `reference` by the classical solution with that velocity (all `ClassicalSolutionT` fields live on `Ico 0 T`, so the zero extension is again a classical solution). No contract change.
- **G5 closed at the canonical level (lane 460 continuation, astra, 2026-09-19 14:03Z)**: `Section3/T17/SlabBridge2.lean: correctionStatementSlab'_holds` — the amended block with `IsPeriodicOn univ v` kept and `ContDiff ℝ ∞ v` replaced by `ContDiffOn ℝ ∞ v (Ioo 0 (place.T+δ) ×ˢ univ)`; proof = T16 potential for the original `v` + time-cutoff `χ·v` for the Paper1 profile lemmas + window-agreement transfer of the 44 remaining fields (cutoff threshold strengthened internally to `2ε² < min T δ / 2`). Probe `research/T17/probes/slab_from_classical.lean` applies it to the zero extension of an arbitrary `ClassicalSolutionT` reference. The first slab statement stays refuted (`SlabBridge.lean`). T19 U0 (lane 461) consumes `correctionStatementSlab'_holds`.
