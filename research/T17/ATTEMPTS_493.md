# Lane 493 attempts

1. Read COMMON, article Lemmas 3.4–3.5, the 45-field API, Assembly,
   SlabBridge2, Threading, G4/G5, V1 bindings and V2 boundary pattern.
2. ArticleScope uses the classical horizon `place.T + δ` (the placement's T)
   and retains the raw support premise: PlacementData does not store it.
   The registered packet supplies it directly.
3. Proved slab existence from the existing bridge and zero extension.
   Proved global force equality with the article reference by correction support
   and eps_time, then exported the displayed bounds without changing rates.
4. Initial compile needed `include A`: Lean omits section variables absent
   from statement types. Added it; canonical module now compiles.

Existing-file edits authorized by the brief: contract registry, blueprint graph,
entrypoints, result map, guide, coverage counts, generated dependency graph,
axiom audit, and SPEC_ISSUES. Existing proof modules remain untouched.
5. Contract first compile reported `Ambiguous term VelocityField` (and
   PressureField/Space) because both registered and implementation namespaces
   were open. Removed the implementation open; V2 and packet specialization pass.
6. Added potential/profile identifications and all profile projections. The
   potential formula needed the qualified `Paper1.RadialPotential.cross`.
   All 26 canonical theorems print exactly the three permitted logical axioms.
7. Blueprint regeneration first reported `Current test roots differ from
   retained tests`; added Tests.Correction3V2 to entrypoints. It then correctly
   required `Source changed: rerun the article axiom audit`. Completed the
   65-declaration audit, reviewed zero forbidden results, and copied report.json.
8. `make paper` built both PDFs but the reader checker asserted its old,
   hard-coded six-entry Partial set (`check_reader_documents.py:61`). As part
   of the authorized blueprint/gate update, removed lem:correction from that
   expected set and added a Closed + correctionStatementArticle_holds check.
   No mathematical check or bound was weakened.
