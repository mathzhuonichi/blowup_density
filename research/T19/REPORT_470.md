# Lane 470 report — T19 U15 assembly and `T03.density` registration

## 1. What was registered, with exact statements

`T03.density`, version 1 under parent task `T03`, registers four `Prop`-valued
records with 3/3/4/3 fields and the following four headline statements over
the registered T10/T11/T15 vocabulary.

```lean
def periodicDensityStatement : Prop :=
  ∀ a : SpatialField, a ∈ initialClassT →
    ∀ ν : ℝ, 0 < ν → ∀ T : ℝ, 0 < T →
      ∀ s : ℝ, s < 1 / 2 →
        RelativelyDenseT 1 s forceClassT (breakdownSetT ν a T)
```

```lean
def mixedRegionStatement : Prop :=
  ∀ a : SpatialField, a ∈ initialClassT →
    ∀ ν : ℝ, 0 < ν → ∀ T : ℝ, 0 < T →
      ∀ (p q : ℝ≥0∞) [Fact (1 ≤ p)], 1 ≤ q →
        3 < 3 / p.toReal + 2 / q.toReal →
          RelativelyDenseMixedT q p forceClassT (breakdownSetT ν a T)
```

```lean
def strongClosureStatement : Prop :=
  ∀ a : SpatialField, a ∈ initialClassT →
    ∀ ν : ℝ, 0 < ν → ∀ T : ℝ, 0 < T →
      ∀ u : SpaceTimeField, RegularTrajectoryT ν a T u →
        ∀ r : ℝ≥0∞, 0 < r →
          ∃ u' : SpaceTimeField, SingularTrajectoryT ν a T u' ∧
            energyENormT T (fun z => u z - u' z) < r
```

```lean
def projectionStatement : Prop :=
  ∀ ν : ℝ, 0 < ν → ∀ T : ℝ, 0 < T →
    (∀ s : ℝ, s < 1 / 2 →
      ∀ a : SpatialField, a ∈ initialClassT →
        ∀ g : SpaceTimeField, g ∈ forceClassT →
          ∀ r : ℝ≥0∞, 0 < r →
            ∃ f : SpaceTimeField,
              (a, f) ∈ extendedBreakdownSetT ν T ∧
                forceSobolevENormT 1 s (fun z => f z - g z) < r) ∧
    Prod.fst '' (extendedBreakdownSetT ν T) = initialClassT
```

The complete records additionally register the exact threshold and
regular-reference clauses, mixed-region arithmetic and examples, the
finite-time energy embedding and simultaneous closure family, reference
finiteness, and the zero-initial projection clause.  The single registry
declaration `BlowupDensity.Tests.checkedDensity` is the conjunction of the
four statements above.

## 2. Files and Lean contents

- `formalization/NSFormalization/Section3/T19/Assembly.lean` constructs the
  four canonical API records from all thirteen U1--U14 field theorems and
  proves the four canonical `...Statement_holds` theorems.
- `verification/Contracts/V1/Density.lean` restates the four records and four
  statements over registered vocabulary.  `verification/Bindings/Density.lean`
  supplies definitional guards, fieldwise conversions for the registered
  `ClassicalSolutionT`, the `maximalLifespanT_eq` rewrites, all four complete
  records, and all four contract-spelling statement theorems.
  `verification/Tests/Density.lean` defines the four public checks and the
  aggregate registry declaration, with an axiom gate for each.
- `research/T19/probes/assembly_closes.lean` proves that zero belongs to the
  registered initial and force classes, instantiates density with
  `ν = T = 1`, `s = 0`, radius `1`, and selects a concrete `densityForce`
  together with its breakdown membership and strict distance bound.
- `research/T19/axioms_u15.lean` audits the canonical assembly, contract
  vocabulary and records, bridges, statement theorems, and public checks.
  `research/T19/ATTEMPTS_U15.md` records the successful transport route and
  resolved development errors.  `research/T19/T19_SPLIT.md` now marks U15
  complete.
- `verification/contracts.json` adds exactly `T03.density`.  The T19 entry in
  `collaboration/work_items.json` now has owner `erenup`, state `in-progress`,
  and contracts `["T03.density"]`; `collaboration/TASKS.md` and
  `collaboration/tasks/T19.md` were regenerated by
  `python3 experiments/tasks.py render`.

## 3. Gaps

There are no proof, registration, non-vacuity, or axiom gaps in the requested
scope.  The contract intentionally does not assert T20 critical regularity,
optimality, a topology or non-density claim on the initial-data factor, or the
paper's separate peaks remark, following `RECONCILIATION.md`.

T18 is consumed internally through the T17 slab route G5: the reference is
zero-extended only to provide T17's global periodicity/smoothness inputs and
is identified with the original reference on `[0,T)`.  Consequently no slab
The required Prop-valued API defs use a module-local defProp-linter suppression, so direct elaboration and the lane's build/test output contain no Assembly warnings.

## 4. Commands and results

- `. scripts/lean-env.sh` was used for the Lean environment.  From
  `verification/`,
  `LEAN_NUM_THREADS=6 lake build NSFormalization.Section3.T19.Projection NSFormalization.Section3.T19.Closure NSFormalization.Section3.T19.DensityEngine`
  passed (10,660 jobs).
- `LEAN_NUM_THREADS=6 lake build NSFormalization.Section3.T19.Assembly` passed
  (10,661 jobs).  Targeted builds of `Contracts.V1.Density`,
  `Bindings.Density`, and `Tests.Density` also passed.
- `make check` passed with 51 registered contracts and a consistent work
  queue.  `make test` passed; `checkedPeriodicDensity`, `checkedMixedRegion`,
  `checkedStrongClosure`, `checkedProjection`, and `checkedDensity` each
  reported `standard logical axioms only`.  `make test-mutations` passed.
- `python3 experiments/check_contracts.py --base-ref origin/erenup/integration-section3`
  passed with `registered_contracts: 51` and
  `base_compatibility_checked: true`; the base contains 50 contracts.
- `LEAN_NUM_THREADS=6 lake env lean ../research/T19/probes/assembly_closes.lean`
  passed.  All six probe declarations print exactly
  `[propext, Classical.choice, Quot.sound]`.
- `LEAN_NUM_THREADS=6 lake env lean ../research/T19/axioms_u15.lean` passed.
  Every audited declaration prints exactly
  `[propext, Classical.choice, Quot.sound]`.
- The forbidden-proof scan and `git diff --check` passed.
