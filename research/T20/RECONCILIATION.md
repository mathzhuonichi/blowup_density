# T20 (`prop:critical`, `paper/sections/03-torus.tex:383-505`) — reconciliation of drafts A (lane 303) and B (lane 304)

Lead: erenup, 2026-09-18. Drafts: `.claude/worktrees/303-SPEC-t20-draft-a/research/T20/DraftA.lean` (562 lines), `.claude/worktrees/304-SPEC-t20-draft-b/research/T20/DraftB.lean` (1320 lines). Both import the registered `Contracts.V1.TorusData` and copy only the still-unregistered T10 solution half plus the T11/T12 declarations they use; both name the structure `CriticalRegularityTAPI` and both make it `Type`-valued.

## 0. Lead review

(left for the lead)

## 0. Lead review (2026-09-18 03:30Z)
Drafted by an Opus subagent from the two blind drafts (303/304); reviewed and **approved** by the lead. Binding: base B with A's spellings; the proposition is for the **zero initial datum** (`maximalLifespanT ν 0 g = ⊤`, inhomogeneous `H^{1/2}` force norm, one constant `c` fixed before `ν, g`) — the lane brief's "`a ∈ initialClassT`" was the brief's error, not the paper's; drop B's four API indices (unstatable without unregistered siblings; R43/C01 are not indexed) — the T11/T12 inputs enter as hypotheses of the consumer theorem, not as structure parameters; interval estimates in the registered `C01.enstrophyIdentity` shape; the two `:411` fields (skew-adjointness, `Λ`-commutation) restored; mean identities belong to T11's `PeriodicMeanReductionAPI` at `a = 0`; real `∫‖·‖²` spellings matching `gradientSq`/`laplacianSq`/`l2Sq`; one `reductionRegular` guard with `MemLp`; `^2` unified. The `:460-464` dissipation display stays derived (both drafts agree).

## 1. Agreement

Both drafts state **the paper's proposition and nothing larger**: `∀ ν, 0 < ν → ∀ g ∈ forceClassT, forceSobolevENormT 1 (1/2) g < ENNReal.ofReal (c * ν) → maximalLifespanT ν (fun _ ↦ 0) g = ⊤`. Both use the **zero initial datum literally** (`:385-389` writes `T_max^ν(0,g)`; there is no `a ∈ initialClassT` hypothesis in this proposition — the lane brief's phrasing is wrong, and both drafts are right), the **inhomogeneous** `H^{1/2}` force norm (not `Ḣ^{1/2}`; same choice as the registered `R43.inhomogeneousAtZero`), one `c` fixed as structure data **before** `ν` and `g` (`:383-384` "depending only on the unit torus and the stated norm conventions"), and `= ⊤` rather than regularity through a horizon.

Both independently converged on: (i) `Type`-valued API with the estimate constants as real data fields plus positivity, and the two proof-internal shrinkings `c < 1/(4C₀)` (`:457`) and `c < 1/(4C₁)` (`:477`) as fields; (ii) the **untranslated** reduction `meanPathT g := galileanMeanT (fun _ ↦ 0) g`, `v = u − m`, `h = g − ḡ`, with `(m·∇)v` retained in `eq:meanfree` — exactly the `research/T11/RECONCILIATION.md` §3 remark; (iii) `y, z, b` as `periodicHomogeneousENorm` at orders `1/2, 3/2, 1/2` of `v, v, h`; (iv) every undifferentiated time integral as a `lintegral` (`Ioc 0 t`, `(0,∞)`) so divergence is `⊤`, never a junk real `0`; (v) the two differential displays as `∃ E', HasDerivAt … E' t ∧ …`, never `HasDerivAt … → …` (a vacuous premise); (vi) `eq:ybound` recorded in its post-bootstrap, lifespan-wide form under `ρ < cν`, not the provisional conditional estimate; (vii) neither draft states the `:460-464` integrated-dissipation display (it is "in particular" and is not used again) — it stays a derived lemma; (viii) no mean-zero hypothesis on the force is inserted anywhere.

## 2. Rulings

| clause | A | B | ruling (reason) |
|---|---|---|---|
| API sort | `structure CriticalRegularityTAPI : Type` | same | **both**: constants are data; a `Prop` would collapse them by proof irrelevance. |
| API indices | none | indexed by `_localTheory`/`_continuation`/`_meanReduction`/`_calculus` (4 unused APIs) | **A.** The four indices are `_`-prefixed and constrain nothing, but they make `Nonempty (CriticalRegularityTAPI …)` unstatable without inhabiting four *unregistered* sibling APIs. R43/C01 are not indexed by their dependencies (C01 `extends`; A04's index is a real `horizon` function). |
| solution shape in interval estimates | `SolvesBelowT ν 0 g S u p` + `t ∈ Ico 0 S` | `w : ClassicalSolutionT ν 0 g T` + `t ∈ Ico/Ioo 0 T` | **B.** Registered house shape (`C01.enstrophyIdentity`: `∀ (T) (w : ClassicalSolutionR ν a f T), ∀ t ∈ Ioo 0 T`). The two are interderivable (pick `b ∈ (t,S)`); B needs no T11 copy in the statement. |
| `eq:meanbound` quantifiers | `ν, g, S, u, p, SolvesBelowT, t` | `g, t` only | **B.** `meanPathT` is data-defined, so A's solution quantifiers are dead hypotheses that only weaken the field. |
| `m(t) = meanT (u t)`, `m(0)=0`, `m' = ḡ` | not fields (owned by T11) | three fields (`mean_identification`, `mean_initial`, `mean_derivative`) | **A.** `research/T11/RECONCILIATION.md` §3 (lead-approved) already assigns these to `PeriodicMeanReductionAPI.mean_formula`/`mean_derivative` at `a = 0`; re-registering them duplicates a sibling contract. |
| `:411` skew-adjointness + `Λ` commutation | omitted (called proof lemmas) | `constant_transport_skew`, `constant_transport_commutes_lambda` | **B, decisively.** `:411` is a displayed assertion of the proof and `SECTION3_PLAN.md:50` lists "常输运的反自伴/与乘子交换" inside T20's scope. Both clauses check out (torus integration by parts with `div m = 0`; `2πi(m·k)` commutes with `√(4π²|k|²)`). |
| `Integrable` premises on the skew field | — | two `Integrable (fun y ↦ ⟪…⟫)` hypotheses | **drop them.** `torusLift` is a *measurable* section (`TorusData.lean:68-70`), so a `SmoothPeriodicT` field lifts to a bounded measurable function on a probability space — integrable. Keeping them is a gratuitous weakening; the lemma is needed everywhere anyway (§4 ①). |
| `eq:meanfree` spelling | `navierStokesResidual ν v p t x + (m·∇)v = h` | term-by-term `∂ₜ + advection + (m·∇) − ν•Δ + ∇p = h` | **A.** `navierStokesResidual` is the spelling `ClassicalSolutionT.momentum` itself uses (`research/T10/Spec.lean:347`); B's expansion must be re-proved equal to it and can drift. |
| junk guards for `toReal` / Bochner | bundled per field, at the single time `t` | one `reduction_regular` field over all `t ∈ Ico 0 T` | **B's factoring, A's spelling.** One regularity field, but stated with `MemLp (torusLift …) 2 periodicTorusMeasure` (A) instead of `periodicLpENorm … ≠ ⊤` (B): `MemLp` also gives strong measurability, which finiteness alone does not (same ruling as T15 `energySlices_memLp`). Replace B's `let v := …` bindings by the explicit terms. |
| `‖∇v‖₂²`, `‖Δv‖₂²`, `‖h‖₂²` | real `∫ ‖torusLift ·‖² ∂periodicTorusMeasure` | `(periodicLpENorm 2 ·).toReal ^ 2` | **A.** Registered spelling: `V1/EnergyAbsorptionPartial.lean:117,127` `l2Sq`/`laplacianSq` and `V2/EnergyAbsorptionPartial.lean:85` `gradientSq` are real integrals `∫ ‖·‖²`. Same junk exposure either way; house style decides. |
| `eq:bintegral` measure | `∫⁻ t in Ioi 0` | `∫⁻ t ∂forceTimeMeasure` | **B.** `forceTimeMeasure = volume.restrict (Ioi 0)` (`V1/Data.lean:118`), same measure, but the named spelling cannot drift from `forceSobolevENormT`. |
| `∫₀ᵗ b` | named `criticalBPartial` | inlined twice in `y_bound` | **A** (one definition, one place to bridge to the real interval integral). |
| `y, z, b` argument convention | `criticalY g u t` | `criticalY v t`, `criticalB h t` | **B.** Taking the already-mean-free field reads like the paper's `y(t) = ‖Λ^{1/2}v(t)‖₂` and keeps the defs reusable. |
| `:492-500` continuation display | not a field (derived) | `continuation_bound`: `∫₀ˢ‖u‖²_{H²} = ∫₀ˢ(|m|²+‖v‖²_{H²}) ≤ Sρ² + Cν⁻²∫₀^∞‖h‖₂² ≠ ⊤` | **B.** It is a displayed paper computation (the orthogonal-mode identity is real content: the `k=0` weight is `1`, so `‖u‖²_{H²} = |m|² + ‖v‖²_{H²}`), and it is the only seam to `eq:criterion`. |
| `maximal_criterion_finite` (T11 premise shape) | — | field | **drop.** No paper display; it is packaging (monotone convergence to the unattained maximal lifespan + T11 uniqueness) whose only consumer is `globalRegularity`, itself a field. Keep it as an internal lemma (§4 ⑫). |
| `zero_initial_admissible` | — | field | **drop (A).** `(fun _ ↦ 0) ∈ initialClassT` unfolds to `ContDiff ∞ 0 ∧ periodic 0 ∧ div 0 = 0`: trivially true, no paper content, a free field. |
| exponent spelling | `^ 2` (ℕ) throughout | mixes `^ 2` (copied `squaredHTwoIntegralT`) and `^ (2:ℝ)` (`meanModeCriterionIntegral`, `criticalRho ^ (2:ℝ)`) | **A.** One spelling, `^ 2`, matching T11's `squaredHTwoIntegralT`; the mixed form forces `ENNReal.rpow_natCast` rewrites at every use. |
| constant names | `C₀`, `C₁`, `CH1` | `Ccritical`, `Censtrophy`, `CHone`, `Ccriterion` | **A for `C₀`/`C₁`** (the paper writes `C_0` at `:430` and `C_1` at `:471`), **B's `Ccriterion`** (needed once `continuation_bound` is a field). |
| field naming | camelCase (`meanBound`, `yBound`, `globalRegularity`) | snake_case (`mean_bound`, `y_bound`, `global_regular`) | **A.** Registered contracts are camelCase (`enstrophyIdentity`, `inhomogeneousAtZero`, `h2TimeIntegralZeroDatum`). |

Nothing else in either draft is false or vacuous. Checked against the paper line by line: the smallness is on `ρ = ‖g‖_{L¹(0,∞;H^{1/2})}` with one universal `c`; `eq:criticalenergy` is `½(y²)' + (ν − C₀y)z² ≤ by` (both write `E'/2 + …`, correct); `eq:bintegral` is `∫₀^∞ b ≤ ρ` (true because the `k=0` weight is `1`, so removing the mean is contractive on the `H^{1/2}` datum); `eq:ybound` is `y(t) ≤ ∫₀ᵗ b ≤ ρ`; `eq:H1energy` is `(‖∇v‖₂²)' + ν‖Δv‖₂² ≤ Cν⁻¹‖h‖₂²`. Neither draft omits a labelled display and neither invents a norm, constant or hypothesis the paper does not write.

## 3. Decisions

**Base draft: B** (field coverage: `:411`, the regularity guard, and the `:492-500` display). **Take from A**: no API indices; camelCase names; `navierStokesResidual` for `eq:meanfree`; real-integral `gradientSqT`/`laplacianSqT`/`lTwoSqT` with `MemLp` guards; `C₀`/`C₁`; `criticalBPartial`; `^ 2` everywhere. **Drop from B**: the four indices, `zero_initial_admissible`, `mean_identification`, `mean_initial`, `mean_derivative`, `maximal_criterion_finite`, the two `Integrable` premises, the `let` bindings.

`research/T20/Spec.lean` = defs `meanPathT`, `meanFreeVelocity`, `meanFreeForce`, `constantTransportT`, `constantTransportSpatialT`, `meanForceIntegralT`, `criticalY`, `criticalZ`, `criticalB`, `criticalBIntegral`, `criticalBPartial`, `criticalRho`, `gradientSqT`, `laplacianSqT`, `lTwoSqT`, `meanFreeForceLTwoSqIntegral`, `meanModeCriterionIntegral`, `periodicPairing` + one `structure CriticalRegularityTAPI : Type` with:

| field | informal statement | paper |
|---|---|---|
| `c`, `hc`, `C₀`, `hC₀`, `C₁`, `hC₁`, `CH1`, `hCH1`, `Ccriterion`, `hCcriterion` | five positive reals, all data before `ν, g, w, t` | `:383-384`, `:430`, `:471`, `:479-481`, `:497-499` |
| `c_lt_C₀`, `c_lt_C₁` | `c < 1/(4C₀)`, `c < 1/(4C₁)` | `:457`, `:477` |
| `reductionRegular` | for every `w : ClassicalSolutionT ν 0 g T` and `t ∈ Ico 0 T`: `v(t)` and `h(t)` are mean-zero smooth periodic, `v(t) ∈ Ḣ^{1/2} ∩ Ḣ^{3/2} ∩ H²`, `h(t) ∈ Ḣ^{1/2}`, and `∇v(t)`, `Δv(t)`, `h(t)` are `MemLp 2` on the torus | `:392-394`, `:414-418`, `:467-488` |
| `meanBound` | `‖m(t)‖ ≤ ∫₀ᵗ‖ḡ‖ ≤ ρ`, for `g ∈ F_T` and `t ≥ 0` | `eq:meanbound` `:404-406` |
| `meanFreeEquation` | `navierStokesResidual ν v p t x + (m(t)·∇)v(t,x) = h(t,x)` at interior times | `eq:meanfree` `:408-410` |
| `constantTransportSkew` | `⟪(m·∇)v, w⟫_{L²(T³)} = −⟪v, (m·∇)w⟫` for smooth periodic `v, w` | `:411` |
| `constantTransportCommutesLambda` | `IsPeriodicLambda v Lv → IsPeriodicLambda ((m·∇)v) ((m·∇)Lv)` | `:411` |
| `criticalEnergy` | `∃ E', HasDerivAt (fun s ↦ y(s)²) E' t ∧ E'/2 + (ν − C₀y(t))z(t)² ≤ b(t)y(t)` | `eq:criticalenergy` `:438-440` |
| `bIntegral` | `∫₀^∞ b ≤ ρ` for `g ∈ F_T` | `eq:bintegral` `:442-444` |
| `yBound` | under `ρ < cν`: `y(t) ≤ ∫₀ᵗ b ≤ ρ` on the whole lifespan | `eq:ybound` `:454-458` |
| `hOneEnergy` | under `ρ < cν`: `∃ E', HasDerivAt (fun s ↦ ‖∇v(s)‖₂²) E' t ∧ E' + ν‖Δv(t)‖₂² ≤ CH1·ν⁻¹·‖h(t)‖₂²` | `eq:H1energy` `:482-484` |
| `continuationBound` | under `ρ < cν`, for `0 < S ≤ T`: `squaredHTwoIntegralT S u = ∫₀ˢ(‖m‖²+‖v‖²_{H²})`, that quantity `≤ Sρ² + Ccriterion·ν⁻²∫₀^∞‖h‖₂²`, and the bound is `≠ ⊤` | `:490-500` |
| `globalRegularity` | `0 < ν → g ∈ F_T → ρ < cν → maximalLifespanT ν 0 g = ⊤` | `prop:critical` `:383-390`, `eq:smallcritical` `:385-387` |

**Vocabulary / copy policy** (T13 policy, as amended for T15): `import Contracts.V1.TorusData` and use the registered `meanT`, `IsMeanZeroT`, `periodicSobolevENorm`, `periodicHomogeneousENorm`, `torusLift`, `periodicTorusMeasure` directly — do **not** copy them. Copy verbatim, each in its own namespace inside delimited blocks with `-- copied from <file>:<line>` markers: from `research/T10/Spec.lean` the solution half (`IsPeriodicSobolevPath`, `forceSobolevENormT`, `initialClassT`, `MemForceT`, `forceClassT`, `pressureMeanT`, `PressureGaugeT`, `normalizePressureT`, `ClassicalSolutionT`, `maximalLifespanT`); from `research/T11/Spec.lean` only `forceMeanT`, `galileanMeanT`, `squaredHTwoIntegralT`; from `research/T12/Spec.lean` only `MemPeriodicHmVector`, `MemPeriodicHomogeneous`, `SmoothPeriodicT`, `periodicLpENorm`, `lift`, `gradientTensor`, `laplacian`, `IsPeriodicLambda`. Do **not** copy `PeriodicLocalTheoryAPI`, `PeriodicContinuationAPI`, `PeriodicMeanReductionAPI`, `MeanZeroSobolevCalculusAPI` (B copies all four; with the indices dropped they are unused). **Constants**: five positive data fields, no product of T12 constants baked into the statement — `C₀` is assembled from `velocityCriticalL3` + `gradientLambdaCriticalL3`, `C₁` from `velocityCriticalL3` + `gradientLSix`, `Ccriterion` from `hTwo_le_laplacian`, all inside the proof.

## 4. Next

**Spec lane** (next lane number per `PLAN.md`): write `research/T20/Spec.lean` per §3, elaborate it (`cd verification && lake env lean ../research/T20/Spec.lean`), and merge the two COMPARISONs into `research/T20/COMPARISON.md`, keeping both paper-clause tables and both ambiguity lists (A §1-5, B §1-5).

**Nothing is added that is absent from both drafts.** The `:460-464` integrated-dissipation display is deliberately not a field (both drafts agree; the paper marks it "in particular" and never uses it again).

**"Needs a lemma" union (deduped, A §1-13 ∪ B §1-13):** ① `torusLift` of a smooth periodic field is bounded measurable, hence `MemLp p` for every `p` on the normalized torus. ② `(fun _ ↦ 0) ∈ initialClassT`, and T11 `mean_formula`/`mean_derivative` specialized at `a = 0` identify `meanT (u t)` with `meanPathT g` without any Galilean spatial shift. ③ mean-free decomposition of every solution/force slice: smoothness, periodicity, divergence, pressure gauge and Haar integrability are preserved by subtracting a spatial constant. ④ the pointwise residual identity for `u − m`: differentiate the data-defined mean, invariance of spatial derivatives under a constant shift, `(u·∇)u = (v·∇)v + (m·∇)v`. ⑤ `|ḡ(t)| ≤ ‖g(t)‖_{H^{1/2}}` (the `k=0` weight is `1`) and its time-integrated form `meanForceIntegralT g t ≤ ρ`. ⑥ removing the zero mode is contractive on the inhomogeneous half-order datum path, with a strongly measurable centered witness ⇒ `bIntegral`. ⑦ periodic integration by parts for `constantTransportSpatialT` (skew-adjointness) and the coefficient computation `2πi(m·k)` ⇒ commutation with `Λ` (and, later, with every real Fourier weight). ⑧ classical T10 slices and centered force slices furnish `MemPeriodicHomogeneous` at `1/2`, `3/2` and `MemPeriodicHmVector 2` — the bridge from integer smooth data to fractional homogeneous data. ⑨ test against `Λv`: pressure cancellation (`div Λv = 0`), constant-transport cancellation, T12 `velocityCriticalL3` + `gradientLambdaCriticalL3` + Hölder ⇒ `C₀` and `criticalEnergy`. ⑩ the scalar continuity bootstrap (continuity of `y` and of the forcing primitive, the `ζ ↓ 0` regularization, the first-crossing contradiction) ⇒ `yBound`. ⑪ test against `−Δv`: T12 `velocityCriticalL3` + `gradientLSix` + Young ⇒ `C₁`, `CH1`, `hOneEnergy`; integrate on preterminal intervals; `∫₀^∞‖h‖₂² < ∞` from `MemForceT`. ⑫ orthogonality of the constant and mean-zero modes + T12 `hTwo_le_laplacian` ⇒ `continuationBound`; then monotone convergence to the unattained maximal lifespan + T11 maximality/uniqueness ⇒ `squaredHTwoIntegralT S u ≠ ⊤` for every `ofReal S ≤ maximalLifespanT`, and T11 `PeriodicContinuationAPI.lifespanInfiniteOfLocallyFinite` ⇒ `globalRegularity`.

**Implementation candidates** (both COMPARISONs, deduped; none imported by the spec): `Paper1/PeriodicCriticalRegularity.lean` (`CriticalRegularityCertificate` at `:63`, `exists_critical_regularity_constant`, `critical_regular_ball`) — the target structure named in `SECTION3_PLAN.md` §3, but it hides the whole analytic bridge in one `global_lifespan` field and speaks the older `TestForce`/`forceDistance`/`PeriodicLifespan.lifespan` vocabulary: a **consumer** shape, not an implementation; `Paper1/CriticalEnergyCertificate.lean` (`CriticalEnergyCertificate` `:16`, `norm_le_radius` `:44`) — the abstract scalar bootstrap for ⑩, all continuity/derivative hypotheses still to be supplied from the PDE; `CriticalEnergyCoercivity.lean` (`dissipation_coercive` `:15`); `CriticalEnergyDerivative.lean` (`absorbed_energy_inequality_on_interval` `:8`, `energy_derivative_le` `:21`); `CriticalEnergyForcing.lean` (`forcing_work_le_radius` `:10`) and `CriticalEnergySignatures.lean:8-25`; `Paper1/PeriodicMeanZeroEstimate.lean` (`:18,:50,:94` finite-mode spectral gap after removing the zero coefficient, `one_le_periodicAngularMagnitude` `:71` reusable arithmetic) — finite-mode sums only, does not yet give the `lp`/physical-field statement.

**Open for the owner.** None specific to T20. The T11 `restart` `H¹`-vs-`H⁷` question (`research/T11/RECONCILIATION.md` §4) is inherited: if the T11 proof lane narrows that field, T20's `globalRegularity` is unaffected (it consumes only `lifespanInfiniteOfLocallyFinite`), but the narrowing must be explicit there, not here.
