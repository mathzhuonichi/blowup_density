# T24 (`prop:affine` / `prop:multiple` / `prop:conservative`, `paper/sections/03-torus.tex:667-740`) — reconciliation of drafts A (lane 306) and B (lane 307)

Lead: erenup, 2026-09-18. Drafts: `.claude/worktrees/306-SPEC-t24-draft-a/research/T24/DraftA.lean` (980 lines; 926-980 is the actual T24 content), `.claude/worktrees/307-SPEC-t24-draft-b/research/T24/DraftB.lean` (1256 lines; 954-1256). Upstream read: `Contracts/V1/{TorusData,Packet,Data,Scaling}.lean`, `research/T10|T14|T15/Spec.lean`, `Contracts/V1/{ForceClasses,GridObservations}.lean`. Neither draft touches T17's `CorrectionAPI`, and neither needs it — the three leaves never use the correction.

## 0. Lead review

(left for the lead)

## 0. Lead review (2026-09-18 07:35Z)
Drafted by an Opus subagent from the two blind drafts (306/307); reviewed and **approved** by the lead. Binding: base B (A's 42 bare `Prop` fields violate hard rule 3 and are used only as a clause list); `prop:affine` is the **whole-space** statement on the registered `I01.packet` (`PacketAPI ν`, `navierStokesResidual`, `SpeedUnboundedAtOne`, `energyENorm 1`) — no torus/scaled variant (that would be a new statement; owner's call, not the spec lane's); B's two false clauses fixed as ruled (`zero_from_rest` on `Ico 0 T` only; supports measured in `fundamentalCube`, T15's `*_singleCopy` form); the `affineCkSeminorm` as an `ℝ≥0∞` `⨆`; the non-paper `−∇φ ∈ forceClassT` hypothesis dropped; cylinder/ball family/`ν,T,N` as parameters with `placement_chart`; dissipation `:719` as an equality; sorts affine `Prop`, multiple `Type`, conservative `Prop`; bounded-domain/no-slip clauses out of V1 scope (recorded in the contract scope). Owner question recorded: whether a torus/scaled `prop:affine` is wanted.

## 1. Agreement

There is very little. **Draft A's three structures are 42 unspecified `Prop` fields** (`DraftA.lean:930-978`: `cylinder : Prop`, `divergence_free : Prop`, …). That is the placeholder-`Prop` pattern forbidden by `CLAUDE.md` 硬规矩 3; an inhabitant carries no mathematics and `Nonempty` of any of them is provable by `⟨True, …⟩`. A's 980-line file is 925 lines of copied T10/T13/T14/T15 vocabulary plus 55 lines of placeholders. **A contributes only its clause enumeration** (`COMPARISON_A.md`), which is genuinely useful and is used below.

What the two drafts do agree on, and what matches the paper: the three propositions are independent leaves; `eq:affine` is the six-term expansion `F̃ = F + ∂ₜb − νΔb + (U·∇)b + (b·∇)U + (b·∇)b` with `P̃ = P`; `prop:multiple` superposes finitely many T15-rescaled packets at one common terminal time `T` with `M, D` taken from the imported packet rather than reintroduced; `prop:conservative` is a universal "zero solution from rest" implication with the potential's periodicity as the hypothesis that excludes affine gauges.

Draft B's `affineForce`, `crossAdvection`, `affineVelocity`, `affinePressure` (`DraftB.lean:981-999`) are byte-faithful to `eq:affine` in the registered operator tokens (`temporalDerivative`, `spatialLaplacian`, `spatialDerivative _ _ _ (v (t,x))`), with the transport terms in the paper's order. I re-checked every display at `:668-740` against B: the `:718` energy bound is `≤`, the `:719` dissipation bound is `=`, `ε_j² < T` at `:702`, `x_j + ε_jK_* ⊂ B_j` strictly at `:701-702`, `(U_i·∇)U_j = 0` at `:713`, and the pairing/energy identity at `:729-734`.

**Ambient-space finding (neither draft got this right).** `prop:affine` is *not* a torus statement: `:669-671` fixes "the localized solution `(U,P,F)` of Theorem~\ref{thm:packet}" (= `01-introduction.tex:15`, the registered `I01.packet`) and a cylinder `Q = B₀×(τ₀,τ₁)` with **`B₀ ⋐ ℝ³`** and **`0<τ₀<τ₁<1`**. Terminal time is the packet's `1`. Draft B re-based it onto `ClassicalSolutionT ν 0 baseForce terminal` with `τ₁ < terminal` and calls this "the rescaled terminal-time form" (`COMPARISON_B.md` row 1) — a silent change of ambient space and of what is being varied. Draft A's `cylinder_hypotheses : Prop` says nothing either way. So the three leaves live in **two different ambient spaces**: T24a whole-space (T14 only), T24b torus (T10+T13+T14+T15), T24c torus (T10 only).

## 2. Rulings

| clause | A | B | ruling (reason) |
|---|---|---|---|
| any field at all | 42 bare `Prop` fields | concrete statements | **B, wholesale.** 硬规矩 3. A is unusable as a base. |
| `prop:affine` ambient | unspecified | torus `ClassicalSolutionT`, free `terminal` | **neither: whole space.** `:671` writes `B₀ ⋐ ℝ³` and `:670` writes `τ₁<1`. Index by `{ν} (P : PacketAPI ν)`, terminal time literally `1`, and use the **registered** `Contracts.V1` whole-space vocabulary (`navierStokesResidual`, `spatialDivergence`, `CompactPositiveTimeSupport`, `SpeedUnboundedAtOne`, `energyENorm 1`). T24a then needs **no T10/T13/T15 copy at all** — only T14's two structures. |
| `AffineVariationAPI` sort | `Type` (placeholders) | `Type` | **`Prop`.** Once the cylinder `(c, r, τ₀, τ₁)` moves from fields to parameters (next row) the record is constant-free: house style then says `Prop`. |
| cylinder / regions: data or parameter | data (`region_count : ℕ`) | data (`ballCenter`, `τ₀`, `N`, `regionCenter`, `ν`, `T`, `packet`, …) | **parameters.** The paper universally quantifies "fix a cylinder", "fix finitely many disjoint balls", "at fixed `ν>0` and `T>0`". As B has them, `Nonempty MultipleRegionsAPI` is satisfiable by picking `N=1` and one convenient ball — the prescription is lost. Parameters: affine `{ν} (P) (c : Space) (r τ₀ τ₁ : ℝ)`; multiple `{ν} (P) (T : ℝ) {N} (regionCenter regionRadius)`. |
| `MultipleRegionsAPI` sort | `Type` | `Type` | **both, `Type`** — it carries `ScalingAPI`, `PlacementData` and `ClassicalSolutionT` witnesses as selected data. No new constants: `M, D` stay `P.energyBound`/`P.dissipationBound` (B, correct). |
| `ConservativeForcingAPI` sort | `Type` | `Prop` | **B.** Constant-free universal implication. |
| **`conservative` conclusion** | `identically_zero : Prop` | `S.velocity = fun _ ↦ 0` | **B's field is FALSE.** `ClassicalSolutionT` constrains `velocity` only on `Ico 0 T ×ˢ univ` (`ContDiffOn`/`momentum`/`divergence` are all `nhdsWithin` that slab); values at `t < 0` are free, so a record with nonzero negative-time velocity exists. Paper says "identically zero **on its classical lifespan**" (`:728`). Ruling: `∀ t ∈ Ico 0 T, ∀ x, S.velocity (t,x) = 0`. |
| `conservative` force-class premise | none | `conservativeForceT φ ∈ forceClassT` | **drop.** `MemForceT` demands compact support in positive time; that excludes every time-independent `φ`, i.e. the paper's main case, and the paper imposes no such hypothesis (`:723-726`). The proof needs only smoothness + periodicity of `φ`, both already in `PeriodicPotentialT`. |
| pairing display `:729-731` | `force_velocity_pairing_zero` (placeholder) | absent | **A's clause, made concrete.** `∫_{T³} f·u = 0` is a reusable torus integration-by-parts fact and is a labelled display; add it as the API's first field. |
| `energy_identity`, `nonnegative_terms`, `no_breakdown`, `affine_potential_excluded`, `domain_choice`, `incompressibility`, `smooth_solution_from_rest` (A) | fields | absent | **drop all seven.** The first four are proof steps or remarks (`:732-740`); the rest are already `ClassicalSolutionT` fields or the `PeriodicPotentialT` hypothesis. The "affine potentials are excluded" sentence *is* the periodicity hypothesis, not a separate claim. |
| **`multiple` component supports** | `scaled_support_containment : Prop` | `tsupport ((component j).velocity (t,·)) ⊆ ball j`, and `∀ z ∈ tsupport (periodizedScaledForce …), z.2 ∈ ball j` | **B's two fields are FALSE.** The periodized fields are spatially unit-periodic, so their `tsupport` in `ℝ³` contains every lattice translate and is never inside one ball. Ruling: measure the support **inside the fundamental cube** — `∀ t ∈ Ico 0 T, ∀ x ∈ fundamentalCube, x ∉ ball j → (component j).velocity (t,x) = 0`, likewise for the force at every `t`. This is the form T15's `velocity_singleCopy`/`force_singleCopy` + `eps_space` actually deliver. |
| region ↔ chart ball | — | unrelated (`regionCenter j` vs `(placement j).chartCenter`) | **add `placement_chart : (placement j).chartCenter = regionCenter j ∧ (placement j).chartRadius = regionRadius j`.** Without it nothing connects T15's conclusions (all stated on the chart ball) to `B_j`, and B's `component_*_support`, `scaled_carrier_in_region`, `placement_center_mem` are unprovable obligations. With it, those three fields and `region_interior` follow from `PlacementData.eps_space`/`x₀_mem`/`chartBall_in_cube` and are dropped. |
| `:719` dissipation bound | placeholder | `≤` | **`=`.** The paper writes `∫₀^T‖∇u‖₂² dt = D²Σεⱼ` — an identity, inherited from T15's `packetDissipationIdentity` (also an equality) by disjointness. `:718` stays `≤` (B correct there). |
| `energy_finite`, `dissipation_finite` | placeholders | fields | **drop.** Implied by the two bounds, whose right sides are `ENNReal.ofReal` of finite reals. |
| `energyEssSupT` vs `sup_{t<T}` | — | `essSup` | **B.** The registered torus `E_T` spelling; velocities are continuous so the two agree, and `eLpNorm`/`essSup` have no junk value (the `≤`/`=` are honest without a `MemLp` guard — unlike T15's identities, which needed one). |
| `C^m` seminorm | `nonisolated_scaling : Prop` | `affineCkSeminorm : ℝ` via `sSup (Set.range …)` in tactic mode | **neither: `ℝ≥0∞`.** Mathlib's real `sSup` of an unbounded range is `0`, so an unbounded difference would satisfy `Tendsto … (𝓝 0)` vacuously. Use `∑_{k≤m} ⨆ z ∈ K, ‖iteratedFDeriv ℝ k f z‖ₑ` (same precedent as `Data.lean:475` choosing `ℝ≥0∞` for `energyENorm`); term mode; drop the unused `_hK : IsCompact K`. |
| non-isolation support set | — | `∀ K compact, tsupport b ⊆ K → …` | **use `tsupport b` itself.** `:677` says "on their compact support"; both differences vanish off `supp b`, so the `∀ K` generality is derivable and only adds an argument. |
| non-isolation form | — | `Tendsto` only | **B.** `:677` states convergence; the quantitative `C_m|λ| + C_m'λ²` at `:694-696` is proof-internal. Do **not** add a `cSeminorm : ℕ → ℝ` data family — that would make the record `Type` again for a constant the proposition does not state. |
| affine conclusions packaging | 14 placeholders | one 8-conjunct `family` field | **split into one field per paper clause** (7 fields, each `∀ b, AffineAdmissible … → <clause>`), per house style; B's `energyGradientT … < ⊤` conjunct is dropped as implied by `energyENorm 1 … < ⊤`. |
| `AffineAdmissible` | — | `ContDiff ∞ ∧ HasCompactSupport ∧ tsupport ⊆ Ioo τ₀ τ₁ ×ˢ ball c r ∧ div b = 0` | **B verbatim.** Exactly `b ∈ C_c^∞(Q;ℝ³)`, `∇·b=0` (`:672-673`); the closed `tsupport` inside the open cylinder is the "away from the endpoints" condition of the title. |
| `distinct` (`b↦U+b` injective) | placeholder | field | **keep** (`:691` asserts it) although it is one line of `add_right_injective`. |
| `component` selection | — | `component` + `component_pin` | **keep**, noting it merely *selects* the witness that `ScalingAPI.solution (ε j)` already provides existentially. |
| bounded-domain / no-slip branch | `no_slip_boundary : Prop` (both leaves) | recorded as a gap | **B.** `:706,720,725` need an `Ω`-carrier, restriction norms and a no-slip class that do not exist anywhere in the tree. A's placeholder field is worse than an honest omission. Out of scope for V1; record in the contract `scope` string. |
| Section 4 counterparts | "can supply concrete shapes" | detailed table | **B, and there are none.** `grep` of `verification/contracts.json` for `affine`/`multiple`/`conservative` returns nothing. `R45.force_classes.regularReference` (fixed named reference + simultaneous solution) and `R47.grid_observations.GridFamilyAPI` (`Fin n` family with `center`/`radius`/`eps_pos`/`velocity_support`/`force_support`/`solution`/`lifespan`) are shape precedents only. |

Nothing else in either draft is false. B's `ε_time : ε j ^2 < T` is redundant (`PlacementData.eps_time` gives `2ε²<T`) but is the paper's `:702` and is kept.

## 3. Decisions

**Base draft: B**, with the three re-basings above (whole-space affine, parameters instead of data, corrected supports). **Take from A**: only the clause enumeration of `COMPARISON_A.md` — specifically the pairing field `:729-731` and the reminder that `:702` (`ε_j²<T`) and `:721` (common terminal time) are separate clauses. **Drop from A**: everything else. **Drop from B**: `base` as a torus solution, all geometry-as-data, `conservativeForceT φ ∈ forceClassT`, the global `S.velocity = 0`, the two `tsupport ⊆ ball` fields, `energy_finite`/`dissipation_finite`, `scaled_carrier_in_region`/`placement_center_mem`, the real `affineCkSeminorm`, the `∀ K` in `nonisolated`.

**Vocabulary / copy policy.** One file `research/T24/Spec.lean`, `import Contracts.V1.{TorusData,Packet,Data,Scaling}`, three namespaces, three future contract ids (`T24.affine_variation`, `T24.multiple_regions`, `T24.conservative_forcing`). Everything registered is imported, never copied. Copied verbatim in their own namespaces with the T13 synchronization header: **T14** `accumulatedForce`, `PacketEnergyAPI`, `PacketImportAPI`, `PacketImportFamily` (all three leaves' entry point); **T10** solution half — `IsPeriodicSobolevPath`, `forceSobolevENormT`, `MemForceT`, `forceClassT`, `pressureMeanT`, `PressureGaugeT`, `normalizePressureT`, `ClassicalSolutionT`, `energyEssSupT`, `energyGradientT`, `energyENormT` (T24b, T24c); **T13** `fundamentalCube`, `latticeVector`, `periodize`, `LocalizationAPI` (T24b); **T15** `scaledStartTime`…`normalizedScaledPressure`, `PlacementData`, `ScalingAPI` and their auxiliaries (T24b). T24a copies **only** T14. **Constants: none new** in any of the three.

`T24a` defs `affineCylinder`, `AffineAdmissible`, `crossAdvection`, `affineVelocity`, `affinePressure`, `affineForce`, `ckSeminormE` + `structure AffineVariationAPI {ν} (P : PacketAPI ν) (c : Space) (r τ₀ τ₁ : ℝ) : Prop`:

| field | informal statement | paper |
|---|---|---|
| `radius_pos`, `window` | `0<r`; `0<τ₀<τ₁<1` | `:670-671` |
| `force_smooth`, `force_support` | for every admissible `b`, `F̃` is `C^∞` on all of spacetime and has compact support in positive time (the correction extends smoothly by zero across `t=1`) | `:681-683` |
| `divergence_free` | `∇·(U+b)=0` on `[0,1)` | `:673,679` |
| `momentum` | `navierStokesResidual ν (U+b) P = F̃` on `(0,1)` | `:674-676,679` |
| `zero_initial` | `(U+b)(0,·)=0` | `:684-685` |
| `late_agreement` | `U+b = U` for every `t ≥ τ₁` | `:684-685` |
| `speed_unbounded` | `SpeedUnboundedAtOne (U+b)` | `:686` |
| `energy_finite` | `energyENorm 1 (U+b) < ⊤` (both summands, i.e. energy and dissipation) | `:686-687` |
| `infinite_dimensional` | a countable admissible family that is `LinearIndependent ℝ` | `:688-691` |
| `distinct` | `b₁ ≠ b₂ → U+b₁ ≠ U+b₂` | `:691` |
| `nonisolated` | for each admissible `b ≠ 0` and each `m`, the `ℝ≥0∞` `C^m` seminorm on `tsupport b` of `λb` and of `F̃_{λb} − F` tends to `0` as `λ→0` | `:677,692-696` |

`T24b` defs `finiteVelocitySum/PressureSum/ForceSum`, `SpeedUnboundedAtOn T B u` + `structure MultipleRegionsAPI {ν} (P : PacketImportAPI ν) (T : ℝ) {N : ℕ} (regionCenter : Fin N → Space) (regionRadius : Fin N → ℝ) : Type`: `T_pos`, `N_pos`, `regionRadius_pos`, `region_interior`, `regions_disjoint` (`:698-699`); `placement`, `placement_time`, `placement_chart`, `scaling`, `ε`, `eps_admissible`, `eps_time` (`:701-706`); `component`, `component_pin`, `component_support`, `component_force_support` (`:706-712`); `assembled_velocity/_pressure/_force` + their three formula fields, `solution`, `solution_pin`, `force_mem`, `rest` (`:710-716`); `region_agreement`, `region_blowup` (`:700,717,721-722`); `energy_bound` (`≤ M²Σεⱼ`, `:718`), `dissipation_bound` (`= D²Σεⱼ`, `:719`).

`T24c` defs `PeriodicPotentialT`, `conservativeForceT` + `structure ConservativeForcingAPI : Prop` with two fields: `potential_pairing` — for every `ν`, `T>0`, periodic smooth `φ` and every `S : ClassicalSolutionT ν 0 (−∇φ) T`, the Haar pairing `∫_{T³} ⟪−∇φ(t), S.velocity(t)⟫ = 0` at every `t ∈ [0,T)` (`:729-731`); `zero_from_rest` — under the same hypotheses with `0<ν`, `∀ t ∈ Ico 0 T, ∀ x, S.velocity (t,x) = 0` (`:723-728,732-737`).

Three `Prop`/`Nonempty` statements close the file: `affineVariationStatement`, `multipleRegionsStatement` (both quantifying the prescribed geometry first), `conservativeForcingStatement`.

## 4. Next

**Spec lane** (one lane, three namespaces): write `research/T24/Spec.lean` per §3, check with `cd verification && lake env lean ../research/T24/Spec.lean`, record the copy header and the ambient-space ruling in `research/T24/ATTEMPTS.md`, and add one line to `logs/LESSONS.md` for the two falsities (junk `sSup`; `tsupport` of a periodized field is never inside one ball).

**Needs a lemma** (union of both `COMPARISON`s plus this reconciliation): ① the `eq:affine` expansion `navierStokesResidual ν (U+b) P = F + L_Ub + (b·∇)b` (bilinearity of `spatialDerivative`); ② smooth zero-extension of the correction across `t=1` from `tsupport b ⊆ Ioo τ₀ τ₁ ×ˢ ball`, `τ₁<1` and boundedness of every fixed derivative of `U` there; ③ finite energy/dissipation of a compactly supported smooth `b` + triangle inequality; ④ disjointly supported div-free curls are linearly independent; ⑤ the `C^m` bound `≤ C_m|λ|+C_m'λ²`; ⑥ T15 support transfer — periodized rescaled fields vanish on `fundamentalCube \ chartBall` (from `velocity_singleCopy`/`force_singleCopy` + `eps_space`); ⑦ disjoint-support superposition: `(U_i·∇)U_j = 0`, the finite sum solves NS, `forceClassT` and the zero-mean gauge are closed under finite sums; ⑧ disjoint-support additivity `‖u(t)‖₂² = Σ‖U_j(t)‖₂²` and `∫‖∇u‖² = Σ∫‖∇U_j‖²`, then `:718-719` from T15's two identities; ⑨ torus integration by parts `∫∇φ·u = 0` and the classical energy identity; ⑩ Haar integrability of smooth periodic lifts (shared with T20 §4).

**Implementation candidates.** **T24c is close to already proved locally**: `formalization/NSFormalization/Paper1/ConservativeForce.lean:91` `zero_of_negative_gradient_on_Ico` has exactly `zero_from_rest`'s hypotheses (`ContDiffOn` on `Ico 0 S ×ˢ univ`, periodicity, `div u = 0` on `Ioo`, `residual ν u p = -pressureGradient φ`, `u(0,·)=0`) and exactly its corrected conclusion `∀ t ∈ Ico 0 S, ∀ x, u (t,x) = 0`. Two `rfl` bridges are needed: `NavierStokes.ProblemStatement.UnitSpatialPeriodsOn` is character-identical to `TorusData.IsPeriodicOn`, and `Source.Insertion.residual` is character-identical to the residual `ClassicalSolutionT.momentum` uses. Same file: `:23` `zero_of_conservative_residual`, `:61` `zero_velocity_negative_potential_residual` (the `f=−∇φ` identification), `:123` `inserted_force_not_conservative`. `Paper1/PeriodicNonpositiveForce.lean` supplies periodic negative-order force-norm transfer for T24b's force-class work. T24b's component fields come entirely from T15's `ScalingAPI`; T24a's from `I01.packet` plus a bump-function library. Shape precedents only (no registered counterpart exists): `Contracts/V1/GridObservations.lean` `GridFamilyAPI` for the `Fin N` family, `Contracts/V1/ForceClasses.lean` `ForceClassesAPI.regularReference` for a fixed named reference.
