# T15 (`prop:scaling`, `paper/sections/03-torus.tex:99-175`) — reconciliation of drafts A (lane 291) and B (lane 292)

Lead: erenup, 2026-09-17. Drafts: `.claude/worktrees/291-SPEC-t15-draft-a/research/T15/DraftA.lean` (950 lines), `.claude/worktrees/292-SPEC-t15-draft-b/research/T15/DraftB.lean` (1086 lines). Both post T10 lead amendment 1 (`Integrable (torusLift z)` in `IsPeriodicDatum`); both copy T10/T13/T14 declarations verbatim because `research/` is not a Lake module — **written before `T01.torus_data` was registered**, which changes the copy policy (§3).

## 0. Lead review

(left for the lead)

## 0. Lead review (2026-09-18 00:15Z)
Drafted by an Opus subagent from the two blind drafts (291/292); reviewed and **approved** by the lead. Binding: base B; `PlacementData` (A) as a structure parameter shared with T17; the PDE clause as one pinned `∃ S : ClassicalSolutionT ν 0 F_ε T`; the "in particular" sentence (`03-torus.tex:138`) is a field `forceConvergence` (it is inside the proposition and `I03.scaling` has it); dropped the `rfl`-true "spatially constant pressure adjustment" clause and the junk-dischargeable gauge without `pressureSlice_integrable`; all norm identities carry explicit `MemLp`/summability guards. Vocabulary: the registered `T01.torus_data` is imported, the unregistered T10 solution-class half and T13/T14 copies stay as delimited verbatim blocks.

## 1. Agreement

Both build one **Type-valued** `ScalingAPI` over a T14 `PacketImportAPI ν`, with the constants `C_s` as a **data field** `sobolevConst : ℝ → ℝ` fixed before every `ε` (house style: `TameProductAPI`, `GradientL6API`, `I03.scaling.positiveConst`), and every scale-indexed clause quantified over one `ε ∈ Ioc 0 ε₀`.

Identical, and identical to the paper: the literal `eq:scaling` (`:112-118`) with `t_ε = T − ε²`, amplitudes `ε⁻¹/ε⁻²/ε⁻³`, space scale `ε⁻¹`, time scale `ε⁻²`, `zeroPastField` on `U,P` and none on `F` (`:108-111`), time-first `SpaceTime`; periodization as the **lattice sum** `∑' n : ℤ³, f(t, x − n)` (T13's `periodize`) with explicit single-copy equalities on `fundamentalCube` (`:120`), the single copy resting on a compact `K_* ⊇ K ∪ pr_x (supp F)`, `x₀ + εK_* ⊂ B`, `closure B ⊆ interior Q`; the Section-3 smallness `2ε² < T` **only** (no Section-4 margin `δ`) plus the harmless `0 < ε₀ ≤ 1` inherited from `I03`; `eq:packetEscale` and `eq:packetFscale` as **equalities** in `ℝ≥0∞` with `M = P.energyBound`, `D = P.dissipationBound`; `α(p,q) = −3 + 3/p.toReal + 2/q.toReal`, token-identical to `Contracts.V1.alpha`, so `p = ∞` / `q = ∞` are covered by `toReal ⊤ = 0` with no endpoint cases, quantified `∀ p q, [Fact (1 ≤ p)] → 1 ≤ q → …`; the left side of `eq:packetFscale` is a **new torus** mixed norm (`mixedLebesgueENormT`, normalized Haar, `⨅` over `Lp`-paths) and the right side is the registered whole-space `Data.mixedLebesgueENorm` of the **unrescaled** `F` — the identity is exact only because `|Q| = 1` (`:93`); `eq:packetHs` only at `q = 1`, `0 ≤ s ≤ 1`, through `forceSobolevENormT 1 s`; `MemMixedLebesgueR/T` and `MemForceSobolevT` (byte-identical in both) so `packetMixedScaling` cannot degenerate to `⊤ = ⊤`; unbounded speed via the registered `Contracts.V1.SpeedUnboundedAt T`; `MemForceT F_ε` as an extra field (not literal in `prop:scaling`, but it is `02-preliminaries.tex:10`'s class and T18 consumes it); the "in particular" (`:138`) as a separate `def` with the same shape `∀ s < 1/2, Tendsto (fun ε ↦ forceSobolevENormT 1 s F_ε) (𝓝[>] 0) (𝓝 0)`, both noting that `s < 0` needs monotonicity, not an extension of `eq:packetHs`. Neither draft omits a display and neither invents a norm or exponent the paper does not write.

## 2. Rulings

| clause | A | B | ruling (one-line reason) |
|---|---|---|---|
| placement packaging | separate `structure PlacementData (P : PacketAPI ν)` (T, x₀, chart ball, `K_*`, ε₀, smallness), passed as a **parameter** | all of it inlined as 18 fields of `ScalingAPI` | **A, as a parameter.** T17 (`lem:correction`) and T18 (`thm:insertion`) must use *the same* `x₀, B, K_*, T, ε₀`; a shared parameter record makes that identification definitional, which is exactly what `research/I03/COMPARISON.md` 2.3 demanded and what `I03` bought by carrying `CorrectionAPI`. T15 and T17 are DAG siblings, so neither can carry the other. |
| chart geometry | `chartCenter`+`chartRadius`, `x₀ ∈ B`, literal `∀ y ∈ K_*, x₀+ε•y ∈ B` | ball centred at `x₀`, `K_* ⊆ ball 0 R_*`, `ε·R_* < r` (the `I03.eps_space` spelling) | **A for the containment** (`:105` literally says `x₀+εK_*⊂B`, and T13's `localization` takes an arbitrary `(c,r)`), **B's `R_*` kept as a derived convenience**, not as the primitive. |
| T13 witness | `localizationAPI : LocalizationAPI` as an **unused parameter** (no field mentions it) | field `localization : LocalizationAPI` | **B.** `I03` carries its upstream record as a field; A's parameter is pure decoration. (Both are proof-irrelevant: `LocalizationAPI` is `Prop`.) |
| lattice-sum junk | none | three `Summable` fields (velocity/pressure for `t<T`, force for all `t`) | **B.** `tsum` of a non-summable family is the junk value `0`. A is saved only indirectly (its single-copy equalities pin the value on `Q`); record the guard explicitly. |
| force single copy | `∀ t, 0 < t` | `∀ t : ℝ` | **B.** The spatial support condition is time-independent, so the restriction to `t>0` is a gratuitous weakening. |
| PDE clause | 8 separate fields: `velocity_smooth`, `pressure_smooth`, `velocity_periodic`, `pressure_periodic`, `zero_initial`, `divergence`, `momentum`, `pressure_gauge` | one `solution : ∀ ε, ∃ S : ClassicalSolutionT ν 0 F_ε T, S.velocity = … ∧ S.pressure = …` | **B.** The paper's "solve the periodic momentum equation … start from zero" *is* T10's `ClassicalSolutionT`; the two pinning equations remove the substitution risk; it is the shape T18/T11 consume. Cost, to be stated in the lane brief: `ClassicalSolutionT.sobolev` (a continuous `H^m` datum path for every `m : ℕ`) and `pressure_gradient` become T15 obligations. A's eight clauses stay as binding-level `theorem`s. |
| pressure adjustment | `pressure_adjustment_constant` (`p_ε−p̃_ε` independent of `x`) | first conjunct of `pressureNormalization` (`p_ε = p̃_ε − pressureMeanT p̃_ε t`) | **neither — both are `rfl`.** `normalizePressureT p = fun z ↦ p z − pressureMeanT p z.1` (`research/T10/Spec.lean:300-301`), so once `normalizedScaledPressure` is *defined* that way, both clauses are definitionally true and carry no content. **Flagged as trivially-true fields**; drop them and let the definition carry "a spatially constant adjustment" (`:123`), keeping only the gauge. |
| pressure gauge honesty | `pressure_gauge : PressureGaugeT …` with **no integrability** | `pressureSlice_integrable` + `PressureGaugeT` | **B.** `pressureMeanT` is a Bochner integral: for a non-integrable slice it is junk `0` and `PressureGaugeT` holds **vacuously**. A's gauge alone is discharge­able by junk. |
| energy honesty | `energy_finite` (`< ⊤` for both quantities) | `energySlices_memLp` (slice-wise `MemLp` for `u` and `∇u`) | **B.** A's field is *derivable* from its own two identities (the right sides are `ENNReal.ofReal`, hence finite); B's adds the strong measurability that finiteness does not give. |
| `C_s` sign | `0 < sobolevConst s` on `[0,1]` | `0 ≤ sobolevConst s` | **A.** Matches `LocalizationAPI.localization`'s `0 < C` and the manuscript's "constant"; neither sign makes the bound vacuous (a negative `C_s` would make it *stronger*), so this is hygiene, not repair. |
| `α` name | `alphaT` | `scalingExponent` | **A.** The `…T` suffix is the T10 convention (`forceSobolevENormT`, `MemForceT`, `energyENormT`) and keeps the whole-space `Contracts.V1.alpha` distinguishable. |
| `t_ε` name | `packetTimeShift` | `scaledStartTime` | **B** (it is the time at which the rescaled packet switches on); cosmetic. |
| source point | helper `scaledSourcePoint` reused three times | inlined three times | **A** (one place to bridge to `Contracts.V1.dilateField`). |
| "in particular" (`:138`) | `def ForceConvergesBelowCritical` over the API | `def SubcriticalForceConvergence` over the API | **neither: promote it to a field `forceConvergence`.** It is a conclusion of the proposition, and the registered `I03.scaling` states its analogue as a field (`Contracts/V1/Scaling.lean:427-432`, same `𝓝[>] 0` / `𝓝 0` shape). Statement unchanged from both drafts — this is a shape change, not a new claim. |
| existential form | none | `scalingStatement : ∀ 𝔉 L ν hν, ∃ A, A.localization = L` | **B's shape, A-pinned.** `A.localization = L` is **vacuous**: `LocalizationAPI` is a `Prop`, so the equality holds by proof irrelevance and guards nothing. Pin the *data* instead: `∀ 𝔉 ν hν, ∀ place, … → Nonempty (ScalingAPI (𝔉.select ν hν) place)`. |
| T10/T13/T14 vocabulary | verbatim copies into `BlowupDensity.T10.Draft` / `T13.Draft` / `T14.Draft` | same | **both are now out of date** — see §3: `T01.torus_data` is registered, so the registered half must be imported, not copied. |

Nothing else in either draft is false. Both `eq:packetEscale` identities, `eq:packetFscale` at every endpoint, and `eq:packetHs` check out against `:141-158` (`ε^{-1}·ε^{3/2}=ε^{1/2}`; `ε^{-4}ε^{3}ε^{2}=ε`; `ε^{-3+3/p}·ε^{2/q}`; `ε^{-3}·ε^{3/2}·ε^{-s}·ε^{2}=ε^{1/2-s}` against the `ε^{1/2}` inhomogeneous term).

## 3. Decisions

**Base draft: B** (single `ScalingAPI`, `ClassicalSolutionT`-shaped PDE clause, explicit summability/integrability/`MemLp` guards). **Take from A**: `PlacementData` as a parameter, the literal `x₀+εK_*⊂B`, `0 < C_s`, `alphaT`, `scaledSourcePoint`. **Drop from both**: the two `rfl` pressure-constancy clauses and A's `energy_finite`. **Promote**: `forceConvergence` from `def` to field.

`research/T15/Spec.lean` = `structure PlacementData {ν} (P : PacketAPI ν)` (A's 17 fields unchanged) + `structure ScalingAPI {ν} (P : PacketImportAPI ν) (place : PlacementData P.toPacketAPI)` with:

| field | informal statement | paper |
|---|---|---|
| `localization` | a `T13.LocalizationAPI` witness | `:22-98`, used `:150-158` |
| `velocity_summable`, `pressure_summable` | the lattice series converges at every `t < T` and every `x` | `:120` |
| `force_summable` | the force lattice series converges at every `t` and `x` | `:120` |
| `velocity_singleCopy`, `pressure_singleCopy` | on `Q`, the sum equals its `n = 0` summand, `t < T` | `:120` |
| `force_singleCopy` | on `Q`, the force sum equals its `n = 0` summand, all `t` | `:120` |
| `force_mem` | `F_ε ∈ F_T` (`MemForceT`: smooth, unit-periodic, compact time support in `(0,∞)`) | `:108-111`, `02-preliminaries.tex:10` |
| `solution` | `∃ S : ClassicalSolutionT ν 0 F_ε T` with `S.velocity = U_ε^per`, `S.pressure = p_ε`: momentum at the **unchanged** `ν`, divergence free, zero initial data, periodic, smooth on `[0,T)`, `∫_{T³}p_ε = 0` | `:122-123`, `:141` |
| `pressureSlice_integrable` | the raw periodized pressure slices are Haar-integrable (makes the gauge honest) | `:123` |
| `unboundedSpeed` | `SpeedUnboundedAt T U_ε^per` | `:123`, `:142-143` |
| `energySlices_memLp` | velocity and full-gradient slices are honest `L²(T³)` functions on `(0,T)` | `:125-128` |
| `packetEnergyIdentity` | `‖U_ε‖_{L^∞(0,T;L²(T³))} = ε^{1/2}M` | `:125-126` |
| `packetDissipationIdentity` | `‖∇U_ε‖_{L²(0,T;L²(T³))} = ε^{1/2}D` | `:127-128` |
| `mixed_memLp` | honest `L^q_tL^p` Bochner paths for `F` (whole space) and `F_ε` (torus), every `1 ≤ p,q ≤ ∞` | `:129-133` |
| `packetMixedScaling` | `‖F_ε‖_{L^q(0,∞;L^p(T³))} = ε^{α(p,q)}‖F‖_{L^q(0,∞;L^p(ℝ³))}`, `α = −3+3/p+2/q` | `:129-133`, `:148-149` |
| `sobolevConst`, `sobolevConst_pos` | `C_s`, data, fixed before `ε`; `0 < C_s` on `[0,1]` | `:133-136` |
| `forceSobolev_memLp` | `F_ε` has an honest `L¹_tH^s_x` datum path, `0 ≤ s ≤ 1` | `:133-137` |
| `packetSobolevBound` | `‖F_ε‖_{L¹(0,∞;H^s(T³))} ≤ C_s(ε^{1/2}+ε^{1/2−s})`, `0 ≤ s ≤ 1` | `:133-137` |
| `forceConvergence` | `∀ s < 1/2` (negative included), `‖F_ε‖_{L¹_tH^s_x} → 0` as `ε ↓ 0` | `:138` |

Nothing here is absent from both drafts: every row is a draft field, except `forceConvergence`, which is both drafts' `def` promoted to a field because the paper states it as part of the proposition.

**Vocabulary / copy policy.** `T01.torus_data` is now registered (`verification/Contracts/V1/TorusData.lean`, namespace `BlowupDensity.Contracts.V1.TorusData`), so the spec **imports** it and **deletes** every copy of a registered name (`PeriodicFrequency`, `PeriodicTorus`, `periodicTorusMeasure`, `IsPeriodicSpatial`, `IsPeriodicOn`, `torusLift`, `periodicFourierCoeff`, `periodicFrequencyWeight`, `PeriodicSobolev`, `IsPeriodicDatum`, `periodicSobolevENorm`, `meanT`, `meanZeroPartT`, `IsMeanZeroT`, `periodicAngularFrequencySq`, `homogeneousDatumWeight`, `IsPeriodicHomogeneousDatum`, `periodicHomogeneousENorm`) — the amended `IsPeriodicDatum` is already in the registered file. Still **unregistered** and therefore still copied verbatim, in the delimited-block form of `research/T13/Spec.lean:18-21`, from `research/T10/Spec.lean`: `IsPeriodicSobolevPath`, `forceSobolevENormT`, `MemForceT`, `pressureMeanT`, `PressureGaugeT`, `normalizePressureT`, `ClassicalSolutionT`, `energyEssSupT`, `energyGradientT`; plus T13's `fundamentalCube`, `SupportedInBall`, `latticeVector`, `periodize`, `LocalizationAPI` (namespace `BlowupDensity.T13.Spec`, the source's own — **not** the brief's `T13.Draft`, resolving the mismatch both drafts flagged) and T14's `accumulatedForce`, `PacketEnergyAPI`, `PacketImportAPI`, `PacketImportFamily`. Every copied declaration gets a `rfl` bridge at registration; the TorusData half can be `rfl`-checked against `Contracts.V1.TorusData` **now**, in the spec lane.

**Constants.** One constant family, `sobolevConst : ℝ → ℝ`, a data field of a Type-valued structure, positive on `[0,1]`, chosen after `P` and `place` and before `ε` (that is the uniformity `:133-136` needs); T13's `C_{s,B}` stays inside `LocalizationAPI.localization`'s existential and is never re-exported here.

## 4. Next

**Spec lane** produces `research/T15/Spec.lean` (statement-only, elaborates under `lake env lean`) + a merged `COMPARISON.md` carrying both drafts' paper tables and both ambiguity lists, and the `Nonempty`-style `scalingStatement` with the data pin from §2.

**Needs a lemma** (union of A 1-12 and B 1-12, deduped): (1) definitional bridges from the three T15 rescalings to `Contracts.V1.scaledPacket`/`scaledPressure`/`scaledForce` for `ε ≠ 0`, and `rfl` bridges for every copied declaration; (2) construct compact `K_* ⊇ P.carrier ∪ pr_x (tsupport P.force)` (`PacketAPI.force_support` gives `HasCompactSupport`, so the projection is compact) and a chart ball with `closure ⊆ interior Q`; (3) scaled-support placement: each slice of `U_ε,P_ε,F_ε` is supported in `x₀+εK_* ⊆ B`; (4) local finiteness/summability of the lattice sum, its unit periodicity, and the single-copy equality on `Q`; (5) periodization commutes with every derivative in `navierStokesResidual`, including the nonlinear term and across cell boundaries; (6) pressure normalization: slice integrability, zero mean, unchanged gradient, unchanged residual; (7) transport of the packet PDE, divergence and quiet interval through the parabolic scaling at fixed viscosity, then through periodization, giving `zero_initial`; (8) `ClassicalSolutionT.sobolev`: a continuous `PeriodicSobolev m` datum path for every `m : ℕ`, plus `pressure_gradient`; (9) blow-up transport: map the packet witnesses near source time `1` into `t ↑ T` with spatial points inside `Q`; (10) exact energy change of variables, including `essSup`-vs-`IsLUB` for `M` and the endpoint-insensitive time interval, and the bridge from T13's `gradientENorm` to T10's `energyGradientT`; (11) mixed change of variables for all `1 ≤ p,q ≤ ∞` with both `∞` endpoints, producing the `MemLp` paths as well as the equality, and the `|Q| = 1` Haar-vs-Lebesgue identification; (12) time-integrated localization: apply `LocalizationAPI.localization` slice by slice with one `ε`-independent constant, combine Euclidean homogeneous scaling `ε^{-3/2-s}` with the `ε²` time change, and use `endpoint_zero`/`endpoint_one` at `s = 0,1`; (13) datum-path construction and strong measurability for `forceSobolev_memLp`; (14) negative-order monotonicity `forceSobolevENormT 1 s f ≤ forceSobolevENormT 1 0 f` for `s < 0` under the amended datum, then `forceConvergence` via the real-power limit through `ENNReal.ofReal` and `𝓝[>] 0`.

**Implementation candidates in `Paper1/`** (union of both COMPARISONs; none is imported, none discharges a T15 field on its own): `PeriodicScalingBounds.lean:19,40` `periodic_interpolation_of_endpoint_energies` / `periodic_interpolation_of_endpoint_bounds` (periodic `H⁰`–`H¹` interpolation once endpoints are supplied; explicitly not localization); `PeriodicPacketEndpointRates.lean:22,49,76,103` (scalar and vector whole-space endpoint rates at `s = 0,1`, inputs to the T13 transfer); `ScalingLimits.lean:11` `sobolev_error_tendsto_zero` (the real-power limit pattern — its exponents are the later **insertion** rates `ε^{1/2-s}+ε^{3/2-s}`, so T15 needs the simpler `ε^{1/2}+ε^{1/2-s}` companion, not a blind citation), `:27,29,35` `mixedExponent`/`mixedExponent_pos_iff`/`mixed_error_tendsto_zero` (exponent arithmetic), `energy_error_tendsto_zero` (T18, not T15). The single-copy lattice theorem, the T13-localization-to-`periodicSobolevENorm` bridge and the T10 datum-path witnesses have no local precedent.
