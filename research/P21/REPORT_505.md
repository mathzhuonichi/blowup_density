# Lane 505 — P21 Route B, B2

Status after cont_505: **B2 closed conditional only on the three torus norm
bridges.** Both analytic residuals and the final classical periodic enstrophy
inequality are kernel-checked. P6 / L21_H1 remains Partial.

The original ten-declaration partial report is preserved below as history;
the `cont` section supersedes its status and remaining-gap statements.

## 1. Statements

The new module is
`NSFormalization.Section3.T11.EnstrophyInequality`.

- `inhomogeneousEnergyIdentityT` differentiates the registered full H¹ norm
  of a classical periodic solution at every interior time. It retains the
  mean, with the derivative still expressed by Fourier H¹ pairings.
- `lintegral_convection_holder_632T` proves Haar 6–3–2 Hölder.
- `eLpNorm_three_interpolationT` proves Haar L³ interpolation between L²/L⁶.
- `young_quarticT`, `young_three_quartersT`, `young_two_factorsT` give
  explicit scalar absorption bounds.
- `weighted_cubic_assemblyT` proves scalar cubic absorption with Y=U+G,
  Z≤U+2G+L, retaining ordinary energy U. It is not itself a theorem about a
  classical solution. The coefficient is
  ((2C)^4/(ν/2)^3+(1+ν)), with force coefficient (1+2/ν).
- `gradient_six_le_laplacian_twoT` removes the mean-zero restriction from
  the gradient estimate by subtracting the mean before applying T12.
- `convection_interpolationT` combines Hölder and gradient interpolation;
  its velocity L⁶ factor remains explicit.
- `velocity_six_le_localized_gradientT` supplies the elementary cutoff route
  to the velocity Sobolev embedding.

No critical smallness or absorbed T20 estimate is used. The canonical
T10.ClassicalSolutionT is used; the contract structure is a distinct type
with existing fieldwise conversions in Bindings.TorusLocalTheory.
No contract extension is asserted.

The periodic normalization is κ=1, since the Fourier weight is 1+|2πk|².
P6_SPLIT.md records the exact three intended normalized torus counterparts
of B1's B0 interfaces. The ten delivered declarations assume none of them;
there is no final consumer theorem yet. No B0 module was created or restated.

## 2. Files

- `formalization/NSFormalization/Section3/T11/EnstrophyInequality.lean`
- `research/P21/probes/b2_closes.lean`: all ten signatures plus Young and a
  nonzero-ordinary-energy scalar instantiation.
- `research/P21/axioms_b2.lean`: prints all ten axiom sets and runs TestSupport.
- `research/P21/ATTEMPTS_B2.md`: exact remaining statements, attempted routes,
  and actual compiler error text.
- `research/P21/P6_SPLIT.md`: B2 status and consumption limits.
- `formalization/blueprint/entrypoints.json`: module registration.
- `formalization/blueprint/AXIOM_AUDIT.json`: refreshed required article audit.
- `formalization/blueprint/DEPENDENCY_GRAPH.md`: required regeneration.

No proof_graph.json, contract, binding, registry, or article coverage marker
was edited. Graph regeneration reflects the inherited C35_FULL/G36_FULL
closures and current audit counts; those are not new B2 closure claims.
PDFs generated by the paper gate were restored because no TeX changed.
The pre-existing untracked lane brief is untouched.

## 3. Gaps with error text

**A. Velocity embedding.** Localization reduces the missing
`velocity_six_le_gradient_twoT` to:

```lean
eLpNorm (gradientTensor (cutoffMul z)) 2 volume ≤
  343 * (3 * (ENNReal.ofReal cutoffGradBound * periodicLpENorm 2 z +
    periodicLpENorm 2 (gradientTensor z)))
```

The actual probe ends with `error: unsolved goals` and exactly this goal.
T12's mean-zero gradient embedding does not directly bound velocity;
the independent cutoff/compact-support route closes through the localized
whole-space Sobolev estimate. Its remaining product/lattice estimate is not
claimed impossible or absent from Mathlib.

**B. Physical RHS conversion.** The differentiated registered norm is proved,
but rewriting its Fourier H¹ pairings as
`-2ν(G+L)+2N+2P-2Q` leaves `error: unsolved goals`: equality of that physical
RHS with
`-2ν(torusGradientNormAt 1 w.velocity t)^2
 + 2 torusRealPairing G F - 2 torusRealPairing G N`.
ATTEMPTS_B2.md contains the full Lean statement and complete residual output.
The Fourier-weight split, angular Parseval, ordinary convection cancellation,
force Cauchy–Schwarz, and final norm conversion still need assembly.
The direct periodic integration-by-parts alternative is recorded, not claimed
implemented.

Therefore `enstrophy_differentialT` and
`enstrophy_differential_on_IccT` are absent, not declared with assumed
nonlinear/differential estimates. B3/B4 must not consume this partial as the
finished B2 inequality. There is no initial-time or maximal-endpoint assertion.

## 4. Commands and results

Lean commands used the sourced scripts/lean-env.sh environment,
verification Lake configuration, and LEAN_NUM_THREADS=6.

- `lake build NSFormalization.Section3.T11.EnstrophyInequality`: passed
  (10656 jobs).
- Direct Lean run of `probes/b2_closes.lean`: passed.
- Direct Lean run of `axioms_b2.lean`: passed; all ten declarations print
  exactly `[propext, Classical.choice, Quot.sound]` and pass TestSupport.
- `python3 experiments/audit_article_axioms.py --build --output-dir tmp/article-audit --workers 2`:
  passed before make check; 69 declarations, 27 article entries, zero
  forbidden-axiom results. Reviewed report copied to AXIOM_AUDIT.json.
- `python3 experiments/check_formalization_plan.py`: passed and regenerated
  the graph.
- `make check`: **failed in one inherited policy regression**. Packaging and
  the 32-contract registry checks pass. Of 11 policy tests, the test
  `test_recoloring_a_missing_clause_cannot_hide_whole_statement_partial`
  fails with `AssertionError: AssertionError not raised`.
  It assumes C35_FULL is Partial, changes it to Closed, and expects a coverage
  mismatch. The inherited proof_graph.json already has C35_FULL Closed and
  the refreshed article audit agrees, so the mutation changes no coverage.
  This was verified against `git show HEAD:formalization/blueprint/proof_graph.json`;
  B2 did not edit that graph. The unrelated test was not changed in this lane.
- `make test`: passed (11015 build/test jobs).
- `make test-mutations`: passed; refactor accepted and admitted proof,
  extra axiom, weakened hypothesis rejected.
- `make paper`: passed; both PDF logs clean and reader checks pass.
- `git diff --check`: passed.

Logs are under ignored `tmp/b2-*.log` and `tmp/article-audit/`.
Closed proof steps were committed separately. No push, merge, rebase,
external message, or sub-agent launch was performed.

## cont

### 1. What was proved

Residual A is closed by the cutoff product rule, the 343-cell covering and
L² triangle inequality. The mean-retaining velocity estimate is
`‖z‖₆ ≤ Cv (‖z‖₂ + ‖∇z‖₂)`, with
`Cv = 3*343*A05.gradientL6Const*(1+cutoffGradBound)`.

Residual B is closed by splitting the H¹ Fourier pairing into ordinary and
Laplacian pairings, Parseval for dissipation, and periodic convection
cancellation. Pressure cancellation is inherited from T11's unconditional
energy identity. Physical force Cauchy–Schwarz is also proved.

The final theorems `enstrophy_differentialT` and
`enstrophy_differential_on_IccT` give

```
Y'(t) + ν Z(t) ≤ Cν (1+Y(t))^3 + Cν lTwoSqT(f(t))
C = √2 * Cv * √Csix
Cν = (2*C)^4/(ν/2)^3 + (1+ν) + (1+2/ν),   c=κ=1.
```

Here Y and Z are exactly the squared `toReal` registered periodic H¹/H²
norms, and the mean is retained. The canonical solution is
`T10.ClassicalSolutionT ν a f T`; `f ∈ forceClassT` is definitionally
`T10.MemForceT f`. The compact-interval theorem has `0<r`, `s<T`, hOne on
all interior times and hTwo/hGradient on Icc r s, matching B1's interface.

### 2. What is in Lean

`Section3/T11/EnstrophyInequality.lean` now has 30 proved theorems (20 new
in this continuation). The main new exports are:

- `cutoff_gradient_two_leT`, `velocity_six_le_gradient_twoT`;
- `torusRealPairing_one_eqT`, `periodicPairing_convection_zeroT`,
  `torusGradientNormAt_one_sqT`, `weightedEnergyIdentityT`;
- `abs_pairing_carrier_leT`, `convection_bound_of_norm_bridgesT`,
  `convection_boundT`;
- `enstrophy_differential_of_norm_bridgesT`, `enstrophy_differentialT`,
  `enstrophy_differential_on_IccT`.

`research/P21/probes/b2_closes.lean` constructs an explicit zero classical
solution with horizon 2 and applies the final theorem on [1/2,1], proving
all bridge hypotheses. Its negative mutation removes the negative
Laplacian dissipation from the energy-balance premise: the same scalar
assembly application is rejected under `#guard_msgs`, and an independent
counterexample proves that its proposed conclusion 3≤2 is false. Removing
the positive dissipation term from the final conclusion would weaken the
inequality, so cannot be a semantic negative test.

`research/P21/axioms_b2.lean` checks all 30 exported theorems: each prints
exactly the standard axiom set `[propext, Classical.choice, Quot.sound]`
(some output wraps across lines) and passes TestSupport. No custom axiom,
admission, assumed differential/nonlinear bound or extra heartbeat budget
was introduced. Closed proof steps have separate `[505-P21-B2]` commits.
`ATTEMPTS_B2.md` preserves failed paths and records the closed residuals;
`P6_SPLIT.md` gives the exact final interface and B4 conversions.

### 3. Gaps and scope

There are **no remaining B2 analytic residuals**. The final theorem is
conditional only on the three requested norm bridges. No lane-503 B0 module
was imported or restated. B4 must reconcile its component-integral energies
with T20 `lTwoSqT`, `gradientSqT`, and `laplacianSqT`; in particular
`periodicHessianEnergy z = laplacianSqT z` is a periodic Parseval obligation,
not a definitional equality or a theorem claimed by this lane.

B3/B4/B5 remain responsible for the ODE/endpoint/lifespan/restart work;
P6 / L21_H1 remains Partial. No initial-time or maximal-endpoint derivative
is asserted. No contract, binding, registry, proof-graph coverage or article
closure marker was changed. The inherited untracked lane brief is untouched.

### 4. Commands and results

All Lean commands sourced `scripts/lean-env.sh`, ran from `verification/`,
and used `LEAN_NUM_THREADS=6`.

- `lake build NSFormalization.Section3.T11.EnstrophyInequality`: passed,
  10656 jobs.
- `lake env lean ../research/P21/probes/b2_closes.lean`: passed, including
  the explicit solution and the expected failed mutation.
- `lake env lean ../research/P21/axioms_b2.lean`: passed; 30 standard-axiom
  checks, including both final theorems.
- `python3 experiments/audit_article_axioms.py --build --output-dir tmp/article-audit --workers 2`:
  passed **before make check**; 69 declarations, 27 article entries,
  zero forbidden-axiom results. Refreshed report copied to `AXIOM_AUDIT.json`.
- `python3 experiments/check_formalization_plan.py`: passed; regeneration
  produces no graph diff.
- `make check`: failed **only** in
  `test_recoloring_a_missing_clause_cannot_hide_whole_statement_partial`
  with `AssertionError: AssertionError not raised`. The other ten policy
  tests pass, as do packaging and `check_contracts.py --summary` (32 contracts).
  This is the known C35_FULL fixture issue fixed by lane 509 (#465) on
  erenup/core. The test was not edited, and core was not merged.
- `make test`: passed (11015 build/test jobs).
- `make test-mutations`: passed; refactor accepted and admitted proof,
  extra axiom, weakened hypothesis rejected.
- `make paper`: passed; both PDF logs and reader checks clean. No PDF diff.
- `git diff --check`: passed.

Logs: ignored `tmp/b2-cont-*.log` and `tmp/article-audit/`.
No push, merge, rebase, external message or sub-agent launch was performed.
