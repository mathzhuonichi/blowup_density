# T12 (`lem:calculus` + `lem:critical-embeddings`, torus halves; `appendix-a-local-theory.tex:7-27`, `appendix-b-embeddings.tex:8-38,85-110`, `03-torus.tex:467-503`) — reconciliation of drafts A (lane 269) and B (lane 270)

Lead: erenup, 2026-09-17 (UTC 21:26). Drafts: `.claude/worktrees/269-SPEC-t12-draft-a/research/T12/DraftA.lean`, `.claude/worktrees/270-SPEC-t12-draft-b/research/T12/DraftB.lean` (both elaborate). Both predate T10 lead amendment 1 (`research/T10/RECONCILIATION.md` §5).

## 0. Lead review (2026-09-17 21:26Z)
Drafted by an Opus subagent from the two blind drafts; reviewed and **approved** by the lead. House-style answer: Section 3 APIs that carry constants are **Type-valued** structures with the constants as data fields (exactly `TameProductAPI`/`GradientL6API`); `Prop`-valued APIs (T10, T13) are only for constant-free statements. The two added fields are accepted: `homogeneous_le_sobolev` holds with constant `1` under T10's weights (`(4π²|k|²)^{s/2} ≤ (1+4π²|k|²)^{s/2}`, zero mode dropped), and `lambda_exists` is an honest existence obligation for the proof lane, not a placeholder.

## 1. Agreement
Same seven target inequalities, same order, same names for six of the seven fields (`tameProduct`, `boundedRepresentative`, `velocityCriticalL3`, `gradientLambdaCriticalL3`, `hTwo_le_laplacian`, `spectralGap`; A `gradientL6` / B `gradientLSix`). Both: physical layer = unit-periodic fields on ℝ³, analytic layer = weighted `lp (Fin 3 → ℤ) 2` assembled with `WithLp 2`/`PiLp 2`; inhomogeneous weight `(1+4π²|k|²)^(s/2)`, homogeneous weight `|2πk|^s` with the zero mode set to `0`; all norms `ℝ≥0∞`, defined as `⨅` over realizing data (never `.toReal`); all `L^p` norms are `eLpNorm (torusLift z) p` against **normalized Haar on `UnitAddTorus (Fin 3)`** (not the cube) — this matches `01-introduction.tex:89,104`, where `∫_{T³}` is the unit-volume integral. Both state `eq:Rproduct` for **scalars** and leave the vector/tensor consequence (`appendix-a-local-theory.tex:50`) as a derived lemma. Both put mean-zero on the four homogeneous/Laplacian clauses and **not** on `tameProduct` / `boundedRepresentative` — correct: `appendix-a-local-theory.tex:8` says "either domain" with no mean-zero, `appendix-b-embeddings.tex:21-22,26-27` says the mean-zero condition is essential on the torus. Both spell `Λ = (-Δ)^{1/2}` coefficientwise by the multiplier `2π|k|` (`appendix-b-embeddings.tex:8-9`), not as a `Real.rpow` of a classical operator. Both split constants into a separate record, both with the same (mistaken) justification.

## 2. Rulings
| clause | A | B | ruling |
|---|---|---|---|
| API packaging | `Constants` record + `API (K) : Prop` | `Constants` record (positivity inside) + `API (K) : Prop` | **neither**: one **Type-valued** `structure MeanZeroSobolevCalculusAPI where Cproduct : ℕ → ℝ; Cproduct_pos; …; tameProduct; …`, exactly `Contracts/V1/TameProduct.lean:215` and `Contracts/V1/GradientL6.lean:118`. Section 4 packages constants as **data fields of a Type-valued structure** (or a structure parameter, `Contracts/V2/GradientL6.lean:41`), never `∃ C`; the drafts' split only exists to satisfy a `Prop` requirement Section 4 does not impose. Ledger `Prop` form, if wanted: `Nonempty MeanZeroSobolevCalculusAPI` (T14 precedent). |
| constant names | `Cproduct, Cinfty, CcriticalHalf, CcriticalThreeHalves, Csix, CHtwo, Cgap` | `product, linfty, criticalHalf, criticalThreeHalves, gradientSix, hTwo, gap` | **A** — `Csix`/`Cinfty` are token-identical to the registered `GradientL6API.Csix`, `BoundedRepresentativeAPI.Cinfty`. |
| `Cgap` positivity | `∀ s, 0 ≤ s → 0 < Cgap s` | `∀ s, 0 < gap s` | **A** (guard matches the field's own `0 ≤ s`). |
| datum predicate | coefficient equation only — **no periodicity, no integrability** | `UnitPeriods ∧ MemLp (torusLift z) 2 ∧ equation` | **T10's amended `IsPeriodicDatum`** (`IsPeriodicSpatial ∧ Integrable (torusLift z) … ∧ ∀ i k`). A's definition is the defect of T10 amendment 1: `periodicFourierCoeff` is a Bochner integral, so for the periodization of `x ↦ 1/x₁` every coefficient is junk `0`, `A = 0` is a datum, and A's `periodicSobolevENorm` is **`0` instead of `⊤`** — the norms are not total. A's *fields* survive only because each carries a `MemPeriodic*` hypothesis; the definitions must not ship. |
| frequency/weight/lift/coeff vocabulary | own copies (imports `NSFormalization.Paper1`) | own copies (imports `NavierStokes.PeriodicIntegration`) | **T10's**: `PeriodicFrequency`, `PeriodicTorus`, `periodicTorusMeasure`, `torusLift`, `periodicFourierCoeff`, `periodicFrequencyWeight`, `periodicAngularFrequencySq`, `homogeneousDatumWeight`, `PeriodicSobolev`, `periodicSobolevENorm`, `periodicHomogeneousENorm`, `IsPeriodicHomogeneousDatum`. B's `(2π)^2·Σk²` and `sqrt(·)^s` are equal but not the T10 spelling; neither draft may import a `formalization/` module (contract import policy). |
| mean-zero | `∀ i, v̂_i(0) = 0` | same + physical `meanZeroPart` | **T10's `IsMeanZeroT z : meanT z = 0`** (physical Haar mean) in every hypothesis; the coefficient form is T10's `meanZero_datum` bridge, not a second definition. Reason: T20 produces `v = u − m` physically (`03-torus.tex:395-411`). |
| scalar layer | `IsPeriodicScalarSobolevDatum`, `periodicScalarSobolevENorm` | same names | **keep, as genuinely new objects** — T10 has no scalar layer and `eq:Rproduct` is scalar. Spell them exactly like T10's vector pair (same weight, same `Integrable` conjunct, same `⨅`); registration folds them into `T01.torus_data`. |
| `L^p` on the torus | `torusScalarENorm` / `torusVectorENorm` (two defs) | `periodicLpENorm {E} (p) (z : Space → E)` | **B** (one generic def; also serves `gradientTensor v : Space → WithLp 2 (Fin 3 → Space)`). |
| `∇`, `Δ` | Fourier-multiplier witness predicates + `⨅` over witnesses | classical `fderiv` `gradientTensor` / `laplacian` on `SmoothPeriodicVector` | **B**, respelled as `Contracts/V1/GradientL6.lean:78-98` (`lift`, `gradientTensor`, `laplacian` via `spatialGradient`/`spatialLaplacian`) — token-identical to the registered A05 shape, and `appendix-b-embeddings.tex:34-37` says the derivative displays are asserted for "all the smooth `H^∞` fields used in the article". A's `⨅`-over-witnesses puts the **infimum on the left-hand side**, which silently weakens three inequalities unless the (unproved) `L¹`-Fourier-uniqueness lemma is available. |
| `Λ` witness | `IsPeriodicLambda v w` with `MemPeriodicVector w`, norm `⨅` | `IsPeriodicLambda v Lv` with `SmoothPeriodicVector Lv`, `Lv` a field argument | **B's witness-as-argument**, **plus** an added existence clause (below): as B writes it the field is *vacuous* for any `v` admitting no `Lv`; A avoids that only by the `⊤`-valued infimum. |
| `H² ↪ L^∞` formulation | ess-sup `eLpNorm … ⊤` | ess-sup `periodicLpENorm ⊤` | **both agree: ess-sup**. The pointwise `supNorm_le` half of `BoundedRepresentativeAPI` is *not* added here (neither draft has it); it is listed in §4 as the derived interface T18 will need, exactly as A03 registered both. |
| `velocityCriticalL3` hypotheses | three separate (`MemPeriodicVector`, `IsMeanZero`, `≠ ⊤`) | bundled `MemPeriodicHomogeneous (1/2) v` | **B** — `appendix-b-embeddings.tex:20-22,26-27` quantifies literally over "mean-zero periodic distributions with finite displayed right-hand side". |
| `gradientLSix` mean-zero | kept, noted redundant | kept | **keep** (paper-literal, `appendix-b-embeddings.tex:26-27`). Flag: it *is* redundant — both sides annihilate constants, so the estimate follows for every smooth periodic `v`. Not an error, just not sharp. |
| `hTwo_le_laplacian` / `spectralGap` mean-zero | present | present | **keep — essential**. Dropping it makes both fields **false**: `v ≡ c ≠ 0` gives `‖v‖_{H²} = |c| > 0 = ‖Δv‖₂`. |
| `spectralGap` direction | `H^s ≤ C_s Ḣ^s` only | same | **both incomplete**: `02-preliminaries.tex:52-54` asserts *equivalence*. Add the trivial converse as a second field (§3). |
| `gradientLSix` field name | `gradientL6` | `gradientLSix` | **B** (matches `GradientL6API.gradientLSix`). |
| vector `eq:Rproduct` | scalar only | scalar only | **keep scalar** (the literal display). The A03-style bridge (`tameProductVector`, `scalarSobolevENorm_component_le`, `sobolevENorm_le_sum_components`) goes to §4 as a required derived interface — `Contracts/V1/TameProduct.lean:259-264` warns that without the component pair the scalar and vector carriers are unrelated. |

Nothing in either draft is false as stated (A is saved by its `MemPeriodic*` hypotheses); the two defects are A's non-total norm definitions and B's potentially-vacuous `Λ` clause.

## 3. Decisions
**Base draft: B.** Take from A: the constant names and `Cgap_pos`'s guard. Take from T10: all data-layer vocabulary.

Vocabulary policy — **identical to T13** (`research/T13/Spec.lean:18-21`): `research/` files are not Lean modules, so `research/T12/Spec.lean` imports `Contracts.V1.Data`, `Contracts.V1.GradientL6`, `Mathlib.Analysis.Fourier.AddCircleMulti` and **copies the needed T10 declarations verbatim, unchanged, inside `namespace BlowupDensity.T10.Draft`**, inside an explicitly delimited block, in the post-amendment form. Copy: `PeriodicFrequency`, `PeriodicTorus`, the two `local instance`s, `periodicTorusMeasure`, `IsPeriodicSpatial`, `torusLift`, `periodicFourierCoeff`, `periodicFrequencyWeight`, `PeriodicScalarData`, `PeriodicVectorData`, `realPeriodicSubmodule`, `PeriodicSobolev`, `IsPeriodicDatum`, `periodicSobolevENorm`, `meanT`, `IsMeanZeroT`, `periodicAngularFrequencySq`, `homogeneousDatumWeight`, `IsPeriodicHomogeneousDatum`, `periodicHomogeneousENorm`. T12-new defs go in `BlowupDensity.T12.Draft`.

New T12 objects (not in T10): `IsPeriodicScalarDatum s f A` / `periodicScalarSobolevENorm` (scalar mirror of T10's pair, `01-introduction.tex:83-84`); `periodicLpENorm p z := eLpNorm (torusLift z) p periodicTorusMeasure` (`:104`); `lift`/`gradientTensor`/`laplacian` copied from `Contracts/V1/GradientL6.lean:78-98`; `SmoothPeriodicT v := ContDiff ℝ ∞ v ∧ IsPeriodicSpatial v` (`appendix-b-embeddings.tex:34-37`); `MemPeriodicHmScalar m f` / `MemPeriodicHmVector m v` / `MemPeriodicHomogeneous s v` (periodicity + `MemLp (torusLift ·) 2 periodicTorusMeasure` + the relevant `≠ ⊤`, mean-zero for the last); `IsPeriodicLambda v Lv` (multiplier `2π|k|`, `appendix-b-embeddings.tex:8-9`).

`structure MeanZeroSobolevCalculusAPI where` — constants first (`Cproduct : ℕ → ℝ`, `Cproduct_pos`; `Cinfty`, `Cinfty_pos`; `CcriticalHalf`, `_pos`; `CcriticalThreeHalves`, `_pos`; `Csix`, `_pos`; `CHtwo`, `_pos`; `Cgap : ℝ → ℝ`, `Cgap_pos : ∀ s, 0 ≤ s → 0 < Cgap s`), then nine clauses:

| field | informal statement | paper |
|---|---|---|
| `tameProduct` | `∀ m ≥ 2`, scalars `a b ∈ H^m(T³)`: `‖ab‖_{H^m} ≤ Cproduct m (‖a‖_{H²}‖b‖_{H^m} + ‖b‖_{H²}‖a‖_{H^m})` | `appendix-a-local-theory.tex:9-11` |
| `boundedRepresentative` | `v ∈ H²(T³;ℝ³)`: `‖v‖_{L^∞(T³)} ≤ Cinfty ‖v‖_{H²}`, ess-sup, no mean-zero | `appendix-a-local-theory.tex:12` |
| `velocityCriticalL3` | `MemPeriodicHomogeneous (1/2) v`: `‖v‖_{L³(T³)} ≤ CcriticalHalf ‖v‖_{Ḣ^{1/2}}` | `appendix-b-embeddings.tex:20-22,29` |
| `lambda_exists` **(added)** | `SmoothPeriodicT v → ∃ Lv, IsPeriodicLambda v Lv` — without it the next field is vacuous; the paper applies `Λv` to exactly these fields | `appendix-b-embeddings.tex:8-9,97` |
| `gradientLambdaCriticalL3` | `SmoothPeriodicT v`, `MemPeriodicHomogeneous (3/2) v`, `IsPeriodicLambda v Lv`: `‖∇v‖_{L³} + ‖Lv‖_{L³} ≤ CcriticalThreeHalves ‖v‖_{Ḣ^{3/2}}` (one display, one constant) | `appendix-b-embeddings.tex:30-31`, `appendix-a-local-theory.tex:22-26` |
| `gradientLSix` | `SmoothPeriodicT v`, `IsMeanZeroT v`: `‖∇v‖_{L⁶} ≤ Csix ‖Δv‖_{L²}` | `appendix-b-embeddings.tex:32`; used `03-torus.tex:475-477` |
| `hTwo_le_laplacian` | `SmoothPeriodicT v`, `IsMeanZeroT v`: `‖v‖_{H²} ≤ CHtwo ‖Δv‖_{L²}` | `03-torus.tex:492-493` |
| `spectralGap` | `∀ s ≥ 0`, `MemPeriodicHomogeneous s v`: `‖v‖_{H^s} ≤ Cgap s · ‖v‖_{Ḣ^s}` | `02-preliminaries.tex:52-54`, `appendix-b-embeddings.tex:85-90` |
| `homogeneous_le_sobolev` **(added)** | `∀ s ≥ 0`, `MemPeriodicHomogeneous s v`: `‖v‖_{Ḣ^s} ≤ ‖v‖_{H^s}` — the paper says the two norms are *equivalent*, and only this direction makes `spectralGap` an equivalence | `02-preliminaries.tex:52-54` |

Constants handling: one universal constant per display, all fixed before every quantifier (`appendix-b-embeddings.tex:109-110`); no constant is shared across displays (equivalent after a max, and dependencies stay visible); no finite-mode constant may discharge `velocityCriticalL3` (`SECTION3_PLAN.md` §7).

## 4. Next
Spec lane (after T10's, which it copies): `research/T12/Spec.lean` (statement-only, elaborates under `lake env lean`) + merged `COMPARISON.md` carrying both drafts' paper tables and both ambiguity lists. Then the proof lanes (M, astra; `velocityCriticalL3` is the analytic core) and registration under the `T01` bucket.

Needs a lemma (union of both drafts, deduped): (1) T10 bridges for every copied declaration, plus `rfl` bridges once `T01.torus_data` is registered; (2) `L¹`-Fourier uniqueness on the unit-volume torus (two integrable periodic fields with equal coefficients agree a.e.) and the reconstruction/measurability bridges `physical ↔ torusLift ↔ eLpNorm`/`MemLp`; (3) vector Parseval: the `WithLp 2` datum norm equals `01-introduction.tex:103`'s sum of squared component norms (T10's `parseval_forward`/`parseval_backward` are the scalar input); (4) Fourier-multiplier identities for `∂_j`, `Λ`, `Δ` on smooth periodic fields, including zero-mode preservation, and existence of the `Λ` representative (`lambda_exists`); (5) the spectral-gap weight inequality `(1+4π²|k|²)^a ≤ (1+(2π)^{-2})^a (2π|k|)^{2a}` for `k ≠ 0` (`appendix-b-embeddings.tex:87-88`) summed in `lp`, uniformly in the field and its frequency support; (6) `‖Δv‖_{L²} = ‖v‖_{Ḣ²}` and the order-two Poincaré/spectral identity ⇒ `hTwo_le_laplacian`; (7) the Hessian–Laplacian Parseval identity (torus counterpart of `GradientL6API.hessianLaplacianIdentity`) plus mean-zero `H¹ ↪ L⁶` ⇒ `gradientLSix`; (8) torus Fourier convolution + weighted convolution/Young estimate + closure of `H^m(T³)` under products ⇒ `tameProduct`; (9) weighted `ℓ² → ℓ¹` at order two, i.e. `Σ_k (1+4π²|k|²)^{-2} < ∞` by dyadic shells (`appendix-a-local-theory.tex:44-47`), plus absolute Fourier convergence ⇒ `boundedRepresentative` without a frequency cutoff; (10) a **genuinely uniform** mean-zero `Ḣ^{1/2}(T³) ↪ L³(T³)` and its `a = 1` companion — `PeriodicFiniteCriticalInterface.lean`, `PeriodicCriticalBridge.lean`, `PeriodicCompactSobolevL6.lean` all retain a finite-mode or compact-periodization loss and cannot discharge it; (11) componentwise assembly: scalar↔vector component norm comparison and the `a = 1/2` embedding applied to each `∂_j v`, yielding the A03-style derived interfaces `tameProductVector` / `scalarSobolevENorm_component_le` / `sobolevENorm_le_sum_components` and the pointwise `supNorm_le` half of `H² ↪ L^∞` that T18 consumes.
