# T12 canonical-module attempts

No Lean elaboration, module build, or probe attempt failed while constructing
the canonical module.  Consequently there is no error diagnostic to preserve.

The first module build replayed pre-existing upstream linter warnings, then
built `NSFormalization.Section3.T12.MeanZeroCalculus` successfully.  Those
warnings were not errors and did not originate in any file added by this lane.
