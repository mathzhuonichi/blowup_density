Contract-registration lane — strict statement fidelity (hard rule 2), as in `research/C01/REVIEW_V3_CONTRACT.md`:
1. `Contracts/V4/EnergyAbsorptionPartial.lean`: the field `enstrophyIdentity` and any restated defs (`advectionWork`, `laplacianSq`, `laplacian`) must be **byte-identical** to `research/C01/Spec.lean` (diff mechanically, cite lines); mathematics = the enstrophy identity of `04-whole-space.tex:105-113` (eq:RH1's derivation), sign by sign; slack hypotheses disclosed.
2. `git diff origin/erenup/integration -- verification/Contracts/V1 verification/Contracts/V2 verification/Contracts/V3 verification/Tests/EnergyAbsorptionPartial*.lean` must be empty except the new V4 test; imports of the contract only `Contracts.*`/Mathlib; `experiments/check_contracts.py` accepts the `/V4/` path (say how the version-path rule is satisfied).
3. Bindings: `{ energyAbsorptionPartialV3 with … }`, the single non-`rfl` `gradientSq` bridge reused from V2, `rfl` bridges for the restated defs (which needed a new bridge? `laplacianSq` = `∫‖Δz‖²` vs the formalization's raw form?), `energyAbsorptionPartialV3_of_v4 := rfl`.
4. Registry entry (`version: 4`, honest scope: excluded `h2TimeIntegral`, `enstrophyIntegralBound`, `sobolevTwoFourier`, …), no `\uXXXX`, additions only; `check_work_queue.py` consistent.
5. Negative: mutate the contract statement (e.g. `2 * ν` → `ν`, or drop the force pairing) in a scratch copy and confirm the binding no longer typechecks; non-vacuity via `maximalPartial_ofA02 (A04.zeroSol …)`.
