SIMP/tester lane — the key check is STATEMENT INVARIANCE (as in `research/MAINT/REVIEW_SIMP_155.md`):
1. Mechanically extract every `theorem`/`def` signature in the four touched modules (`Paper1/ScalarEnergy.lean`, `Section4/A01/ConstructorDivergence.lean`, `Section4/A01/SliceWiring.lean`, `Section4/C01/EnergyBounds.lean`) at `origin/erenup/integration` vs HEAD; report `changed = 0` for every pre-existing declaration that has a consumer in `formalization/`, `verification/` or `research/` (grep each name), and list removed/renamed ones with proof that no consumer exists (`velocitySliceSmoothL2` retirement: who used it?).
2. `Paper1/ScalarEnergy.lean` is outside Section 4 and pre-dates this project's lanes: confirm `sqrt_energy_le_primitive`'s statement is byte-identical and that `C01.sqrt_energy_le_primitive'` (consumed by the registered C01 V3 binding `l2Bound`) is byte-identical; `make test` must pass (the reviewer must run `scripts/gates.sh` with all four modules).
3. `ConstructorDivergence` was rewritten via a "direct vendor divergence bridge": is the new proof still deriving divergence-freeness from the cylinder pair's `divergenceFreeSpace` clause (cite the vendor lemma), or from an extra hypothesis? Diff the statements of `divergence_ae_of_cylinder` / `divergence_of_cylinder_pointwise_of_contDiff` before/after.
4. Build the entire Section 4 tree once (`${=MODS}` word-split in zsh — see LESSONS) and paste the tail.
5. `research/MAINT/CLOSURE_PLAN.md`: is the A01 V2 / C01 V4 bundle plan consistent with what is now registered (C01 V4 lane 177 registered `enstrophyIdentity` today — check `origin/erenup/177-C01-v4-contract`)?
