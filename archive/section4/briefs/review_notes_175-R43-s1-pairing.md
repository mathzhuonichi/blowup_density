1. `hcrit : CriticalDatumPath w hf` bundles several assumptions (homogeneous half-order datum paths of `u`, smoothness, a lifted momentum equation, Fourier symbols, Leray compatibility). Audit each field: is it a genuine open obligation (G3/G4 of `research/R43/COMPARISON.md` §4: integer data ⇒ half-order homogeneous data; the momentum equation transported to the homogeneous datum level) or does any field already encode the conclusion (e.g. a field asserting the derivative of `y²` or the pairing identities themselves)? State plainly whether `rcritical1_of_trilinear` would be vacuous or trivial under `hcrit`.
2. `htri : CriticalTrilinearEstimate` must be exactly `|⟪(u·∇)u, Λu⟫| ≤ C₀·y·z²` in the datum language (`04-whole-space.tex:98`), nothing weaker/stronger.
3. The pairing identities `critical_laplacian_pairing` (`⟪Δu, Λu⟫ = −z²`), `critical_pressure_pairing` (`= 0` from divergence-freeness — which tree lemma), `critical_force_pairing` (Cauchy–Schwarz): check they are proved at datum level against the tree's Fourier symbols (`D01/LeraySymbol.lean`, `LaplacianPairing.lean`, `HalfOrder.lean`), not assumed via `hcrit`.
4. The scalar conclusion `E'/2 + (ν − C₀y)z² ≤ by` must be the `henergy` hypothesis of `R43.Pieces.criticalNormBound_radius` / `Paper1.critical_norm_bound` token for token (compose them in a probe).
5. Non-vacuity on `zeroSol`: does the zero example instantiate `hcrit` with a genuine (zero) datum path — i.e. is `CriticalDatumPath` satisfiable at all? Try to instantiate it for a nonzero field if cheap; if the structure is unsatisfiable for any non-flat solution, that is a REJECT-level finding.
