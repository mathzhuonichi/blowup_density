1. Circularity: `pressureOfVelocity ν f u` is the radial potential of `G = R − ∂ₜu` with `R = f − (u·∇)u + νΔu` — it uses the *time derivative of the candidate velocity*. For the constructor this is legitimate only if (i) `∂ₜu` exists pointwise for the candidate (B1's c3 gives it) and (ii) `momentum_of_projected` does not assume the momentum equation it proves. Trace the hypotheses of `momentum_of_projected` and `pressureGradient_pressureOfVelocity_lerayComplement` back to the *projected* equation (the Duhamel/mild form `∂ₜu = P R`) and confirm nothing assumes `∇p` or the classical residual. State plainly whether the construction is circular.
2. `HasSymmetricJacobian G(t,·)` (curl-free) is a hypothesis of `pressureGradient_pressureOfVelocity`. Is it discharged anywhere from the projected equation (`(I − P) R` is a gradient: check `Section4/D01/OrderZeroCurl.lean`, `LerayDatum.lean`, `LerayLowering.lean` for a curl-free / gradient characterization of `lerayComplement`)? If not, it is an open obligation of the constructor — say so and give its exact statement.
3. Gauge: `p(t,0) = 0` (basepoint gauge) vs `A02.pressure_normalization` (`Section4/A02/Restrict.lean`, `Patch.lean`) — compatible? If the A02 normalization is different (e.g. spatial mean or decay at infinity), record the exact mismatch.
4. `pressure_smooth_of_velocity_smooth` takes joint `ContDiff` of `G`; check that this hypothesis is implied by the c3 hypothesis (`ContDiffOn ℝ ∞ velocity (Ico 0 T ×ˢ univ)`) plus `MemForceR f`, or whether it silently requires more (time smoothness of `∂ₜu`, i.e. `C²` in time).
