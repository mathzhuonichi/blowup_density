# Lane 229-R44-prop44 — land S1d on top of the endpoint assembly, instantiate `RCritical2Differential`, make Proposition 4.4's zero-datum conclusion unconditional, and derive Theorem 4.1(ii)'s `q = 2` non-density clause

You are a Lean 4 (v4.34.0-rc2 + Mathlib) proof worker on the repository checked out at your working directory
`/data_8T/ping/blowup_density/.claude/worktrees/229-R44-prop44` (git branch `erenup/229-R44-prop44`, based on lane 227's branch `erenup/227-R44-endpoint` = `origin/erenup/integration`
+ `Section4/R44/Endpoint.lean` (27 declarations: `structure RCritical2Differential`, the fixed constants `theta = min R43.criticalConst (1/(100·(A05.criticalL3Const+1)^3))`, `C₂ = 2`, `C₃ = 4`,
`radius ν S = theta/20 · ν^(3/2) · exp(−3νS)`, and `rcritical2_endpoint_of_differential : (∀ T w, RCritical2Differential w hf) → forceSobolevENormL2 (-1/2) f < ofReal (radius ν S) →
ofReal S < maximalLifespanR ν (fun _ => 0) f`, plus the skeleton `rcritical2_endpoint`); read it and `research/R44/REPORT_227.md`). Lane 228 (branch `erenup/228-R44-s1d-absorption`, **not merged**,
read it with `git show erenup/228-R44-s1d-absorption:formalization/NSFormalization/Section4/R44/Absorption.lean` and `git show erenup/228-R44-s1d-absorption:research/R44/REPORT_228.md`) proved
the S1d absorption `rcritical2_differential` with **its own** constants `theta₂₂₈ = (4·trilinearConstJ)⁻¹`, `C₂ = 3/2`, `C₃ = 3`, one integrable `E'` on every `[0,S]`, `S < T`, the
`energyAdvectionJPairing_eq` bridge (222↔220), and its own copy of the structure — which **collides** with 227's names in namespace `R44`. Your job is the integration.
Also read `research/R44/R44_SPLIT.md` (S1–S6, the gap ledger), `research/R44/Spec.lean:169-230` (`RCritical2API` — the `main` field and any `q = 2` consequence), lane 224's
`Section4/R41/NonDensityL1.lean` (**template** for the non-density clause: `forceSobolevENorm_mono_order` (general `q`), `nonDensityZero_L1`, `not_breakdownDenseR_zero_L1`, the local
restatements of `breakdownSetRZero`/`RelativelyDense`/`BreakdownDenseR` — import them, do not duplicate), `research/R41D/REPORT_224.md`, `verification/Contracts/V1/Thresholds.lean`
(`l2 : exponent 2 s = -1/2 - s`, so `s₂ = -1/2`), `research/section4/STATEMENTS.md:140-146` ("From R44 (Prop. 4.4) exactly one consequence, applied at `S = T`: there is `r_{ν,T} > 0` such
that `{f ∈ F_R : ‖f‖_{L²_tH^{-1/2}} < r_{ν,T}}` contains no element of `B^R_{ν,0,T}`; with `‖·‖_{H^{-1/2}} ≤ ‖·‖_{H^s}` for `s ≥ −1/2`, same conclusion in `L²_tH^s`"), the paper
`04-whole-space.tex:136-181`, `CLAUDE.md`, `collaboration/HANDOFF.md` §0 and §2 P6/P10, and the top 40 lines of `logs/LESSONS.md`.

## Ground rules (non-negotiable)
- Work ONLY inside this worktree (reading other branches with `git show` is fine); never `git push`, merge or rebase; committing on your branch is allowed.
- Lean: `. scripts/lean-env.sh`; `lake` only from `verification/` with `LEAN_NUM_THREADS=6`.
- No `sorry`/`admit`/`axiom`/`native_decide`; `set_option maxHeartbeats N in` only per declaration, `N ≤ 400000`, commented. **No edits to modules on `origin/erenup/integration`**; 227's
  `Endpoint.lean` is on your base but not yet merged — you may NOT edit it either (its PR is in review); put everything in new files.
  Every declaration must print exactly `[propext, Classical.choice, Quot.sound]`; non-vacuity `example`s (`f = 0`).
- **Statement fidelity:** the non-density theorem must be `Data.lean`'s `BreakdownDenseR ν (fun _ => 0) T 2 s` vocabulary exactly as lane 224 did at `q = 1`.

## Goal
1. New module `Section4/R44/Absorption.lean` = lane 228's file **adapted**: import `R44.Endpoint`; delete 228's duplicate `theta`/`C₂`/`C₃`/`RCritical2Differential` (keep 228's constants
   under distinct names, e.g. `thetaAbs`, `C₂Abs`, `C₃Abs`), keep all its theorems, and add `rCritical2Differential_of_classical : ∀ T w, RCritical2Differential w hf` for 227's structure
   with 227's constants by monotonicity (`theta ≤ thetaAbs` — check `1/(100(C_emb+1)³) ≤ 1/(12·C_emb³)` and `criticalConst`'s definition — and `3/2 ≤ 2`, `3 ≤ 4`; the RHS is monotone in the
   constants, the hypothesis `Y ≤ theta·ν` implies `Y ≤ thetaAbs·ν`). Credit lane 228 in the module docstring.
2. New module `Section4/R44/Prop44.lean`: `rcritical2_endpoint_unconditional : ∀ ν S, 0 < ν → 0 < S → ∀ f, MemForceR f → forceSobolevENormL2 (-1/2) f < ofReal (radius ν S) →
   ofReal S < maximalLifespanR ν (fun _ => 0) f` (instantiate 227's theorem), and the `RCritical2API` field(s) this proves (copy the spec's binders; explicit `radius`).
3. New module `Section4/R41/NonDensityL2.lean`: `nonDensityZero_L2 : ∀ ν T, 0 < ν → 0 < T → ∀ s, -1/2 ≤ s → ∃ ρ > 0, ∀ f ∈ breakdownSetRZero ν T, ofReal ρ ≤ forceSobolevENorm 2 s f` with
   `ρ = radius ν T` (contrapositive of item 2 at `S = T`: `f ∈ breakdownSetRZero` ⇒ `maximalLifespanR ≤ ofReal T` ⇒ `¬ (‖f‖_{L²H^{-1/2}} < ofReal (radius ν T))` ⇒ order monotonicity
   `forceSobolevENorm_mono_order 2 (-1/2) s`), and `not_breakdownDenseR_zero_L2 : … → ¬ BreakdownDenseR ν (fun _ => 0) T 2 s` (224's pattern; threshold `-1/2` cites `Thresholds.lean` `l2`).
4. Audits with `Contracts.V1.Data`-vocabulary conformance (224/227 style); non-vacuity examples.

## Deliverables
1. The three modules above.
2. Records `research/R44/ATTEMPTS_PROP44.md`, update `R44_SPLIT.md` (S1–S6 closed; what of `RCritical2API` is proved) and `research/R41D/COMPARISON.md` (both `nonDensityZero` instances now proved),
   conformance `research/R44/axioms_prop44.lean`, `research/R41D/axioms_nondensity_l2.lean`.

## Gates
`cd verification && LEAN_NUM_THREADS=6 lake build NSFormalization.Section4.R44.Absorption NSFormalization.Section4.R44.Prop44 NSFormalization.Section4.R41.NonDensityL2` (silent),
`lake env lean` on each module (0 output), the audits, `make check`.

## Report
Commit on your branch; end with four parts. Also write it to `research/R44/REPORT_229.md`.
