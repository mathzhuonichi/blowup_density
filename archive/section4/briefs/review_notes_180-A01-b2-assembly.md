1. The lane did NOT prove the requested `carrierConstructor_of_localTheory`; it reports three residuals. Verify each is genuine (reproduce the quoted errors from `research/A01/probes/rev180_*_fail.lean`): (a) `ClassicalSolutionR.sobolev` quantifies over **every** order `m : ℕ` while the cylinder pair carries order `q+1` only — confirm from `Section4/A02/SolutionClass.lean` and state that the constructor from a finite-order local theory needs the order-independent-horizon unit A3 (all orders on one `Icc 0 S`); (b) the divergence bridge needs a family of smooth representatives; (c) pressure smoothness needs `ContDiff` of `pressureGradientOfVelocity`. Are these the true remaining B1/B2 obligations, or could any be discharged from tree lemmas the worker missed (grep `Section4/{A01,D01,C01}` and — important — `git fetch origin main` and read `origin/main`'s `Section4/A01/ForcedMaximalRegularity.lean`, `ForcedSourceUpgrade.lean`, `HeatGradientTrace.lean`, `ConstructorDatumPath.lean`, `ConstructorPieces.lean` and `research/A01/IMPLEMENTATION_PLAN_169.md` via `git show`, which the owner's team wrote for the same constructor)? Report per residual: closed on main / open everywhere / partially.
2. `constructedVelocity` (clamped carrier, constant after `S`): is the slice identity `constructedVelocity_slice` exactly `∀ t : Icc 0 S, (fun x => velocity (↑t,x)) =ᵐ ⇑(U t)`, and is the clamp compatible with `Ico 0 (S+1)` as the solution horizon (why `S+1`)?
3. `CarrierConstructorFull` and `rows_from_constructor_full`: token-identical to `research/A01/probes/rev157_constructor_loop.lean` / `rev158_ctor_restated.lean` (read them from `origin/main` if absent here)?
4. Gates as usual; non-vacuity on the zero pair; one substantive mutation.
