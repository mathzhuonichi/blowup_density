# Lane 163 verification and scope (2026-09-15)

## Result

`ConstructorDivergence.lean` pulls the genuine cylinder divergence constraint back
through the isometric ordinary L² lift. It then proves pointwise zero divergence
for the supplied actual smooth representative. The localTheory consumer retains
all of that theorem's original hypotheses, including its a priori bound input.
This closes constructor component c6, not the classical constructor.

## Evidence

Astra low wrote the Lean code; a second Astra low writer independently reviewed
the closed-gradient-space argument and the actual representative/consumer types.
Luna high compiled the module (exit 0) and the corrected research probe (exit 0).
All six exported results and two consumers report only `propext`,
`Classical.choice`, and `Quot.sound`.

Commands from `verification/`:

- `lake build NSFormalization.Section4.A01.ConstructorDivergence`: passed.
- `lake env lean ../research/A01/axioms_constructor_divergence.lean`: passed.

The lead ran the existing Python checks with the installed Python runtime:
`check_formalization_plan.py --check`, `check_contracts.py`,
`test_contract_policy.py` (13 tests), `check_work_queue.py`, and
`check_contracts.py --base-ref origin/main`: all passed. The first run of four
checks hit the Windows GBK default decoding; rerunning with process-only
`PYTHONUTF8=1` passed without source changes. Policy tests used process-only
`core.autocrlf=false` for their temporary Git repositories.

Logs are ignored local files under `tmp/`; detailed proof attempts and source
review are in `research/A01/ATTEMPTS_DIVERGENCE.md` and
`REVIEW_CONSTRUCTOR_DIVERGENCE_163.md`.

## Scope correction and remaining work

`REVIEW_CONSTRUCTOR_FORCE_SCOPE.md` identifies insufficient time regularity in
the old `CarrierConstructorFull` research target. Its counterexample route is
mathematical analysis, not a Lean-formalized counterexample. The original A01
parent already assumes actual `MemForceR f`; future construction must retain
that hypothesis and the actual input-output identities. The old Lean target,
frozen contracts, acceptance tests, dependencies, and manuscript are unchanged.
`CONSTRUCTOR_SPLIT.md` now records the completed c6/finite-order datum steps and
the corrected future target scope.

Common-horizon all-order regularity, time smoothness, pressure recovery and the
actual constructor remain open. The primary worktree's independent manuscript
revision and reference changes were not touched.

## PR management

This lane is based on PR #162 (`codex/161-a01-datum-path`, `a4e18a2`).
The parent remains unmerged. Cloud CI has a known account billing/limit blocker;
local success does not claim cloud CI success or authorize bypassing protection.

## Full local regression results

The assigned Luna run also completed `lake test`: all 26 registered contract
checks report standard logical axioms only (`tmp/lake_test_163.log`).
`experiments/test_contract_mutations.py --skip-build` passed with the ordinary
implementation refactor accepted and the admitted-proof, extra-axiom and
weakened-hypothesis variants rejected (`tmp/mutations_163_r2.log`). Its first
launch lacked Lake on PATH; rerunning in the established elan environment
resolved the launch issue without changing tests.

The independent route audit `research/A01/NEXT_CONSTRUCTOR_ROUTE_163.md` identifies
the next non-circular step: connect the actual forced mild witness to existing
Sobolev maximal regularity on the same horizon. That source result supplies only
L²-in-time higher regularity, not a continuous all-order tower.
