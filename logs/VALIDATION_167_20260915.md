# Lane 167: maximal-family H2 endpoint estimate

## Mathematical result and scope

This lane follows PR #166 at b436f72. The target is R43 gap G5, the passage in
paper/sections/04-whole-space.tex:119-132 from ordinary energy and absorption
on shorter classical intervals to an H2 squared time integral at a finite
maximal lifespan. The two production declarations are in
`NSFormalization.Section4.R43.MaximalEndpoint`.

For each 0<t<S, choose t<r<S and obtain the actual classical solution from
the supplied A02.IsMaximalSolution predicate. Its velocity is literally the
given u. The existing C01 Ioc estimate already uses a uniform budget at S;
the existing countable directed union then covers Ioo 0 S. This does not
request a classical solution at S. The estimate retains all three terms,
with coefficient 32, and uses the canonical angular Sobolev norm. The A04
corollary uses the existing natural-square/real-power equality.

The research consumers transport the actual frozen V2 maximal-family predicate
and lifespan through existing bindings. The absorption constant is the inherited
V4 C1, and the initial gradient uses the frozen Frobenius norm bridge.

This is conditional on the actual absorption bound throughout Ico 0 S. It does
not prove the critical estimate, its smallness bootstrap, maximal-family
existence, or unconditional continuation. No new contract, registry entry,
or replacement solution predicate is introduced.

## Roles and source review

Lean authoring: gpt-6-astra, low. Independent source review: gpt-6-astra, low,
recorded in research/R43/REVIEW_ENDPOINT_167.md (source ACCEPT). All Lean builds,
consumer checks and mutation execution are assigned to gpt-5.6-luna, high.
The lead manages worktrees/PRs and the original Python administrative checks.

## Verification evidence

The five original administrative checks completed with native exit 0:
check_formalization_plan --check, check_contracts, test_contract_policy,
check_work_queue and check_contracts --base-ref origin/main. Logs are
tmp/check_{plan,contracts,policy,queue,base}.log. Policy has 13 passing tests.

The first module build exited 1 on a final definitional slice equality in the
natural-square corollary. The writer removed an unused simp argument and added
rfl; the mathematical statements are unchanged. The second module build passed.
The first consumer probe then failed because `simpa using` did not unfold the
source gradient norm at the required transparency. The successful consumer
uses the existing V4 binding pattern: simplify the target through the gradient
bridge, then apply the source theorem with `exact`. The statements are unchanged.

| Check | Native exit | Log in tmp |
| --- | --- | --- |
| MaximalEndpoint r2 build, 10389 jobs | 0 | build_167_R43_MaximalEndpoint_r2.log |
| Four transitive axiom outputs, probe r3 | 0 | probe_167_axioms_endpoint_r3.log |
| Full lake test, 27 registered contracts / 29 checked outputs | 0 | lake_test_167_endpoint.log |
| Original mutations | 0 | mutations_167_endpoint.log |

The two extra checked outputs belong to existing V4 auxiliary audits, not new
registry entries. All four new audited declarations use only propext,
Classical.choice and Quot.sound. Mutations accept refactoring and reject an
admitted proof, an extra axiom and a weakened hypothesis. Native exits were
reported by the assigned Luna compiler; the lead also read the actual audit
and build outputs. This evidence supplements the independent source ACCEPT.

No rendering or new infrastructure checks are claimed. The main worktree's
independent manuscript/reference changes remain untouched. Cloud handoff is recorded below;
required CI/review remain pending; earlier cloud jobs could not start due to
account billing/spending limits.

## Cloud handoff

PR #168: https://github.com/mathzhuonichi/blowup_density/pull/168, based on PR #166.
Proof commit e2d05ad25246ef094a21c9713dc625b187fe437a is pushed. Architecture check
104329375348 could not start because account payments failed or the spending limit
needs increasing; lean-contracts was skipped. The PR is open and unmerged.

The changed-module selector dry run against b436f72 selected exactly
NSFormalization.Section4.R43.MaximalEndpoint (exit 0). This verifies CI selection;
the actual local build is recorded above.
