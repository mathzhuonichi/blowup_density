# 进展总结（2026-09-13，erenup 侧）

范围：用 Lean 4 证 `paper/blowup_density.tex` 第 4 节（全空间 R³，定理 4.1–4.7）。截至本文：34 条车道（001–034），75 次 agent 运行（`logs/AGENT_RUNS.csv`），`erenup/integration` 上 9 条注册合同，全部传递公理 = `propext, Classical.choice, Quot.sound`。

## 1. 做了什么

- 工作系统：集成分支 `erenup/integration`（我们自己合），编号 worktree/分支/PR（`NNN-TaskID-slug`），lane 协议（claim → 2 份盲稿比对 → opus worker → opus reviewer 跑 Lean → lead 合入记 CSV），`CLAUDE.md` / `PLAN.md` / `NEXT_SESSION.md` / `logs/AGENT_RUNS.csv`，自包含 Lean（`.elan` + 共享 Mathlib 缓存 + worktree 软链），CI 四处修复（磁盘、缓存、超时、取消策略），JSON 三方合并脚本。
- 数学：D01 定义层定稿并把单元 L2 全关；I01→I02→I03→R42 插入脊柱注册到定理 4.2（除寿命同定）；A03/A05 分析工具箱注册；A02 非分析单元证完；A01/A02/A04/B01/B02 等 11 份 spec 经审；第 4 节陈述台账 v2。

## 2. 完成了什么（可验证）

| 合同 | 内容 | 来源 |
|---|---|---|
| `R41.threshold_arithmetic` | 指数算术（owner） | main |
| `I01.packet` | OpenAI 紧支包 + 引理 2.2 + 零延拓 | 007 |
| `I02.correction` / `_v2` | 引理 3.4–3.5 的 R³ 内容（73 条）；V2 加"调用方钉 K ⊆ plateau" | 010/029 |
| `I03.scaling` | 命题 3.3 缩放搬运、eq:REclose、正/负范数缩放、力收敛 | 012 |
| `A05.gradient_l6` | 引理 B.1 ‖∇v‖₆ ≤ C‖Δv‖₂ + Hessian–Laplacian 恒等式 | 019 |
| `A03.bounded_representative` | 引理 A.1 的 H² ↪ L^∞（jet 形式） | 023 |
| `A03.tame_products` | 引理 A.1 的 tame 积（eq:Rproduct/algebra/tame、advection 界） | 026 |
| `R42.insertion_family` | 定理 4.2 的 (u_ε,p_ε,g_ε) 除寿命同定外全部子句 | 027 |

非合同但已证（标准公理）：D01 四模块（jets⇔datum、齐次见证、F_R 闭包与 g_ε ∈ F_R、切片提取）；A02 U4+U6（restrict/congruence/压力归一化/六个序论字段，待审）；HeliCorgi 88 模块在我们的 pin 下零改动编译。

## 3. 失败与负例（都已写进 `research/<ID>/ATTEMPTS*.md` 或 REVIEW）

| 事件 | 教训 |
|---|---|
| D01 草稿 A 的 F_R 漏了 C^∞ → 定理 4.1(ii) 变假，REJECT | 两份盲稿比对是有效的；陈述保真靠比对不靠 kernel |
| I03 `scalingStatement` 不可满足；`force_carrier_subset` 惰性前提 | spec 要先 typecheck 再问"能不能被错的实现满足" |
| A04 卡片证据 `ScalarEnergyContinuation.lean` 是 36 行转发，与 continuation 无关；A02 卡片证据也不是最合适的定理 | 卡片证据必须重验；真正的资产往往在别处（`classical_uniqueness_on_Icc`、Euler 高阶能量） |
| Mathlib 没有连续变系数 Grönwall（只有常系数/离散） | A04 单元 G3 是新数学，要单独建 |
| `ClassicalSolutionR` 只在合同里定义，本地重述是另一个类型，`rfl` 桥不可能 | 结构体用逐字段双向转换 + 往返引理；本地重述只留一份 |
| 在 `formalization/` 下跑 lake 会重新 clone + 从源码编 Mathlib（worker 一次、lead 一次） | lake 只从 `verification/` 跑；安装脚本已加软链防护 |
| CI：25 分钟超时 → ENOSPC → 失败不存缓存 → 120 分钟仍被掐（冷 `lake test` 78 分钟 + 改动模块） | 现 180 分钟 + 失败也存缓存 + vendor 缓存；记账 push 要等 CI 结束 |
| 合同不能 import `NavierStokes.*` → 内联 20 个定义 + rfl 桥 → 政策放宽为 7 模块白名单 | 政策变更单独标注交 owner |
| `tasks.py render` 覆盖手写 Attempts | Attempts 只放 `research/<ID>/` |
| 6 处无效 `maxHeartbeats` 提升、绑定名冲突、rebase 卡两 commit | reviewer 跑 Lean 这一步便宜且抓得住 |
| R42 寿命子句：无合同给 `g_ε ∈ F_R`（已补）、非紧支 u_ε 的 `sobolev` 字段无人证 | 组合时才暴露的缺口要反向加 DAG 边（A03→R42、A03→A02） |
| I03 U7c 卡 datum 路径强可测 | 齐次范数与时间可测性要一起设计 |

## 4. 地基（可以继续往上压的）

1. `Contracts/V1/Data.lean` 63 定义 + D01 四模块：下游所有陈述都用它们。
2. A03 两条 + A05 一条：Grönwall/能量估计需要的全部不等式已在 jet/datum 层可用。
3. I01→I02→I03→R42：定理 4.2 只差寿命同定，且已知差什么。
4. A02 的 U4/U6 + 本地 `classical_uniqueness_on_Icc`：唯一性在正确载体上，只差把 `ClassicalSolutionR` 的能量/上界喂进去。
5. HeliCorgi 移植：A01 的 mild 续接单元 A2b 有现成来源。
6. 流程：合并脚本、CI 缓存、review 协议、记录文件。

## 5. 宏观进展表（按 DAG 节点）

| 节点 | 内容 | 状态 | Lean 里有什么 | 下一步 / 阻塞 |
|---|---|---|---|---|
| U01–U04 | 三个源码库 + Tao/Taylor 参考 | 参考可用 | `EXTERNAL_REUSE.md` 索引 | — |
| U05 | HeliCorgi 工具链兼容 | 完成 | 88 模块 `Formal` + 4 个 `FormalPatched` | 报告归档 |
| D01 | 定义、力类、范数、压力 | 定义定稿；L2 全关 | `Data.lean` + 4 模块 | 034 打包成 `D01.datum_lemmas` 合同（进行中） |
| A01 | 带外力 R³ 局部理论 | spec + 策略备忘 | 无证明 | 单元 A2 = A04 的 G1/Z1/G2，共建；A2b 走 HeliCorgi；卡 U7c 型可测性问题少 |
| A02 | 唯一性与最大寿命 | spec 通过；U4+U6 证完（待审）；U1a 进行中 | `A02/Restrict.lean`、`Order.lean` | U1b（∇ 阶移 + 物理导数识别，L）→ U2/U3/U5 → U7/U8 |
| A03 | tame 积 + 有界代表元 | 两条合同已注册 | `TameProduct.lean`、`BoundedRepresentative.lean` | 三个未注册字段（MemHInfty 版 advection 等）用 025 关掉 |
| A04 | 平方 H² continuation | spec 完成（review 中） | 无证明 | G3 变系数 Grönwall（新）；G1 forced viscous eq:Rhigh（L） |
| A05 | 临界嵌入 | `gradient_l6` 已注册 | `GradientL6.lean` | `CriticalEmbeddingAPI` 其余子句未注册 |
| C01 | 能量 + H¹ 吸收 | spec 进行中（031） | 无 | spec 回来后拆单元；依赖 A05/A02 |
| I01 | 包能量与早期消失 | 合同已注册 | `I01/*` | — |
| I02 | 局部势与光滑修正 | V1 + V2 已注册 | `I02/*` | — |
| I03 | 同族缩放与负范数 | 合同已注册 | `I03/*` | U7c（齐次范数缩放，卡强可测）；V2 带 `V2.CorrectionAPI` |
| R42 | 定理 4.2 精确插入 | 合同已注册（除寿命） | `R42/Assembly.lean` | V2：`hg : MemForceR g` + 非紧支 u_ε 的 `sobolev` + A02 最大寿命 |
| R43 / R44 | 命题 4.3 / 4.4 | 台账 v2 有伪 Lean 骨架 | 无 | 等 A04 + C01 spec 定稿后写 spec |
| R41 / R41D | 定理 4.1 双阈值 / 密度分支 | 未开始（owner 有阈值算术） | `R41.threshold_arithmetic` | 依赖 R42 + R43/R44；R41D 建议参数化 |
| R45 | 推论 4.5 紧支/快衰减类 | 未开始 | 无 | 依赖 R41（建议加 R41D → R45） |
| B01 / B02 | 正时间 Bochner 逼近 / 齐次 H⁻¹ 逼近 | spec 通过 | 无 | B01 主字段现成（`exists_angular_real_vector_positive_physical_approx` + 028 时间正则搬运），可开合同 |
| R46 | 命题 4.6 密度与轨道 | 未开始 | 无 | 依赖 B01/B02 + I03 齐次部分 |
| G01 / R47 | 有限网格观测 / 定理 4.7 | 未开始 | 无 | 依赖 I02；最靠后 |
| T01–T04 | 环面（第 3 节） | deferred | — | 不做 |

## 6. 接下来

- 本波（5 槽位）：031 C01 spec、033 A02 U1a、034 `D01.datum_lemmas` 合同；030、032 的 reviewer。
- 下一波（035 起）：A02 U1b + U2/U3/U5；A04 单元 G3（变系数 Grönwall）与 G1；A01 A2 共建；C01 证明单元；B01 合同；R42 V2；I03 U7c；本地重述去重（MAINT）；R43/R44 spec。
- 交 owner：PR #15（9 条合同）、白名单政策、CI 改动、DAG 加边建议。
