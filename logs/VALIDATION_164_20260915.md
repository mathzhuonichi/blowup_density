# Lane 164: H² comparison and terminal time integral (2026-09-15)

## Result

`SobolevTwo.lean` proves the genuine angular-datum order-two norm bound
with universal constant CH2=16. Actual weak derivatives at orders 0, 1 and 2
are bounded using integration by parts and the Hessian/Laplacian identity;
the existing quantitative datum constructor gives the Sobolev norm estimate.

`H2TimeIntegral.lean` combines that result with the ordinary L² estimate and
the true enstrophy integral bound. A single universal Cassembly=32 controls all
three terms of the original estimate. A countable directed exhaustion by
strictly earlier closed-right intervals proves the lower-integral bound on
(0,S), including S=T. It neither evaluates a terminal velocity nor assumes
unproved measurability of its Sobolev norm. The zero-datum version uses the
same constant.

## Validation

Astra low wrote the code and an independent Astra low reviewer checked the
norm bridge, constants, integrability and terminal interval argument. Luna
high performed all Lean compilation. `lake build
NSFormalization.Section4.C01.H2TimeIntegral` passed, including SobolevTwo.
The research probe `lake env lean ../research/C01/axioms_h2_164.lean` outputs
only `propext`, `Classical.choice`, and `Quot.sound` for three exact original
Spec consumers, the S=T finite-integral consumer, and two analytic helpers.
The consumers use the existing non-definitional Frobenius gradient bridge.

The lead ran the five original Python administrative checks: formalization
plan, contracts, policy (13 tests), work queue, and compatibility against
origin/main. All pass. The first four non-policy attempts hit the Windows
GBK default codec; process-only PYTHONUTF8=1 resolved this without source
changes. Policy temporary repositories use process-only core.autocrlf=false.
The author checked UTF-8 without BOM, LF and single final newlines on all
new source/research files. `git diff --check` passes.

Detailed failed proof attempts and subsequent fixes are retained in
`research/C01/ATTEMPTS_H2_164.md`; the independent source review is
`research/C01/REVIEW_H2_164.md`. Local command logs live under ignored `tmp/`.

## Remaining scope

This proves the remaining H² analytic fields of the original C01 research
specification. It does not register a full C01 contract or complete Section 4.
Full contract registration must follow independent paper-first statement
review and preserve frozen V1/V2/V3 interfaces and tests. No dependencies,
existing contracts/tests, manuscript or unrelated primary-worktree edits were
changed. The original manuscript remains the formalization target.

## PR management

This lane is based on PR #163, branch codex/162-c01-enstrophy at 032e5bb.
The parent remains unmerged. GitHub CI has a known billing/limit blocker;
local validation does not claim successful cloud CI or bypass protection.

## Full local regression results

Luna's `lake test` exited 0 and all 26 registered contracts report standard
logical axioms only (`tmp/lake_test_164.log`, `tmp/compile_session.log`).
`experiments/test_contract_mutations.py --skip-build` passed: implementation
refactor accepted; admitted proof, extra axiom and weakened hypothesis rejected
(`tmp/mutations_164.log`). These are infrastructure regressions, distinct from
the newly proved analytic estimates and their explicit conformance consumers.
