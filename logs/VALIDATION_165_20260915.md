# Lane 165: full C01 contract registration (2026-09-15)

## What this registers

`C01.energy_absorption_v4` extends the frozen V3 record and adds the six
remaining fields of the original C01 specification: enstrophy identity,
differential absorption and integrated absorption, the genuine angular H²
comparison, and the general/zero-datum H² time bounds. Constants are globally
selected before all data; the binding uses CRH1=2, CH2=16 and Cassembly=32.
The time-integral estimates retain 0<S≤T and therefore include S=T.

## Independent statement and binding review

Two fresh Astra low agents independently read only the original manuscript
and frozen contract vocabulary before writing `BLIND165_A.*` and
`BLIND165_B.*`. They did not read implementations, the old C01 Spec or one
another's draft during that phase. `COMPARISON_165.md` records their actual
differences: the real integral convergence clause, datum-path versus canonical
norm language, threshold form, powers, universal constants and endpoint scope.

The lead independently compared all six selected field bodies to the original
Spec; they match after whitespace normalization. A separate independent source
review accepts the final structure, including the existing datum uniqueness,
exact norm bridge and half-open-interval continuity that give the lower-integral
bound its intended measurable physical meaning. No stronger path regularity
premise is added. The four underlying proof modules required no simplification
or reorganization in the review. The library does not yet expose a dedicated
arbitrary-datum real-integral consumer; that packaging limitation is disclosed
in the review, not replaced by a new hypothesis.

## Validation already observed

All Lean execution is assigned to Luna high. Both blind draft types compile
(the A draft needed only a syntax correction from ν⁻² to `(ν⁻¹)^2`). The V4
contract and `Tests.EnergyAbsorptionV4` build successfully. Actual axiom audits
pass for the checked full record, exact V3 parent projection and S=T finiteness
consumer. The six explicit field-shape tests use the original statement types.

The five existing Python checks all pass: formalization plan, contract registry,
13 policy tests, work queue and compatibility against origin/main. Windows
execution uses process-only PYTHONUTF8=1 and core.autocrlf=false. The task cards
were produced by the existing generator, then its Windows output line endings
were restored to the repository's LF convention; only the C01 card and queue
entry have content changes.

## Scope and PR discipline

This lane is based on PR #165 at 31621e5. All existing Contracts/V1/V2/V3 files,
registered acceptance tests and registry entries are preserved. New specification
imports only the previous contract; bindings reuse the existing solution
conversion and Frobenius gradient bridge. No research draft enters the checked
proof closure. `research/R43/R43_SPLIT.md` now distinguishes completed C01
fixed-horizon supply from its still-open maximal-family endpoint and critical
energy obligations.

This completes a full C01 interface locally when the final regressions pass;
it does not complete Section 4, classical local construction, continuation or
critical half-order estimates. Required cloud CI/review and parent PRs remain
part of the merge process. The primary worktree's independent manuscript and
reference edits are unchanged.

## Final local regression evidence

`lake test` completed all 27 registered contract tests. The extra V4 parent
projection and terminal consumer are auxiliary audits, not extra registry
contracts. `tmp/lake_test_165_v4.log` records the checked declarations.
The deliberate no-smallness consumer failed with exactly the expected missing
absorption-function argument (`tmp/probe_165_no_smallness.log`). This checks a
load-bearing premise of the consumer, not a mathematical counterexample.
The original mutation suite passed: refactor accepted; admitted proof, extra
axiom and weakened hypothesis rejected (`tmp/mutations_165_v4.log`).

The independent final binding/registry review is ACCEPT in
`research/C01/REVIEW_BINDING_165.md`. Its source and initial-build conditions
are supplemented by the final regressions above. The C01 work item is now
assembly/in-review with the additional V4 registry id; its regenerated card
and work queue pass the original consistency check. All earlier accepted
records remain registered and unchanged.

The source-backed next step is written in `research/C01/NEXT_R43_ENDPOINT_165.md`:
transfer this fixed-horizon supply to the actual maximal family by uniformly
bounding shorter intervals. That theorem is proposed, not proved by this lane.
