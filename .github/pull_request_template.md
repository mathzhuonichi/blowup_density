## Task and scope

Task ID:
Work kind: specification / compatibility / proof / assembly / maintenance
Contract ID and version (if applicable):

## Mathematical or behavioral change

State the exact output and hypotheses. Explain which existing upstream result
is reused and which adapter is new. Distinguish a component from its parent theorem.

## Validation

List commands actually run and their results, including `make check`,
`make test` and `make test-mutations` where applicable.
For manuscript changes include `make paper`.

## Review boundary

Describe remaining obligations, definition/contract changes, dependency upgrades
and any unchecked scope. Keep existing acceptance statements and tests intact.
